diff --git a/app/index.tsx b/app/index.tsx
index 604c0e0..813a050 100644
--- a/app/index.tsx
+++ b/app/index.tsx
@@ -46,7 +46,7 @@ export default function LabsIndexScreen() {
       },
       {
         title: "Thread Lab",
-        description: "Isolated public comment rows + private bubble thread environment for trade conversations.",
+        description: "Real entry flow for public comments and private bubble threads.",
         href: "/thread-lab",
         badge: "new",
       },
diff --git a/features/thread-lab/ThreadLabScreen.tsx b/features/thread-lab/ThreadLabScreen.tsx
index c07fbe1..2ff7fe7 100644
--- a/features/thread-lab/ThreadLabScreen.tsx
+++ b/features/thread-lab/ThreadLabScreen.tsx
@@ -12,24 +12,16 @@ import {
 import { useTheme } from "@/providers/ThemeProvider";
 
 import { threadLabScenario } from "./threadLab.mockData";
-import type {
-  ThreadLabMode,
-  ThreadMessage,
-  ThreadParticipant,
-} from "./threadLab.types";
+import type { ThreadMessage, ThreadParticipant } from "./threadLab.types";
 
-const MODES: Array<{ key: ThreadLabMode; label: string; helper: string }> = [
-  {
-    key: "public",
-    label: "Public",
-    helper: "Comment-section rows",
-  },
-  {
-    key: "private",
-    label: "Private",
-    helper: "Two-user bubbles",
-  },
-];
+type ThreadLabView =
+  | "home"
+  | "public"
+  | "private"
+  | "public-menu"
+  | "private-menu"
+  | "details"
+  | "guide";
 
 function getParticipantName(
   participants: ThreadParticipant[],
@@ -51,212 +43,125 @@ function getParticipantInitials(
   );
 }
 
-function HeaderAction({
-  label,
-  onPress,
+function ScreenHeader({
+  title,
+  eyebrow,
+  onBack,
+  onMenu,
 }: {
-  label: string;
-  onPress: () => void;
+  title: string;
+  eyebrow?: string;
+  onBack?: () => void;
+  onMenu?: () => void;
 }) {
-  const { palette } = useTheme();
+  const { mode, palette, toggleMode } = useTheme();
 
   return (
-    <Pressable
-      onPress={onPress}
-      accessibilityRole="button"
-      style={({ pressed }) => [
-        styles.headerButton,
-        { borderColor: palette.border, backgroundColor: palette.surface },
-        pressed ? styles.pressed : null,
-      ]}
-    >
-      <Text style={[styles.headerButtonText, { color: palette.text }]}>
-        {label}
-      </Text>
-    </Pressable>
-  );
-}
-
-function ModeSwitcher({
-  value,
-  onChange,
-}: {
-  value: ThreadLabMode;
-  onChange: (mode: ThreadLabMode) => void;
-}) {
-  const { palette } = useTheme();
+    <View style={[styles.header, { borderBottomColor: palette.border }]}>
+      <View style={styles.headerTopRow}>
+        <Pressable
+          onPress={onBack ?? (() => router.back())}
+          accessibilityRole="button"
+          style={({ pressed }) => [styles.textButton, pressed ? styles.pressed : null]}
+        >
+          <Text style={[styles.textButtonLabel, { color: palette.text }]}>‹ Labs</Text>
+        </Pressable>
 
-  return (
-    <View
-      style={[
-        styles.modeShell,
-        { borderColor: palette.border, backgroundColor: palette.surfaceAlt },
-      ]}
-    >
-      {MODES.map((mode) => {
-        const selected = mode.key === value;
-        return (
+        {onMenu ? (
           <Pressable
-            key={mode.key}
-            onPress={() => onChange(mode.key)}
+            onPress={onMenu}
             accessibilityRole="button"
-            style={({ pressed }) => [
-              styles.modeButton,
-              selected ? { backgroundColor: palette.text } : null,
-              pressed ? { opacity: 0.86 } : null,
-            ]}
+            accessibilityLabel="Thread options"
+            style={({ pressed }) => [styles.iconButton, pressed ? styles.pressed : null]}
           >
-            <Text
-              style={[
-                styles.modeLabel,
-                { color: selected ? palette.background : palette.text },
-              ]}
-            >
-              {mode.label}
-            </Text>
-            <Text
-              style={[
-                styles.modeHelper,
-                { color: selected ? palette.background : palette.muted },
-              ]}
-            >
-              {mode.helper}
+            <Text style={[styles.iconButtonText, { color: palette.text }]}>⋯</Text>
+          </Pressable>
+        ) : (
+          <Pressable
+            onPress={toggleMode}
+            accessibilityRole="button"
+            style={({ pressed }) => [styles.textButton, pressed ? styles.pressed : null]}
+          >
+            <Text style={[styles.textButtonLabel, { color: palette.text }]}>
+              {mode === "dark" ? "Light" : "Dark"}
             </Text>
           </Pressable>
-        );
-      })}
+        )}
+      </View>
+
+      {eyebrow ? (
+        <Text style={[styles.eyebrow, { color: palette.muted }]}>{eyebrow}</Text>
+      ) : null}
+      <Text style={[styles.title, { color: palette.text }]}>{title}</Text>
     </View>
   );
 }
 
-function TradeDetailsPanel({ mode }: { mode: ThreadLabMode }) {
+function FullBleedDivider() {
+  const { palette } = useTheme();
+  return <View style={[styles.fullBleedDivider, { backgroundColor: palette.border }]} />;
+}
+
+function RowButton({
+  title,
+  subtitle,
+  onPress,
+}: {
+  title: string;
+  subtitle?: string;
+  onPress: () => void;
+}) {
   const { palette } = useTheme();
-  const scenario = threadLabScenario;
 
   return (
-    <View
-      style={[
-        styles.detailsPanel,
-        { borderColor: palette.border, backgroundColor: palette.surface },
-      ]}
+    <Pressable
+      onPress={onPress}
+      accessibilityRole="button"
+      style={({ pressed }) => [styles.rowButton, pressed ? styles.pressed : null]}
     >
-      <View style={styles.detailZone}>
-        <Text style={[styles.detailKicker, { color: palette.muted }]}>
-          I NEED
-        </Text>
-        <Text style={[styles.detailTitle, { color: palette.text }]}>
-          {scenario.needTitle}
-        </Text>
-        <Text style={[styles.detailText, { color: palette.muted }]}>
-          {scenario.needDescription}
-        </Text>
+      <View style={styles.rowButtonTextBlock}>
+        <Text style={[styles.rowButtonTitle, { color: palette.text }]}>{title}</Text>
+        {subtitle ? (
+          <Text style={[styles.rowButtonSubtitle, { color: palette.muted }]}>{subtitle}</Text>
+        ) : null}
       </View>
-
-      <View
-        style={[styles.detailDivider, { backgroundColor: palette.border }]}
-      />
-
-      <View style={styles.detailZone}>
-        <Text style={[styles.detailKicker, { color: palette.muted }]}>
-          I OFFER
-        </Text>
-        <Text style={[styles.detailTitle, { color: palette.text }]}>
-          {scenario.offerTitle}
-        </Text>
-        <Text style={[styles.detailText, { color: palette.muted }]}>
-          {scenario.offerDescription}
-        </Text>
-      </View>
-
-      {mode === "private" ? (
-        <>
-          <View
-            style={[styles.detailDivider, { backgroundColor: palette.border }]}
-          />
-          <View style={styles.detailZone}>
-            <Text style={[styles.detailKicker, { color: palette.muted }]}>
-              PRIVATE DEAL CONTEXT
-            </Text>
-            <Text style={[styles.detailText, { color: palette.text }]}>
-              {scenario.proposalSummary}
-            </Text>
-            <Text style={[styles.detailText, { color: palette.muted }]}>
-              {scenario.acceptedDealSummary}
-            </Text>
-          </View>
-        </>
-      ) : null}
-    </View>
+      <Text style={[styles.chevron, { color: palette.muted }]}>›</Text>
+    </Pressable>
   );
 }
 
-function ThreadSummaryStrip({
-  mode,
-  expanded,
-  onToggle,
-}: {
-  mode: ThreadLabMode;
-  expanded: boolean;
-  onToggle: () => void;
-}) {
+function HomeScreen({ onOpen }: { onOpen: (view: "public" | "private") => void }) {
   const { palette } = useTheme();
   const scenario = threadLabScenario;
-  const statusLabel =
-    mode === "public" ? scenario.tradeStatus : scenario.proposalStatus;
 
   return (
-    <View
-      style={[
-        styles.summaryShell,
-        { borderColor: palette.border, backgroundColor: palette.surfaceAlt },
-      ]}
-    >
-      <View style={styles.summaryTopRow}>
-        <View style={styles.summaryTextBlock}>
-          <Text style={[styles.summaryKicker, { color: palette.muted }]}>
-            {mode === "public" ? "PUBLIC THREAD" : "PRIVATE THREAD"}
-          </Text>
-          <Text
-            style={[styles.summaryTitle, { color: palette.text }]}
-            numberOfLines={2}
-          >
-            {scenario.tradeTitle}
-          </Text>
-          <Text
-            style={[styles.summaryMeta, { color: palette.muted }]}
-            numberOfLines={1}
-          >
-            {scenario.tradeMeta}
-          </Text>
+    <>
+      <ScreenHeader title="Conversation lab" eyebrow="THREAD LAB" />
+      <ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={styles.content}>
+        <View style={styles.tradeIntro}>
+          <Text style={[styles.homeTitle, { color: palette.text }]}>Public + private threads</Text>
+          <Text style={[styles.homeTradeTitle, { color: palette.text }]}>{scenario.tradeTitle}</Text>
+          <Text style={[styles.homeMeta, { color: palette.muted }]}>{scenario.tradeMeta}</Text>
         </View>
-        <View
-          style={[
-            styles.statusPill,
-            { borderColor: palette.border, backgroundColor: palette.surface },
-          ]}
-        >
-          <Text style={[styles.statusPillText, { color: palette.text }]}>
-            {statusLabel}
-          </Text>
-        </View>
-      </View>
 
-      <Pressable
-        onPress={onToggle}
-        accessibilityRole="button"
-        style={({ pressed }) => [
-          styles.detailsToggle,
-          { borderColor: palette.border, backgroundColor: palette.surface },
-          pressed ? styles.pressed : null,
-        ]}
-      >
-        <Text style={[styles.detailsToggleText, { color: palette.text }]}>
-          {expanded ? "Hide details" : "Show details"}
-        </Text>
-      </Pressable>
+        <FullBleedDivider />
 
-      {expanded ? <TradeDetailsPanel mode={mode} /> : null}
-    </View>
+        <Text style={[styles.sectionTitle, { color: palette.text }]}>Conversations</Text>
+
+        <RowButton
+          title="Public thread"
+          subtitle={`${scenario.publicMessages.length} comments · Ask openly`}
+          onPress={() => onOpen("public")}
+        />
+        <FullBleedDivider />
+        <RowButton
+          title="Private thread"
+          subtitle="Proposal and deal conversation"
+          onPress={() => onOpen("private")}
+        />
+        <FullBleedDivider />
+      </ScrollView>
+    </>
   );
 }
 
@@ -273,40 +178,31 @@ function PublicMessageRow({
 
   return (
     <View style={styles.publicRow}>
-      <View
-        style={[
-          styles.avatar,
-          { borderColor: palette.border, backgroundColor: palette.surfaceAlt },
-        ]}
-      >
-        <Text style={[styles.avatarText, { color: palette.text }]}>
-          {initials}
-        </Text>
+      <View style={[styles.avatar, { backgroundColor: palette.surfaceAlt }]}>
+        <Text style={[styles.avatarText, { color: palette.text }]}>{initials}</Text>
       </View>
       <View style={styles.publicBody}>
         <View style={styles.messageMetaRow}>
-          <Text style={[styles.messageAuthor, { color: palette.text }]}>
-            {name}
-          </Text>
-          <Text style={[styles.messageTime, { color: palette.muted }]}>
-            {message.createdAtLabel}
-          </Text>
+          <Text style={[styles.messageAuthor, { color: palette.text }]}>{name}</Text>
+          <Text style={[styles.messageTime, { color: palette.muted }]}>{message.createdAtLabel}</Text>
           {message.edited ? (
-            <Text style={[styles.messageTime, { color: palette.muted }]}>
-              Edited
-            </Text>
+            <Text style={[styles.messageTime, { color: palette.muted }]}>Edited</Text>
           ) : null}
         </View>
-        <Text style={[styles.publicMessageText, { color: palette.text }]}>
-          {message.body}
-        </Text>
+        <Text style={[styles.publicMessageText, { color: palette.text }]}>{message.body}</Text>
       </View>
       <Text style={[styles.moreText, { color: palette.muted }]}>⋯</Text>
     </View>
   );
 }
 
-function PublicThread() {
+function PublicThreadScreen({
+  onBack,
+  onMenu,
+}: {
+  onBack: () => void;
+  onMenu: () => void;
+}) {
   const { palette } = useTheme();
   const scenario = threadLabScenario;
   const participants = useMemo(
@@ -315,45 +211,24 @@ function PublicThread() {
   );
 
   return (
-    <View
-      style={[
-        styles.threadCard,
-        { borderColor: palette.border, backgroundColor: palette.surface },
-      ]}
-    >
-      <View style={styles.threadIntro}>
-        <Text style={[styles.threadIntroTitle, { color: palette.text }]}>
-          Public comment-section model
-        </Text>
-        <Text style={[styles.threadIntroText, { color: palette.muted }]}>
-          Flat rows, thin dividers, visible to public trade viewers.
-        </Text>
-      </View>
-
-      <View
-        style={[
-          styles.fakeComposer,
-          { borderColor: palette.border, backgroundColor: palette.surfaceAlt },
-        ]}
-      >
-        <Text style={[styles.fakeComposerText, { color: palette.muted }]}>
-          Write a public comment…
-        </Text>
-      </View>
+    <>
+      <ScreenHeader title="Public thread" onBack={onBack} onMenu={onMenu} />
+      <ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={styles.threadContent}>
+        <Text style={[styles.sectionTitle, { color: palette.text }]}>Conversations</Text>
+        <View style={styles.plainComposer}>
+          <Text style={[styles.plainComposerText, { color: palette.muted }]}>Write a public comment...</Text>
+          <Text style={[styles.sendText, { color: palette.text }]}>Send</Text>
+        </View>
+        <FullBleedDivider />
 
-      <View style={styles.publicList}>
-        {scenario.publicMessages.map((message, index) => (
+        {scenario.publicMessages.map((message) => (
           <React.Fragment key={message.id}>
             <PublicMessageRow message={message} participants={participants} />
-            {index < scenario.publicMessages.length - 1 ? (
-              <View
-                style={[styles.rowDivider, { backgroundColor: palette.border }]}
-              />
-            ) : null}
+            <FullBleedDivider />
           </React.Fragment>
         ))}
-      </View>
-    </View>
+      </ScrollView>
+    </>
   );
 }
 
@@ -370,44 +245,23 @@ function PrivateMessageBubble({
   const name = getParticipantName(participants, message.authorId);
 
   return (
-    <View
-      style={[
-        styles.privateBubbleRow,
-        mine ? styles.privateBubbleRowMine : styles.privateBubbleRowOther,
-      ]}
-    >
+    <View style={[styles.privateBubbleRow, mine ? styles.privateBubbleRowMine : styles.privateBubbleRowOther]}>
       <View
         style={[
           styles.privateBubble,
           mine ? styles.privateBubbleMine : styles.privateBubbleOther,
           {
-            borderColor: palette.border,
             backgroundColor: mine ? palette.text : palette.surfaceAlt,
           },
         ]}
       >
-        <Text
-          style={[
-            styles.privateAuthor,
-            { color: mine ? palette.background : palette.muted },
-          ]}
-        >
+        <Text style={[styles.privateAuthor, { color: mine ? palette.background : palette.muted }]}>
           {mine ? "You" : name}
         </Text>
-        <Text
-          style={[
-            styles.privateMessageText,
-            { color: mine ? palette.background : palette.text },
-          ]}
-        >
+        <Text style={[styles.privateMessageText, { color: mine ? palette.background : palette.text }]}>
           {message.body}
         </Text>
-        <Text
-          style={[
-            styles.privateTime,
-            { color: mine ? palette.background : palette.muted },
-          ]}
-        >
+        <Text style={[styles.privateTime, { color: mine ? palette.background : palette.muted }]}>
           {message.createdAtLabel}
           {message.edited ? " · edited" : ""}
         </Text>
@@ -416,7 +270,13 @@ function PrivateMessageBubble({
   );
 }
 
-function PrivateThread() {
+function PrivateThreadScreen({
+  onBack,
+  onMenu,
+}: {
+  onBack: () => void;
+  onMenu: () => void;
+}) {
   const { palette } = useTheme();
   const scenario = threadLabScenario;
   const participants = useMemo(
@@ -425,130 +285,146 @@ function PrivateThread() {
   );
 
   return (
-    <View
-      style={[
-        styles.threadCard,
-        { borderColor: palette.border, backgroundColor: palette.surface },
-      ]}
-    >
-      <View style={styles.threadIntro}>
-        <Text style={[styles.threadIntroTitle, { color: palette.text }]}>
-          Private bubble-thread model
-        </Text>
-        <Text style={[styles.threadIntroText, { color: palette.muted }]}>
-          Conversation first. Proposal/deal details stay collapsed until needed.
-        </Text>
-      </View>
-
-      <View
-        style={[
-          styles.privateStatusStrip,
-          { borderColor: palette.border, backgroundColor: palette.surfaceAlt },
-        ]}
-      >
-        <Text style={[styles.privateStatusTitle, { color: palette.text }]}>
-          Proposal context
-        </Text>
-        <Text style={[styles.privateStatusText, { color: palette.muted }]}>
-          {scenario.proposalSummary}
-        </Text>
+    <>
+      <ScreenHeader title="Private thread" onBack={onBack} onMenu={onMenu} />
+      <View style={styles.privateScreenBody}>
+        <ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={styles.privateMessagesContent}>
+          <Text style={[styles.dayLabel, { color: palette.muted }]}>Today</Text>
+          {scenario.privateMessages.map((message) => (
+            <PrivateMessageBubble key={message.id} message={message} participants={participants} />
+          ))}
+        </ScrollView>
+        <View style={[styles.bottomComposer, { borderTopColor: palette.border }]}>
+          <Text style={[styles.bottomComposerText, { color: palette.muted }]}>Message...</Text>
+          <Text style={[styles.sendText, { color: palette.text }]}>Send</Text>
+        </View>
       </View>
+    </>
+  );
+}
 
-      <View style={styles.privateList}>
-        {scenario.privateMessages.map((message) => (
-          <PrivateMessageBubble
-            key={message.id}
-            message={message}
-            participants={participants}
-          />
-        ))}
-      </View>
+function ThreadMenuScreen({
+  kind,
+  onBack,
+  onOpenDetails,
+  onOpenGuide,
+}: {
+  kind: "public" | "private";
+  onBack: () => void;
+  onOpenDetails: () => void;
+  onOpenGuide: () => void;
+}) {
+  const { palette } = useTheme();
 
-      <View
-        style={[
-          styles.fakeComposer,
-          { borderColor: palette.border, backgroundColor: palette.surfaceAlt },
-        ]}
-      >
-        <Text style={[styles.fakeComposerText, { color: palette.muted }]}>
-          Write a private message…
-        </Text>
-        <View style={[styles.sendButton, { backgroundColor: palette.text }]}>
-          <Text style={[styles.sendButtonText, { color: palette.background }]}>
-            Send
-          </Text>
-        </View>
-      </View>
-    </View>
+  return (
+    <>
+      <ScreenHeader title="Thread options" onBack={onBack} />
+      <ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={styles.content}>
+        <Text style={[styles.sectionTitle, { color: palette.text }]}>What do you want to see?</Text>
+        <RowButton title="See details" subtitle="Trade, proposal, or deal context" onPress={onOpenDetails} />
+        <FullBleedDivider />
+        <RowButton title="See guide" subtitle="How this thread type works" onPress={onOpenGuide} />
+        <FullBleedDivider />
+        <RowButton
+          title={kind === "public" ? "Report thread" : "Report problem"}
+          subtitle="Placeholder action for moderation/support"
+          onPress={onOpenGuide}
+        />
+        <FullBleedDivider />
+      </ScrollView>
+    </>
   );
 }
 
-export function ThreadLabScreen() {
-  const { mode, palette, toggleMode } = useTheme();
-  const [activeMode, setActiveMode] = useState<ThreadLabMode>("public");
-  const [detailsExpanded, setDetailsExpanded] = useState(false);
+function DetailsScreen({ onBack }: { onBack: () => void }) {
+  const { palette } = useTheme();
+  const scenario = threadLabScenario;
 
   return (
-    <View style={[styles.screen, { backgroundColor: palette.background }]}>
-      <Stack.Screen options={{ title: "Thread Lab", headerShown: false }} />
+    <>
+      <ScreenHeader title="Details" onBack={onBack} />
+      <ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={styles.content}>
+        <Text style={[styles.detailPageKicker, { color: palette.muted }]}>OPEN · TRADE</Text>
+        <Text style={[styles.detailPageTitle, { color: palette.text }]}>{scenario.tradeTitle}</Text>
+        <Text style={[styles.detailPageMeta, { color: palette.muted }]}>{scenario.tradeMeta}</Text>
+
+        <FullBleedDivider />
+
+        <View style={styles.detailBlock}>
+          <Text style={[styles.detailPageKicker, { color: palette.muted }]}>I NEED</Text>
+          <Text style={[styles.detailBlockTitle, { color: palette.text }]}>{scenario.needTitle}</Text>
+          <Text style={[styles.detailText, { color: palette.text }]}>{scenario.needDescription}</Text>
+        </View>
 
-      <View style={[styles.header, { borderBottomColor: palette.border }]}>
-        <View style={styles.headerTopRow}>
-          <HeaderAction label="‹ Labs" onPress={() => router.back()} />
-          <HeaderAction
-            label={mode === "dark" ? "Light" : "Dark"}
-            onPress={toggleMode}
-          />
+        <FullBleedDivider />
+
+        <View style={styles.detailBlock}>
+          <Text style={[styles.detailPageKicker, { color: palette.muted }]}>I OFFER</Text>
+          <Text style={[styles.detailBlockTitle, { color: palette.text }]}>{scenario.offerTitle}</Text>
+          <Text style={[styles.detailText, { color: palette.text }]}>{scenario.offerDescription}</Text>
         </View>
-        <Text style={[styles.eyebrow, { color: palette.muted }]}>
-          CONVERSATION LAB
-        </Text>
-        <Text style={[styles.title, { color: palette.text }]}>
-          Public + private threads
-        </Text>
-        <Text style={[styles.subtitle, { color: palette.muted }]}>
-          Mock-only environment for comment rows, private bubbles, and collapsed
-          trade/deal details.
-        </Text>
-        <ModeSwitcher
-          value={activeMode}
-          onChange={(nextMode) => {
-            setActiveMode(nextMode);
-            setDetailsExpanded(false);
-          }}
-        />
-      </View>
 
-      <ScrollView
-        showsVerticalScrollIndicator={false}
-        contentContainerStyle={styles.content}
-      >
-        <ThreadSummaryStrip
-          mode={activeMode}
-          expanded={detailsExpanded}
-          onToggle={() => setDetailsExpanded((current) => !current)}
-        />
-        {activeMode === "public" ? <PublicThread /> : <PrivateThread />}
-
-        <View
-          style={[
-            styles.notesCard,
-            {
-              borderColor: palette.border,
-              backgroundColor: palette.surfaceAlt,
-            },
-          ]}
-        >
-          <Text style={[styles.notesTitle, { color: palette.text }]}>
-            THREAD-LAB1 scope
-          </Text>
-          <Text style={[styles.notesText, { color: palette.muted }]}>
-            This route is isolated from the real app, uses local mock data, and
-            does not call any API. Next patches can replace these shells with
-            the final public row and private bubble designs.
-          </Text>
+        <FullBleedDivider />
+
+        <View style={styles.detailBlock}>
+          <Text style={[styles.detailPageKicker, { color: palette.muted }]}>PRIVATE CONTEXT</Text>
+          <Text style={[styles.detailText, { color: palette.text }]}>{scenario.proposalSummary}</Text>
+          <Text style={[styles.detailText, { color: palette.muted }]}>{scenario.acceptedDealSummary}</Text>
         </View>
       </ScrollView>
+    </>
+  );
+}
+
+function GuideScreen({ onBack }: { onBack: () => void }) {
+  const { palette } = useTheme();
+
+  return (
+    <>
+      <ScreenHeader title="Thread guide" onBack={onBack} />
+      <ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={styles.content}>
+        <Text style={[styles.sectionTitle, { color: palette.text }]}>Public thread</Text>
+        <Text style={[styles.guideText, { color: palette.muted }]}>Public comments are visible to people viewing the trade. Use them for simple questions and clarifications.</Text>
+        <FullBleedDivider />
+        <Text style={[styles.sectionTitle, { color: palette.text }]}>Private thread</Text>
+        <Text style={[styles.guideText, { color: palette.muted }]}>Private messages are for proposal and deal discussion between the two users. Details stay outside the chat by default.</Text>
+        <FullBleedDivider />
+      </ScrollView>
+    </>
+  );
+}
+
+export function ThreadLabScreen() {
+  const { palette } = useTheme();
+  const [view, setView] = useState<ThreadLabView>("home");
+  const [threadKind, setThreadKind] = useState<"public" | "private">("public");
+
+  const openThread = (kind: "public" | "private") => {
+    setThreadKind(kind);
+    setView(kind);
+  };
+
+  const openMenu = (kind: "public" | "private") => {
+    setThreadKind(kind);
+    setView(kind === "public" ? "public-menu" : "private-menu");
+  };
+
+  const backToThread = () => setView(threadKind);
+
+  return (
+    <View style={[styles.screen, { backgroundColor: palette.background }]}>
+      <Stack.Screen options={{ title: "Thread Lab", headerShown: false }} />
+      {view === "home" ? <HomeScreen onOpen={openThread} /> : null}
+      {view === "public" ? <PublicThreadScreen onBack={() => setView("home")} onMenu={() => openMenu("public")} /> : null}
+      {view === "private" ? <PrivateThreadScreen onBack={() => setView("home")} onMenu={() => openMenu("private")} /> : null}
+      {view === "public-menu" ? (
+        <ThreadMenuScreen kind="public" onBack={backToThread} onOpenDetails={() => setView("details")} onOpenGuide={() => setView("guide")} />
+      ) : null}
+      {view === "private-menu" ? (
+        <ThreadMenuScreen kind="private" onBack={backToThread} onOpenDetails={() => setView("details")} onOpenGuide={() => setView("guide")} />
+      ) : null}
+      {view === "details" ? <DetailsScreen onBack={backToThread} /> : null}
+      {view === "guide" ? <GuideScreen onBack={backToThread} /> : null}
     </View>
   );
 }
@@ -560,11 +436,11 @@ const styles = StyleSheet.create({
     flex: 1,
   },
   header: {
-    borderBottomWidth: 1,
+    borderBottomWidth: StyleSheet.hairlineWidth,
     paddingHorizontal: 18,
-    paddingBottom: 14,
+    paddingBottom: 16,
     paddingTop: Platform.select({ ios: 18, default: 16 }),
-    gap: 8,
+    gap: 7,
   },
   headerTopRow: {
     flexDirection: "row",
@@ -572,23 +448,30 @@ const styles = StyleSheet.create({
     justifyContent: "space-between",
     gap: 12,
   },
-  headerButton: {
+  textButton: {
     minHeight: 36,
-    alignItems: "center",
     justifyContent: "center",
-    borderWidth: 1,
-    borderRadius: 999,
-    paddingHorizontal: 12,
     paddingVertical: 8,
   },
-  headerButtonText: {
+  textButtonLabel: {
     fontSize: 12,
     fontWeight: "900",
     letterSpacing: 0.2,
   },
+  iconButton: {
+    width: 40,
+    height: 36,
+    alignItems: "flex-end",
+    justifyContent: "center",
+  },
+  iconButtonText: {
+    marginTop: -8,
+    fontSize: 28,
+    fontWeight: "900",
+    letterSpacing: 0.2,
+  },
   pressed: {
-    opacity: 0.82,
-    transform: [{ scale: 0.98 }],
+    opacity: 0.66,
   },
   eyebrow: {
     marginTop: 2,
@@ -602,166 +485,92 @@ const styles = StyleSheet.create({
     letterSpacing: -0.65,
     lineHeight: 32,
   },
-  subtitle: {
-    fontSize: 12,
-    fontWeight: "700",
-    lineHeight: 17,
-  },
-  modeShell: {
-    flexDirection: "row",
-    borderWidth: 1,
-    borderRadius: 22,
-    padding: 4,
-    gap: 4,
-  },
-  modeButton: {
-    flex: 1,
-    minHeight: 56,
-    justifyContent: "center",
-    borderRadius: 18,
-    paddingHorizontal: 12,
-    paddingVertical: 8,
-  },
-  modeLabel: {
-    fontSize: 14,
-    fontWeight: "900",
-    letterSpacing: -0.1,
-  },
-  modeHelper: {
-    marginTop: 2,
-    fontSize: 10,
-    fontWeight: "800",
-  },
   content: {
-    paddingHorizontal: 16,
-    paddingBottom: 40,
-    paddingTop: 16,
-    gap: 14,
+    paddingHorizontal: 18,
+    paddingBottom: 44,
+    paddingTop: 18,
+    gap: 18,
   },
-  summaryShell: {
-    borderWidth: 1,
-    borderRadius: 28,
-    padding: 14,
-    gap: 12,
+  threadContent: {
+    paddingHorizontal: 18,
+    paddingBottom: 44,
+    paddingTop: 18,
+    gap: 0,
   },
-  summaryTopRow: {
-    flexDirection: "row",
-    alignItems: "flex-start",
-    justifyContent: "space-between",
-    gap: 12,
+  tradeIntro: {
+    gap: 6,
   },
-  summaryTextBlock: {
-    flex: 1,
-    gap: 4,
-  },
-  summaryKicker: {
-    fontSize: 10,
+  homeTitle: {
+    fontSize: 24,
     fontWeight: "900",
-    letterSpacing: 1.1,
+    letterSpacing: -0.45,
+    lineHeight: 29,
   },
-  summaryTitle: {
-    fontSize: 20,
+  homeTradeTitle: {
+    marginTop: 8,
+    fontSize: 17,
     fontWeight: "900",
-    letterSpacing: -0.35,
-    lineHeight: 24,
+    lineHeight: 22,
   },
-  summaryMeta: {
+  homeMeta: {
     fontSize: 12,
     fontWeight: "800",
   },
-  statusPill: {
-    maxWidth: 130,
-    borderWidth: 1,
-    borderRadius: 999,
-    paddingHorizontal: 10,
-    paddingVertical: 7,
-  },
-  statusPillText: {
-    fontSize: 10,
-    fontWeight: "900",
-    letterSpacing: 0.25,
-  },
-  detailsToggle: {
-    minHeight: 40,
-    alignItems: "center",
-    justifyContent: "center",
-    borderWidth: 1,
-    borderRadius: 999,
-    paddingHorizontal: 14,
-    paddingVertical: 8,
-  },
-  detailsToggleText: {
-    fontSize: 12,
-    fontWeight: "900",
-  },
-  detailsPanel: {
-    borderWidth: 1,
-    borderRadius: 22,
-    padding: 14,
-    gap: 12,
-  },
-  detailZone: {
-    gap: 5,
-  },
-  detailKicker: {
-    fontSize: 10,
-    fontWeight: "900",
-    letterSpacing: 1.05,
-  },
-  detailTitle: {
-    fontSize: 15,
+  sectionTitle: {
+    fontSize: 16,
     fontWeight: "900",
+    letterSpacing: -0.2,
   },
-  detailText: {
-    fontSize: 12,
-    fontWeight: "700",
-    lineHeight: 17,
-  },
-  detailDivider: {
+  fullBleedDivider: {
     height: StyleSheet.hairlineWidth,
+    marginHorizontal: -18,
   },
-  threadCard: {
-    borderWidth: 1,
-    borderRadius: 30,
-    padding: 14,
-    gap: 14,
+  rowButton: {
+    minHeight: 70,
+    flexDirection: "row",
+    alignItems: "center",
+    justifyContent: "space-between",
+    gap: 16,
+    paddingVertical: 12,
   },
-  threadIntro: {
+  rowButtonTextBlock: {
+    flex: 1,
     gap: 4,
   },
-  threadIntroTitle: {
-    fontSize: 16,
+  rowButtonTitle: {
+    fontSize: 17,
     fontWeight: "900",
     letterSpacing: -0.2,
   },
-  threadIntroText: {
+  rowButtonSubtitle: {
     fontSize: 12,
+    fontWeight: "800",
+  },
+  chevron: {
+    fontSize: 28,
     fontWeight: "700",
-    lineHeight: 17,
   },
-  fakeComposer: {
+  plainComposer: {
     minHeight: 48,
     flexDirection: "row",
     alignItems: "center",
     justifyContent: "space-between",
-    borderWidth: 1,
-    borderRadius: 999,
-    paddingHorizontal: 14,
-    paddingVertical: 8,
-    gap: 10,
+    gap: 12,
+    paddingVertical: 12,
   },
-  fakeComposerText: {
+  plainComposerText: {
     flex: 1,
     fontSize: 13,
-    fontWeight: "700",
+    fontWeight: "800",
   },
-  publicList: {
-    gap: 0,
+  sendText: {
+    fontSize: 13,
+    fontWeight: "900",
   },
   publicRow: {
     flexDirection: "row",
     alignItems: "flex-start",
-    paddingVertical: 12,
+    paddingVertical: 14,
     gap: 10,
   },
   avatar: {
@@ -769,7 +578,6 @@ const styles = StyleSheet.create({
     height: 34,
     alignItems: "center",
     justifyContent: "center",
-    borderWidth: 1,
     borderRadius: 17,
   },
   avatarText: {
@@ -803,29 +611,20 @@ const styles = StyleSheet.create({
     fontSize: 20,
     fontWeight: "900",
   },
-  rowDivider: {
-    height: StyleSheet.hairlineWidth,
-    marginLeft: 44,
+  privateScreenBody: {
+    flex: 1,
   },
-  privateStatusStrip: {
-    borderWidth: 1,
-    borderRadius: 20,
-    paddingHorizontal: 14,
-    paddingVertical: 12,
-    gap: 4,
+  privateMessagesContent: {
+    paddingHorizontal: 18,
+    paddingBottom: 20,
+    paddingTop: 18,
+    gap: 12,
   },
-  privateStatusTitle: {
-    fontSize: 13,
+  dayLabel: {
+    alignSelf: "center",
+    fontSize: 11,
     fontWeight: "900",
   },
-  privateStatusText: {
-    fontSize: 12,
-    fontWeight: "700",
-    lineHeight: 17,
-  },
-  privateList: {
-    gap: 10,
-  },
   privateBubbleRow: {
     width: "100%",
     flexDirection: "row",
@@ -838,7 +637,6 @@ const styles = StyleSheet.create({
   },
   privateBubble: {
     maxWidth: "82%",
-    borderWidth: 1,
     borderRadius: 22,
     paddingHorizontal: 14,
     paddingVertical: 10,
@@ -866,30 +664,51 @@ const styles = StyleSheet.create({
     fontSize: 10,
     fontWeight: "800",
   },
-  sendButton: {
+  bottomComposer: {
+    minHeight: 58,
+    flexDirection: "row",
     alignItems: "center",
-    justifyContent: "center",
-    borderRadius: 999,
-    paddingHorizontal: 12,
-    paddingVertical: 8,
+    justifyContent: "space-between",
+    borderTopWidth: StyleSheet.hairlineWidth,
+    paddingHorizontal: 18,
+    paddingVertical: 12,
+    gap: 14,
   },
-  sendButtonText: {
-    fontSize: 12,
-    fontWeight: "900",
+  bottomComposerText: {
+    flex: 1,
+    fontSize: 13,
+    fontWeight: "800",
   },
-  notesCard: {
-    borderWidth: 1,
-    borderRadius: 24,
-    padding: 14,
-    gap: 5,
+  detailPageKicker: {
+    fontSize: 10,
+    fontWeight: "900",
+    letterSpacing: 1.05,
   },
-  notesTitle: {
-    fontSize: 14,
+  detailPageTitle: {
+    fontSize: 24,
     fontWeight: "900",
+    letterSpacing: -0.4,
+    lineHeight: 29,
   },
-  notesText: {
+  detailPageMeta: {
     fontSize: 12,
+    fontWeight: "800",
+  },
+  detailBlock: {
+    gap: 7,
+  },
+  detailBlockTitle: {
+    fontSize: 17,
+    fontWeight: "900",
+  },
+  detailText: {
+    fontSize: 13,
+    fontWeight: "700",
+    lineHeight: 19,
+  },
+  guideText: {
+    fontSize: 13,
     fontWeight: "700",
-    lineHeight: 17,
+    lineHeight: 19,
   },
 });
