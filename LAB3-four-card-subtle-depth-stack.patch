diff --git a/docs/SQUARE_STACK_DECK_LAB.md b/docs/SQUARE_STACK_DECK_LAB.md
index f94fa78..c46ed3b 100644
--- a/docs/SQUARE_STACK_DECK_LAB.md
+++ b/docs/SQUARE_STACK_DECK_LAB.md
@@ -90,3 +90,30 @@ cardSize = min(
 The preferred minimum card size is only a preference. On small phones, the card is allowed to shrink below that minimum instead of clipping at the bottom or right edge.
 
 This keeps the lab honest for later HelloWhen mobile work, where the real deck sits between native headers, top tabs, action bars, and bottom tab/navigation areas.
+
+
+## LAB3 four-card subtle depth rule
+
+The deck still mounts one previous card and one hidden forward tail card, but the idle visual stack should read as four cards only:
+
+```txt
+active card
+back card 1
+back card 2
+back card 3
+hidden tail card mounted at offset 4
+```
+
+The hidden tail card exists so that a next swipe can slide it into the far depth slot during the same continuous motion. It should not suddenly appear after release.
+
+Depth is intentionally subtle:
+
+```txt
+offset 0: full size
+offset 1: about 7px down/right, barely smaller
+offset 2: about 13px down/right
+offset 3: about 19px down/right
+offset 4: mounted but invisible
+```
+
+Shadows also fade with depth instead of every card using the same heavy shadow. Users should feel a physical stack, but they should not notice a strong stair-step effect.
diff --git a/features/square-stack-deck/components/ContinuousSquareStackDeck.tsx b/features/square-stack-deck/components/ContinuousSquareStackDeck.tsx
index 80ef279..99514de 100644
--- a/features/square-stack-deck/components/ContinuousSquareStackDeck.tsx
+++ b/features/square-stack-deck/components/ContinuousSquareStackDeck.tsx
@@ -14,6 +14,7 @@ import {
   clamp,
   getCommitDuration,
   getSquareStackLayoutMetrics,
+  getSquareStackShadowStyle,
   getSquareStackTransform,
   getSquareStackZIndex,
   getVisibleSquareStackIndexes,
@@ -53,9 +54,26 @@ function SquareStackLayerInner<TCard extends SquareStackDeckCard>({
     };
   }, [index, size]);
 
+  const shadowStyle = useAnimatedStyle(() => {
+    const visualOffset = index - baseIndex.value - progress.value;
+    return getSquareStackShadowStyle(visualOffset);
+  }, [index]);
+
   return (
     <Animated.View pointerEvents="box-none" style={[styles.layer, animatedStyle]}>
-      <SquareStackCardShell size={size}>{renderCard({ card, index, total })}</SquareStackCardShell>
+      <Animated.View
+        style={[
+          styles.shadowLayer,
+          {
+            width: size,
+            height: size,
+            borderRadius: Math.max(24, Math.round(size * 0.075)),
+          },
+          shadowStyle,
+        ]}
+      >
+        <SquareStackCardShell size={size}>{renderCard({ card, index, total })}</SquareStackCardShell>
+      </Animated.View>
     </Animated.View>
   );
 }
@@ -236,6 +254,10 @@ const styles = StyleSheet.create({
     alignItems: "center",
     justifyContent: "center",
   },
+  shadowLayer: {
+    shadowColor: "#000",
+    shadowOffset: { width: 0, height: 14 },
+  },
   empty: {
     alignItems: "center",
     justifyContent: "center",
diff --git a/features/square-stack-deck/components/SquareStackCardShell.tsx b/features/square-stack-deck/components/SquareStackCardShell.tsx
index f08d696..7b7b1d3 100644
--- a/features/square-stack-deck/components/SquareStackCardShell.tsx
+++ b/features/square-stack-deck/components/SquareStackCardShell.tsx
@@ -15,40 +15,23 @@ export function SquareStackCardShell({ size, children }: Props) {
   return (
     <View
       style={[
-        styles.shadow,
+        styles.card,
         {
           width: size,
           height: size,
           borderRadius: radius,
-          shadowColor: palette.mode === "light" ? "#000" : "#000",
+          borderColor: palette.mode === "light" ? "rgba(0,0,0,0.08)" : "rgba(255,255,255,0.12)",
+          backgroundColor: palette.surface,
         },
       ]}
     >
-      <View
-        style={[
-          styles.card,
-          {
-            borderRadius: radius,
-            borderColor: palette.mode === "light" ? "rgba(0,0,0,0.08)" : "rgba(255,255,255,0.12)",
-            backgroundColor: palette.surface,
-          },
-        ]}
-      >
-        {children}
-      </View>
+      {children}
     </View>
   );
 }
 
 const styles = StyleSheet.create({
-  shadow: {
-    shadowOffset: { width: 0, height: 16 },
-    shadowOpacity: 0.22,
-    shadowRadius: 28,
-    elevation: 16,
-  },
   card: {
-    flex: 1,
     overflow: "hidden",
     borderWidth: StyleSheet.hairlineWidth,
     ...(Platform.OS === "web" ? ({ willChange: "transform" } as any) : null),
diff --git a/features/square-stack-deck/squareStackDeck.model.ts b/features/square-stack-deck/squareStackDeck.model.ts
index 7a6c4c1..cc9d347 100644
--- a/features/square-stack-deck/squareStackDeck.model.ts
+++ b/features/square-stack-deck/squareStackDeck.model.ts
@@ -1,6 +1,8 @@
 import { Extrapolation, interpolate } from "react-native-reanimated";
 
 export const SQUARE_STACK_VISIBLE_BEFORE = 1;
+// Keep one hidden tail card mounted so next-swipe depth can promote smoothly
+// without making the idle stack look like more than four visible cards.
 export const SQUARE_STACK_VISIBLE_AFTER = 4;
 
 export const SQUARE_STACK_COMMIT_THRESHOLD = 0.36;
@@ -74,23 +76,26 @@ export function getVisibleSquareStackIndexes(activeIndex: number, total: number)
 export function getSquareStackTransform(visualOffset: number, cardSize: number) {
   "worklet";
 
-  const clampedOffset = clamp(visualOffset, -1, 4.25);
+  const clampedOffset = clamp(visualOffset, -1, 4);
   const input = [-1, 0, 1, 2, 3, 4];
 
+  // LAB3 keeps the physical model but makes the depth almost invisible. The
+  // fourth-after card stays mounted as a hidden tail so it can slide into the
+  // third depth slot during a next swipe instead of appearing after release.
   const translateX = interpolate(
     clampedOffset,
     input,
-    [-cardSize * 0.68, 0, 10, 20, 30, 40],
+    [-cardSize * 0.62, 0, 7, 13, 19, 25],
     Extrapolation.CLAMP
   );
   const translateY = interpolate(
     clampedOffset,
     input,
-    [-cardSize * 0.58, 0, 10, 20, 30, 40],
+    [-cardSize * 0.54, 0, 7, 13, 19, 25],
     Extrapolation.CLAMP
   );
-  const scale = interpolate(clampedOffset, input, [0.96, 1, 0.97, 0.94, 0.91, 0.88], Extrapolation.CLAMP);
-  const opacity = interpolate(clampedOffset, input, [0, 1, 0.92, 0.78, 0.62, 0.38], Extrapolation.CLAMP);
+  const scale = interpolate(clampedOffset, input, [0.985, 1, 0.988, 0.978, 0.97, 0.964], Extrapolation.CLAMP);
+  const opacity = interpolate(clampedOffset, input, [0, 1, 0.965, 0.9, 0.78, 0], Extrapolation.CLAMP);
 
   return {
     translateX,
@@ -100,6 +105,20 @@ export function getSquareStackTransform(visualOffset: number, cardSize: number)
   };
 }
 
+
+export function getSquareStackShadowStyle(visualOffset: number) {
+  "worklet";
+
+  const clampedOffset = clamp(visualOffset, -1, 4);
+  const input = [-1, 0, 1, 2, 3, 4];
+
+  return {
+    shadowOpacity: interpolate(clampedOffset, input, [0.18, 0.2, 0.13, 0.08, 0.045, 0], Extrapolation.CLAMP),
+    shadowRadius: interpolate(clampedOffset, input, [24, 26, 18, 12, 8, 0], Extrapolation.CLAMP),
+    elevation: Math.round(interpolate(clampedOffset, input, [14, 16, 9, 5, 2, 0], Extrapolation.CLAMP)),
+  };
+}
+
 export function getSquareStackZIndex(visualOffset: number) {
   "worklet";
 
