diff --git a/features/auth-motion-lab/AuthMotionLabScreen.tsx b/features/auth-motion-lab/AuthMotionLabScreen.tsx
index 9f7b514..653996f 100644
--- a/features/auth-motion-lab/AuthMotionLabScreen.tsx
+++ b/features/auth-motion-lab/AuthMotionLabScreen.tsx
@@ -1,43 +1,74 @@
 import { Stack, router } from "expo-router";
-import React, { useMemo, useState } from "react";
-import { Platform, Pressable, ScrollView, StyleSheet, Text, View } from "react-native";
+import React, { useEffect, useMemo, useRef, useState } from "react";
+import {
+  Animated,
+  Easing,
+  Platform,
+  Pressable,
+  ScrollView,
+  StyleSheet,
+  Text,
+  TextInput,
+  View,
+  type KeyboardTypeOptions,
+} from "react-native";
 import { useSafeAreaInsets } from "react-native-safe-area-context";
 
 import { useTheme } from "@/providers/ThemeProvider";
 
 type AuthMode = "login" | "register" | "reset";
-type MoodState = "straight" | "smile" | "frown";
+type MoodState = "straight" | "smile" | "frown" | "submitting";
+type AuthFieldId = "name" | "email" | "password" | "confirmPassword";
 
-type PreviewField = {
+type AuthValues = Record<AuthFieldId, string>;
+
+type FieldConfig = {
+  id: AuthFieldId;
   label: string;
   placeholder: string;
-  state: MoodState;
+  keyboardType?: KeyboardTypeOptions;
+  secureTextEntry?: boolean;
   helper?: string;
 };
 
+const emptyValues: AuthValues = {
+  name: "",
+  email: "",
+  password: "",
+  confirmPassword: "",
+};
+
 const modes: { id: AuthMode; label: string }[] = [
   { id: "login", label: "Login" },
   { id: "register", label: "Register" },
   { id: "reset", label: "Reset" },
 ];
 
-const stateCards: { state: MoodState; title: string; description: string; line: string }[] = [
+const stateCards: {
+  state: Exclude<MoodState, "submitting">;
+  title: string;
+  description: string;
+  line: string;
+}[] = [
   {
     state: "straight",
     title: "Typing / incomplete",
-    description: "Keep the field stable while the user is still entering information.",
+    description:
+      "Keep the field stable while the user is still entering information.",
     line: "────────────",
   },
   {
     state: "smile",
     title: "Valid / ready",
-    description: "Curve the mood line gently upward after the field passes validation.",
+    description:
+      "Curve the mood line gently upward after the field passes validation.",
     line: "╰────────╯",
   },
   {
     state: "frown",
     title: "Error / failed",
-    description: "Curve the mood line downward, then pair it with clear helper text.",
+    description:
+      "Curve the mood line downward, then pair it with clear helper text.",
     line: "╭────────╮",
   },
 ];
@@ -47,14 +78,19 @@ function getModeCopy(mode: AuthMode) {
     case "register":
       return {
         title: "Create your account",
-        subtitle: "Create your account and set local display preferences for the beta.",
+        subtitle:
+          "Create your account and set local display preferences for the beta.",
         button: "Create account",
+        success:
+          "Account shape looks ready. This lab does not create a real account.",
       };
     case "reset":
       return {
         title: "Reset your password",
         subtitle: "Request a reset link for your Hellowhen account.",
         button: "Send reset link",
+        success:
+          "Reset-link success state preview. No email is sent from this lab.",
       };
     case "login":
     default:
@@ -62,48 +98,186 @@ function getModeCopy(mode: AuthMode) {
         title: "Welcome back",
         subtitle: "Sign in to create needs, offers, trades, and proposals.",
         button: "Login",
+        success:
+          "Login success state preview. This lab does not call production auth.",
       };
   }
 }
 
-function getPreviewFields(mode: AuthMode): PreviewField[] {
+function getFieldConfigs(mode: AuthMode): FieldConfig[] {
   switch (mode) {
     case "register":
       return [
-        { label: "Full name", placeholder: "Mina Chen", state: "smile" },
-        { label: "Email", placeholder: "you@example.com", state: "straight" },
-        { label: "Password", placeholder: "At least 8 characters", state: "straight" },
+        { id: "name", label: "Full name", placeholder: "Mina Chen" },
         {
+          id: "email",
+          label: "Email",
+          placeholder: "you@example.com",
+          keyboardType: "email-address",
+        },
+        {
+          id: "password",
+          label: "Password",
+          placeholder: "At least 8 characters",
+          secureTextEntry: true,
+        },
+        {
+          id: "confirmPassword",
           label: "Confirm password",
           placeholder: "Repeat password",
-          state: "frown",
-          helper: "Passwords should match before the curve turns into a smile.",
+          secureTextEntry: true,
+          helper:
+            "The frown appears only when the confirmation is wrong or submit is attempted.",
         },
       ];
     case "reset":
       return [
         {
+          id: "email",
           label: "Email",
           placeholder: "you@example.com",
-          state: "smile",
+          keyboardType: "email-address",
           helper: "Reset flow only needs one focused motion test field.",
         },
       ];
     case "login":
     default:
       return [
-        { label: "Email", placeholder: "you@example.com", state: "smile" },
         {
+          id: "email",
+          label: "Email",
+          placeholder: "you@example.com",
+          keyboardType: "email-address",
+        },
+        {
+          id: "password",
           label: "Password",
           placeholder: "At least 8 characters",
-          state: "frown",
+          secureTextEntry: true,
           helper: "Wrong password state should never rely on motion alone.",
         },
       ];
   }
 }
 
-function ModeTabs({ activeMode, onChange }: { activeMode: AuthMode; onChange: (mode: AuthMode) => void }) {
+function getSampleValues(mode: AuthMode): AuthValues {
+  switch (mode) {
+    case "register":
+      return {
+        name: "Mina Chen",
+        email: "mina@example.com",
+        password: "hello2026",
+        confirmPassword: "hello2026",
+      };
+    case "reset":
+      return { ...emptyValues, email: "mina@example.com" };
+    case "login":
+    default:
+      return {
+        ...emptyValues,
+        email: "mina@example.com",
+        password: "hello2026",
+      };
+  }
+}
+
+function getErrorSampleValues(mode: AuthMode): AuthValues {
+  switch (mode) {
+    case "register":
+      return {
+        name: "M",
+        email: "mina",
+        password: "short",
+        confirmPassword: "different",
+      };
+    case "reset":
+      return { ...emptyValues, email: "not-an-email" };
+    case "login":
+    default:
+      return { ...emptyValues, email: "mina", password: "short" };
+  }
+}
+
+function isEmailValid(value: string) {
+  return /^\S+@\S+\.\S+$/.test(value.trim());
+}
+
+function isFieldValid(fieldId: AuthFieldId, values: AuthValues) {
+  const value = values[fieldId].trim();
+
+  switch (fieldId) {
+    case "name":
+      return value.length >= 2;
+    case "email":
+      return isEmailValid(value);
+    case "password":
+      return values.password.length >= 8;
+    case "confirmPassword":
+      return (
+        values.confirmPassword.length > 0 &&
+        values.confirmPassword === values.password
+      );
+    default:
+      return false;
+  }
+}
+
+function getFieldError(fieldId: AuthFieldId) {
+  switch (fieldId) {
+    case "name":
+      return "Use at least 2 characters.";
+    case "email":
+      return "Enter a valid email address.";
+    case "password":
+      return "Use at least 8 characters.";
+    case "confirmPassword":
+      return "Passwords should match before the curve turns into a smile.";
+    default:
+      return "Check this field.";
+  }
+}
+
+function getStateProgress(state: MoodState) {
+  switch (state) {
+    case "smile":
+      return 1;
+    case "frown":
+      return 2;
+    case "submitting":
+      return 3;
+    case "straight":
+    default:
+      return 0;
+  }
+}
+
+function getMoodColor(
+  state: MoodState,
+  palette: ReturnType<typeof useTheme>["palette"],
+  isFocused: boolean,
+) {
+  if (state === "frown") {
+    return palette.danger;
+  }
+
+  if (state === "smile") {
+    return "#35E6C6";
+  }
+
+  if (state === "submitting") {
+    return "#4CC9FF";
+  }
+
+  return isFocused ? "#8CCBFF" : palette.border;
+}
+
+function ModeTabs({
+  activeMode,
+  onChange,
+}: {
+  activeMode: AuthMode;
+  onChange: (mode: AuthMode) => void;
+}) {
   const { palette } = useTheme();
 
   return (
@@ -122,7 +296,9 @@ function ModeTabs({ activeMode, onChange }: { activeMode: AuthMode; onChange: (m
               pressed ? styles.pressed : null,
             ]}
           >
-            <Text style={[styles.tabText, { color: palette.text }]}>{mode.label}</Text>
+            <Text style={[styles.tabText, { color: palette.text }]}>
+              {mode.label}
+            </Text>
           </Pressable>
         );
       })}
@@ -130,110 +306,508 @@ function ModeTabs({ activeMode, onChange }: { activeMode: AuthMode; onChange: (m
   );
 }
 
-function FieldPreview({ field }: { field: PreviewField }) {
+function AuthMoodInput({
+  field,
+  value,
+  state,
+  helper,
+  shakeKey,
+  onChangeText,
+}: {
+  field: FieldConfig;
+  value: string;
+  state: MoodState;
+  helper?: string;
+  shakeKey: number;
+  onChangeText: (value: string) => void;
+}) {
   const { palette } = useTheme();
-  const lineStyle = useMemo(() => {
-    if (field.state === "smile") {
-      return styles.moodLineSmile;
+  const [isFocused, setIsFocused] = useState(false);
+  const progress = useRef(new Animated.Value(getStateProgress(state))).current;
+  const submitPulse = useRef(new Animated.Value(0)).current;
+  const shake = useRef(new Animated.Value(0)).current;
+
+  useEffect(() => {
+    Animated.timing(progress, {
+      toValue: getStateProgress(state),
+      duration: state === "frown" ? 170 : 260,
+      easing: Easing.out(Easing.cubic),
+      useNativeDriver: true,
+    }).start();
+  }, [progress, state]);
+
+  useEffect(() => {
+    if (state !== "submitting") {
+      submitPulse.stopAnimation();
+      submitPulse.setValue(0);
+      return;
     }
 
-    if (field.state === "frown") {
-      return styles.moodLineFrown;
+    const loop = Animated.loop(
+      Animated.sequence([
+        Animated.timing(submitPulse, {
+          toValue: 1,
+          duration: 560,
+          easing: Easing.inOut(Easing.sin),
+          useNativeDriver: true,
+        }),
+        Animated.timing(submitPulse, {
+          toValue: 0,
+          duration: 560,
+          easing: Easing.inOut(Easing.sin),
+          useNativeDriver: true,
+        }),
+      ]),
+    );
+
+    loop.start();
+    return () => loop.stop();
+  }, [state, submitPulse]);
+
+  useEffect(() => {
+    if (state !== "frown" || shakeKey === 0) {
+      return;
     }
 
-    return styles.moodLineStraight;
-  }, [field.state]);
+    shake.setValue(0);
+    Animated.sequence([
+      Animated.timing(shake, {
+        toValue: -5,
+        duration: 38,
+        useNativeDriver: true,
+      }),
+      Animated.timing(shake, {
+        toValue: 5,
+        duration: 38,
+        useNativeDriver: true,
+      }),
+      Animated.timing(shake, {
+        toValue: -3,
+        duration: 38,
+        useNativeDriver: true,
+      }),
+      Animated.timing(shake, {
+        toValue: 3,
+        duration: 38,
+        useNativeDriver: true,
+      }),
+      Animated.timing(shake, {
+        toValue: 0,
+        duration: 55,
+        useNativeDriver: true,
+      }),
+    ]).start();
+  }, [shake, shakeKey, state]);
+
+  const moodColor = getMoodColor(state, palette, isFocused);
+  const straightOpacity = progress.interpolate({
+    inputRange: [0, 1, 2, 3],
+    outputRange: [1, 0.08, 0.05, 0.3],
+  });
+  const smileOpacity = progress.interpolate({
+    inputRange: [0, 1, 2, 3],
+    outputRange: [0, 1, 0, 0.82],
+  });
+  const frownOpacity = progress.interpolate({
+    inputRange: [0, 1, 2, 3],
+    outputRange: [0, 0, 1, 0],
+  });
+  const smileScaleY = submitPulse.interpolate({
+    inputRange: [0, 1],
+    outputRange: [0.86, 1.14],
+  });
+  const smileScaleX = submitPulse.interpolate({
+    inputRange: [0, 1],
+    outputRange: [0.94, 1.02],
+  });
 
   return (
-    <View style={styles.fieldBlock}>
-      <Text style={[styles.fieldLabel, { color: palette.text }]}>{field.label}</Text>
-      <View style={[styles.inputShell, { borderColor: palette.border, backgroundColor: palette.surfaceAlt }]}>
-        <Text style={[styles.placeholder, { color: palette.muted }]}>{field.placeholder}</Text>
+    <Animated.View
+      style={[styles.fieldBlock, { transform: [{ translateX: shake }] }]}
+    >
+      <View style={styles.fieldLabelRow}>
+        <Text style={[styles.fieldLabel, { color: palette.text }]}>
+          {field.label}
+        </Text>
+        <Text style={[styles.fieldStateText, { color: moodColor }]}>
+          {state === "straight" ? "typing" : state}
+        </Text>
       </View>
+
       <View
         style={[
-          styles.moodLine,
-          lineStyle,
+          styles.inputShell,
           {
-            borderColor:
-              field.state === "frown" ? palette.danger : field.state === "smile" ? "#35E6C6" : palette.border,
+            borderColor: moodColor,
+            backgroundColor: palette.surfaceAlt,
+            shadowColor: moodColor,
+            shadowOpacity:
+              Platform.OS === "ios" && state !== "straight" ? 0.22 : 0,
           },
         ]}
-      />
-      {field.helper ? (
+      >
+        <TextInput
+          value={value}
+          onChangeText={onChangeText}
+          onFocus={() => setIsFocused(true)}
+          onBlur={() => setIsFocused(false)}
+          placeholder={field.placeholder}
+          placeholderTextColor={palette.muted}
+          keyboardType={field.keyboardType}
+          secureTextEntry={field.secureTextEntry}
+          autoCapitalize="none"
+          autoCorrect={false}
+          editable={state !== "submitting"}
+          style={[
+            styles.textInput,
+            { color: palette.text },
+            Platform.OS === "web" ? styles.webTextInput : null,
+          ]}
+        />
+      </View>
+
+      <View style={styles.moodTrack} pointerEvents="none">
+        <Animated.View
+          style={[
+            styles.moodStraightStroke,
+            { backgroundColor: moodColor, opacity: straightOpacity },
+          ]}
+        />
+        <Animated.View
+          style={[
+            styles.moodCurveStroke,
+            styles.moodCurveSmile,
+            {
+              borderColor: moodColor,
+              opacity: smileOpacity,
+              transform: [{ scaleX: smileScaleX }, { scaleY: smileScaleY }],
+            },
+          ]}
+        />
+        <Animated.View
+          style={[
+            styles.moodCurveStroke,
+            styles.moodCurveFrown,
+            { borderColor: moodColor, opacity: frownOpacity },
+          ]}
+        />
+      </View>
+
+      {helper ? (
         <Text
           style={[
             styles.helper,
-            { color: field.state === "frown" ? palette.danger : palette.muted },
+            { color: state === "frown" ? palette.danger : palette.muted },
           ]}
         >
-          {field.helper}
+          {helper}
         </Text>
       ) : null}
-    </View>
+    </Animated.View>
   );
 }
 
-function AuthPreview({ mode }: { mode: AuthMode }) {
+function AuthPreview({
+  mode,
+  onModeChange,
+}: {
+  mode: AuthMode;
+  onModeChange: (mode: AuthMode) => void;
+}) {
   const { palette } = useTheme();
   const copy = getModeCopy(mode);
-  const fields = getPreviewFields(mode);
+  const fields = useMemo(() => getFieldConfigs(mode), [mode]);
+  const [values, setValues] = useState<AuthValues>(emptyValues);
+  const [attempted, setAttempted] = useState(false);
+  const [isSubmitting, setIsSubmitting] = useState(false);
+  const [successMessage, setSuccessMessage] = useState<string | null>(null);
+  const [shakeKey, setShakeKey] = useState(0);
+  const submitTimer = useRef<ReturnType<typeof setTimeout> | null>(null);
+
+  const clearSubmitTimer = () => {
+    if (submitTimer.current) {
+      clearTimeout(submitTimer.current);
+      submitTimer.current = null;
+    }
+  };
+
+  useEffect(() => {
+    setAttempted(false);
+    setIsSubmitting(false);
+    setSuccessMessage(null);
+    setShakeKey(0);
+    setValues(emptyValues);
+    clearSubmitTimer();
+  }, [mode]);
+
+  useEffect(
+    () => () => {
+      if (submitTimer.current) {
+        clearTimeout(submitTimer.current);
+      }
+    },
+    [],
+  );
+
+  const activeFieldsValid = fields.every((field) =>
+    isFieldValid(field.id, values),
+  );
+
+  const updateField = (fieldId: AuthFieldId, nextValue: string) => {
+    setValues((current) => ({ ...current, [fieldId]: nextValue }));
+    setSuccessMessage(null);
+  };
+
+  const getMoodState = (fieldId: AuthFieldId): MoodState => {
+    if (isSubmitting) {
+      return "submitting";
+    }
+
+    const value = values[fieldId];
+    const isValid = isFieldValid(fieldId, values);
+
+    if (isValid) {
+      return "smile";
+    }
+
+    if (attempted || value.length > 0) {
+      return "frown";
+    }
+
+    return "straight";
+  };
+
+  const getHelper = (field: FieldConfig, state: MoodState) => {
+    if (state === "frown") {
+      return getFieldError(field.id);
+    }
+
+    if (state === "submitting") {
+      return "Checking shape and state transition…";
+    }
+
+    return field.helper;
+  };
+
+  const handleSubmit = () => {
+    if (isSubmitting) {
+      return;
+    }
+
+    setAttempted(true);
+    setSuccessMessage(null);
+
+    if (!activeFieldsValid) {
+      setShakeKey((current) => current + 1);
+      return;
+    }
+
+    clearSubmitTimer();
+    setIsSubmitting(true);
+    submitTimer.current = setTimeout(() => {
+      setIsSubmitting(false);
+      setSuccessMessage(copy.success);
+    }, 780);
+  };
+
+  const fillValidDemo = () => {
+    setValues(getSampleValues(mode));
+    setAttempted(false);
+    setSuccessMessage(null);
+    setIsSubmitting(false);
+    clearSubmitTimer();
+  };
+
+  const triggerErrorDemo = () => {
+    setValues(getErrorSampleValues(mode));
+    setAttempted(true);
+    setSuccessMessage(null);
+    setIsSubmitting(false);
+    clearSubmitTimer();
+    setShakeKey((current) => current + 1);
+  };
+
+  const resetDemo = () => {
+    setValues(emptyValues);
+    setAttempted(false);
+    setSuccessMessage(null);
+    setIsSubmitting(false);
+    clearSubmitTimer();
+    setShakeKey(0);
+  };
 
   return (
-    <View style={[styles.phoneFrame, { backgroundColor: palette.background, borderColor: palette.border }]}>
+    <View
+      style={[
+        styles.phoneFrame,
+        { backgroundColor: palette.background, borderColor: palette.border },
+      ]}
+    >
       <View style={styles.previewTopRow}>
-        <View style={[styles.backPill, { borderColor: palette.border, backgroundColor: palette.surfaceAlt }]}>
-          <Text style={[styles.backPillText, { color: palette.text }]}>Back</Text>
+        <View
+          style={[
+            styles.backPill,
+            {
+              borderColor: palette.border,
+              backgroundColor: palette.surfaceAlt,
+            },
+          ]}
+        >
+          <Text style={[styles.backPillText, { color: palette.text }]}>
+            Back
+          </Text>
         </View>
       </View>
 
-      <View style={[styles.tradeBadge, { borderColor: "rgba(130,80,255,0.65)" }]}>
+      <View
+        style={[styles.tradeBadge, { borderColor: "rgba(130,80,255,0.65)" }]}
+      >
         <Text style={styles.tradeBadgeText}>TRADE</Text>
       </View>
 
-      <Text style={[styles.previewTitle, { color: palette.text }]}>{copy.title}</Text>
-      <Text style={[styles.previewSubtitle, { color: palette.muted }]}>{copy.subtitle}</Text>
+      <Text style={[styles.previewTitle, { color: palette.text }]}>
+        {copy.title}
+      </Text>
+      <Text style={[styles.previewSubtitle, { color: palette.muted }]}>
+        {copy.subtitle}
+      </Text>
 
-      <ModeTabs activeMode={mode} onChange={() => undefined} />
+      <ModeTabs activeMode={mode} onChange={onModeChange} />
 
       <View style={styles.previewFields}>
-        {fields.map((field) => (
-          <FieldPreview key={`${mode}-${field.label}`} field={field} />
-        ))}
+        {fields.map((field) => {
+          const state = getMoodState(field.id);
+          return (
+            <AuthMoodInput
+              key={`${mode}-${field.id}`}
+              field={field}
+              value={values[field.id]}
+              state={state}
+              helper={getHelper(field, state)}
+              shakeKey={shakeKey}
+              onChangeText={(nextValue) => updateField(field.id, nextValue)}
+            />
+          );
+        })}
       </View>
 
-      <View style={[styles.primaryButton, { backgroundColor: palette.text }]}>
-        <Text style={[styles.primaryButtonText, { color: palette.background }]}>{copy.button}</Text>
+      {successMessage ? (
+        <View style={styles.successBox}>
+          <Text style={styles.successText}>{successMessage}</Text>
+        </View>
+      ) : null}
+
+      <Pressable
+        onPress={handleSubmit}
+        accessibilityRole="button"
+        accessibilityState={{ disabled: isSubmitting }}
+        style={({ pressed }) => [
+          styles.primaryButton,
+          { backgroundColor: isSubmitting ? "#4CC9FF" : palette.text },
+          pressed && !isSubmitting ? styles.pressed : null,
+        ]}
+      >
+        <Text
+          style={[
+            styles.primaryButtonText,
+            { color: isSubmitting ? "#031018" : palette.background },
+          ]}
+        >
+          {isSubmitting ? "Checking…" : copy.button}
+        </Text>
+      </Pressable>
+
+      <View style={styles.demoControls}>
+        <Pressable
+          onPress={fillValidDemo}
+          accessibilityRole="button"
+          style={({ pressed }) => [
+            styles.demoButton,
+            { borderColor: palette.border },
+            pressed ? styles.pressed : null,
+          ]}
+        >
+          <Text style={[styles.demoButtonText, { color: palette.text }]}>
+            Fill valid
+          </Text>
+        </Pressable>
+        <Pressable
+          onPress={triggerErrorDemo}
+          accessibilityRole="button"
+          style={({ pressed }) => [
+            styles.demoButton,
+            { borderColor: palette.border },
+            pressed ? styles.pressed : null,
+          ]}
+        >
+          <Text style={[styles.demoButtonText, { color: palette.text }]}>
+            Trigger error
+          </Text>
+        </Pressable>
+        <Pressable
+          onPress={resetDemo}
+          accessibilityRole="button"
+          style={({ pressed }) => [
+            styles.demoButton,
+            { borderColor: palette.border },
+            pressed ? styles.pressed : null,
+          ]}
+        >
+          <Text style={[styles.demoButtonText, { color: palette.text }]}>
+            Reset
+          </Text>
+        </Pressable>
       </View>
 
       <View style={styles.noticeBox}>
         <Text style={styles.noticeText}>
-          Google sign-in is disabled for the first launch. Please use email login or create an
-          account with email for now.
+          Google sign-in is disabled for the first launch. Please use email
+          login or create an account with email for now.
         </Text>
       </View>
     </View>
   );
 }
 
-function MotionStateCard({ title, description, line, state }: (typeof stateCards)[number]) {
+function MotionStateCard({
+  title,
+  description,
+  line,
+  state,
+}: (typeof stateCards)[number]) {
   const { palette } = useTheme();
 
   return (
-    <View style={[styles.stateCard, { backgroundColor: palette.surface, borderColor: palette.border }]}>
+    <View
+      style={[
+        styles.stateCard,
+        { backgroundColor: palette.surface, borderColor: palette.border },
+      ]}
+    >
       <View style={styles.stateCardTopRow}>
-        <Text style={[styles.stateTitle, { color: palette.text }]}>{title}</Text>
+        <Text style={[styles.stateTitle, { color: palette.text }]}>
+          {title}
+        </Text>
         <Text
           style={[
             styles.stateBadge,
-            { color: state === "frown" ? palette.danger : state === "smile" ? "#35E6C6" : palette.muted },
+            {
+              color:
+                state === "frown"
+                  ? palette.danger
+                  : state === "smile"
+                    ? "#35E6C6"
+                    : palette.muted,
+            },
           ]}
         >
           {state.toUpperCase()}
         </Text>
       </View>
       <Text style={[styles.stateLine, { color: palette.text }]}>{line}</Text>
-      <Text style={[styles.stateDescription, { color: palette.muted }]}>{description}</Text>
+      <Text style={[styles.stateDescription, { color: palette.muted }]}>
+        {description}
+      </Text>
     </View>
   );
 }
@@ -245,7 +819,9 @@ export function AuthMotionLabScreen() {
 
   return (
     <View style={[styles.screen, { backgroundColor: palette.background }]}>
-      <Stack.Screen options={{ title: "Auth Motion Lab", headerShown: false }} />
+      <Stack.Screen
+        options={{ title: "Auth Motion Lab", headerShown: false }}
+      />
 
       <View
         style={[
@@ -261,15 +837,25 @@ export function AuthMotionLabScreen() {
           <Pressable
             onPress={() => router.back()}
             accessibilityRole="button"
-            style={({ pressed }) => [styles.headerButton, { borderColor: palette.border }, pressed ? styles.pressed : null]}
+            style={({ pressed }) => [
+              styles.headerButton,
+              { borderColor: palette.border },
+              pressed ? styles.pressed : null,
+            ]}
           >
-            <Text style={[styles.headerButtonText, { color: palette.text }]}>‹ Labs</Text>
+            <Text style={[styles.headerButtonText, { color: palette.text }]}>
+              ‹ Labs
+            </Text>
           </Pressable>
 
           <Pressable
             onPress={toggleMode}
             accessibilityRole="button"
-            style={({ pressed }) => [styles.headerButton, { borderColor: palette.border }, pressed ? styles.pressed : null]}
+            style={({ pressed }) => [
+              styles.headerButton,
+              { borderColor: palette.border },
+              pressed ? styles.pressed : null,
+            ]}
           >
             <Text style={[styles.headerButtonText, { color: palette.text }]}>
               {themeMode === "dark" ? "Light" : "Dark"}
@@ -277,30 +863,47 @@ export function AuthMotionLabScreen() {
           </Pressable>
         </View>
 
-        <Text style={[styles.eyebrow, { color: palette.muted }]}>AUTH MOTION LAB</Text>
-        <Text style={[styles.title, { color: palette.text }]}>Straight, smile, and frown fields</Text>
+        <Text style={[styles.eyebrow, { color: palette.muted }]}>
+          AUTH MOTION LAB
+        </Text>
+        <Text style={[styles.title, { color: palette.text }]}>
+          Animated smile and frown inputs
+        </Text>
         <Text style={[styles.subtitle, { color: palette.muted }]}>
-          Isolated playground for the playful auth-page curve idea. This first route is static and
-          safe: AUTH-LAB2 can add real animated mood inputs here without touching production auth.
+          Isolated playground for the playful auth-page curve idea. Type into
+          the fields or use the demo buttons to test straight, smile, frown,
+          shake, and submitting states without touching production auth.
         </Text>
       </View>
 
-      <ScrollView contentContainerStyle={[styles.content, { paddingBottom: insets.bottom + 32 }]}>
+      <ScrollView
+        contentContainerStyle={[
+          styles.content,
+          { paddingBottom: insets.bottom + 32 },
+        ]}
+      >
         <View style={styles.sectionHeader}>
-          <Text style={[styles.sectionTitle, { color: palette.text }]}>Flow preview</Text>
+          <Text style={[styles.sectionTitle, { color: palette.text }]}>
+            Live animated prototype
+          </Text>
           <Text style={[styles.sectionText, { color: palette.muted }]}>
-            Switch between login, register, and reset to test the auth layout target before adding
-            motion.
+            The input value stays straight while empty, smiles when valid,
+            frowns when invalid, and breathes during submit. Error text remains
+            visible so motion is never the only feedback.
           </Text>
         </View>
 
         <ModeTabs activeMode={activeMode} onChange={setActiveMode} />
-        <AuthPreview mode={activeMode} />
+        <AuthPreview mode={activeMode} onModeChange={setActiveMode} />
 
         <View style={styles.sectionHeader}>
-          <Text style={[styles.sectionTitle, { color: palette.text }]}>Mood states</Text>
+          <Text style={[styles.sectionTitle, { color: palette.text }]}>
+            Mood states
+          </Text>
           <Text style={[styles.sectionText, { color: palette.muted }]}>
-            These cards define the three baseline field states for the next animated component.
+            These cards define the three baseline field shapes. The live
+            prototype adds a temporary submitting pulse on top of the smile
+            shape.
           </Text>
         </View>
 
@@ -456,42 +1059,73 @@ const styles = StyleSheet.create({
   fieldBlock: {
     gap: 7,
   },
+  fieldLabelRow: {
+    flexDirection: "row",
+    alignItems: "center",
+    justifyContent: "space-between",
+    gap: 12,
+  },
   fieldLabel: {
     fontSize: 13,
     fontWeight: "900",
   },
+  fieldStateText: {
+    fontSize: 10,
+    fontWeight: "900",
+    letterSpacing: 0.7,
+    textTransform: "uppercase",
+  },
   inputShell: {
     minHeight: 46,
     borderWidth: 1,
     borderRadius: 16,
     justifyContent: "center",
     paddingHorizontal: 14,
+    shadowRadius: 12,
+    shadowOffset: { width: 0, height: 6 },
   },
-  placeholder: {
+  textInput: {
+    minHeight: 44,
+    paddingVertical: 0,
     fontSize: 14,
     fontWeight: "800",
   },
-  moodLine: {
+  webTextInput: {
+    outlineStyle: "none",
+  } as any,
+  moodTrack: {
     alignSelf: "center",
     width: "82%",
-    height: 13,
-    marginTop: -4,
+    height: 15,
+    marginTop: -5,
+    position: "relative",
+    overflow: "visible",
   },
-  moodLineStraight: {
-    height: 0,
-    borderTopWidth: 2,
+  moodStraightStroke: {
+    position: "absolute",
+    top: 6,
+    left: 0,
+    right: 0,
+    height: 2,
+    borderRadius: 999,
   },
-  moodLineSmile: {
-    borderBottomWidth: 2,
+  moodCurveStroke: {
+    position: "absolute",
+    left: 0,
+    right: 0,
+    height: 15,
     borderLeftWidth: 2,
     borderRightWidth: 2,
+  },
+  moodCurveSmile: {
+    top: -1,
+    borderBottomWidth: 2,
     borderBottomLeftRadius: 999,
     borderBottomRightRadius: 999,
   },
-  moodLineFrown: {
+  moodCurveFrown: {
+    top: 1,
     borderTopWidth: 2,
-    borderLeftWidth: 2,
-    borderRightWidth: 2,
     borderTopLeftRadius: 999,
     borderTopRightRadius: 999,
   },
@@ -499,6 +1133,19 @@ const styles = StyleSheet.create({
     fontSize: 12,
     lineHeight: 16,
   },
+  successBox: {
+    borderWidth: 1,
+    borderColor: "rgba(53,230,198,0.5)",
+    borderRadius: 16,
+    padding: 13,
+    backgroundColor: "rgba(53,230,198,0.12)",
+  },
+  successText: {
+    color: "#B8FFF1",
+    fontSize: 13,
+    lineHeight: 18,
+    fontWeight: "800",
+  },
   primaryButton: {
     minHeight: 48,
     borderRadius: 16,
@@ -510,6 +1157,24 @@ const styles = StyleSheet.create({
     fontSize: 14,
     fontWeight: "900",
   },
+  demoControls: {
+    flexDirection: "row",
+    flexWrap: "wrap",
+    gap: 8,
+  },
+  demoButton: {
+    flexGrow: 1,
+    borderWidth: 1,
+    borderRadius: 999,
+    alignItems: "center",
+    justifyContent: "center",
+    minHeight: 38,
+    paddingHorizontal: 12,
+  },
+  demoButtonText: {
+    fontSize: 12,
+    fontWeight: "900",
+  },
   noticeBox: {
     borderWidth: 1,
     borderColor: "rgba(44,229,255,0.45)",
