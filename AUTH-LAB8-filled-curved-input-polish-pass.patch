diff --git a/features/auth-motion-lab/AuthMotionLabScreen.tsx b/features/auth-motion-lab/AuthMotionLabScreen.tsx
index e313d40..3b10825 100644
--- a/features/auth-motion-lab/AuthMotionLabScreen.tsx
+++ b/features/auth-motion-lab/AuthMotionLabScreen.tsx
@@ -184,8 +184,6 @@ function getFieldConfigs(mode: AuthMode): FieldConfig[] {
           label: "Confirm password",
           placeholder: "Repeat password",
           secureTextEntry: true,
-          helper:
-            "The frown appears only when the confirmation is wrong or submit is attempted.",
         },
       ];
     case "reset":
@@ -195,7 +193,6 @@ function getFieldConfigs(mode: AuthMode): FieldConfig[] {
           label: "Email",
           placeholder: "you@example.com",
           keyboardType: "email-address",
-          helper: "Reset flow only needs one focused motion test field.",
         },
       ];
     case "login":
@@ -212,7 +209,6 @@ function getFieldConfigs(mode: AuthMode): FieldConfig[] {
           label: "Password",
           placeholder: "At least 8 characters",
           secureTextEntry: true,
-          helper: "Wrong password state should never rely on motion alone.",
         },
       ];
   }
@@ -393,9 +389,9 @@ function getStateProgress(state: MoodState) {
 function getCurveScale(intensity: CurveIntensity) {
   switch (intensity) {
     case "soft":
-      return 0.78;
+      return 0.94;
     case "deep":
-      return 1.18;
+      return 1.08;
     case "standard":
     default:
       return 1;
@@ -436,30 +432,30 @@ function getGlowOpacity(state: MoodState, tuning: MotionTuning) {
     return 0;
   }
 
-  return tuning.glow === "strong" ? 0.34 : 0.18;
+  return tuning.glow === "strong" ? 0.2 : 0.1;
 }
 
 function getTextCurveAmplitude(intensity: CurveIntensity) {
   switch (intensity) {
     case "soft":
-      return 6;
+      return 2;
     case "deep":
-      return 13;
+      return 5;
     case "standard":
     default:
-      return 9;
+      return 3;
   }
 }
 
 function getTextCurveRotation(intensity: CurveIntensity) {
   switch (intensity) {
     case "soft":
-      return 3.5;
+      return 0.4;
     case "deep":
-      return 7.5;
+      return 1.2;
     case "standard":
     default:
-      return 5.5;
+      return 0.7;
   }
 }
 
@@ -500,6 +496,64 @@ function getMoodColor(
   return isFocused ? "#FFB74D" : palette.border;
 }
 
+function getMoodFillColor(
+  state: MoodState,
+  palette: ReturnType<typeof useTheme>["palette"],
+  isTyping: boolean,
+) {
+  const isLight = palette.mode === "light";
+
+  if (isTyping || state === "submitting") {
+    return isLight ? "#FFF4D8" : "#3B2A11";
+  }
+
+  if (state === "smile") {
+    return isLight ? "#E8F9EE" : "#123321";
+  }
+
+  if (state === "frown") {
+    return isLight ? "#FFECEF" : "#3A151D";
+  }
+
+  return palette.surfaceAlt;
+}
+
+function getMoodBorderColor(
+  state: MoodState,
+  palette: ReturnType<typeof useTheme>["palette"],
+  isFocused: boolean,
+  isTyping: boolean,
+) {
+  if (isTyping || state === "submitting") {
+    return "rgba(255,183,77,0.52)";
+  }
+
+  if (state === "smile") {
+    return "rgba(56,232,111,0.46)";
+  }
+
+  if (state === "frown") {
+    return "rgba(255,93,119,0.52)";
+  }
+
+  return isFocused ? "rgba(255,183,77,0.4)" : palette.border;
+}
+
+function getHelperTextColor(
+  state: MoodState,
+  palette: ReturnType<typeof useTheme>["palette"],
+) {
+  if (state === "frown") {
+    return palette.danger;
+  }
+
+  if (state === "submitting") {
+    return palette.mode === "light" ? "#8A5200" : "#FFD38A";
+  }
+
+  return palette.muted;
+}
+
 function ModeTabs({
   activeMode,
   onChange,
@@ -702,23 +756,25 @@ function AuthMoodInput({
   }, [shake, shakeKey, state, tuning]);
 
   const moodColor = getMoodColor(visualState, palette, isFocused, isTyping);
+  const moodFillColor = getMoodFillColor(visualState, palette, isTyping);
+  const moodBorderColor = getMoodBorderColor(
+    visualState,
+    palette,
+    isFocused,
+    isTyping,
+  );
   const curveScale = getCurveScale(tuning.curveIntensity);
-  const stateLabel = isTyping
-    ? "typing"
-    : tuning.reducedMotion && state === "submitting"
-      ? "checking"
-      : state;
   const straightOpacity = progress.interpolate({
     inputRange: [0, 1, 2, 3],
-    outputRange: [1, 0.08, 0.05, 0.3],
+    outputRange: [1, 0.2, 0.2, 0.5],
   });
   const smileOpacity = progress.interpolate({
     inputRange: [0, 1, 2, 3],
-    outputRange: [0, 1, 0, 0.82],
+    outputRange: [0, 0.72, 0, 0.55],
   });
   const frownOpacity = progress.interpolate({
     inputRange: [0, 1, 2, 3],
-    outputRange: [0, 0, 1, 0],
+    outputRange: [0, 0, 0.72, 0],
   });
   const smileScaleY = submitPulse.interpolate({
     inputRange: [0, 1],
@@ -730,14 +786,13 @@ function AuthMoodInput({
   });
   const fieldBodyTranslateY = progress.interpolate({
     inputRange: [0, 1, 2, 3],
-    outputRange: [0, 4, -4, 2],
+    outputRange: [0, 2, -2, 1],
   });
   const fieldBodyScaleY = progress.interpolate({
     inputRange: [0, 1, 2, 3],
-    outputRange: [1, 0.95, 0.95, 0.98],
+    outputRange: [1, 0.985, 0.985, 0.995],
   });
-  const visibleHelper =
-    isTyping && state !== "submitting" ? field.helper : helper;
+  const visibleHelper = state === "straight" && !isTyping ? undefined : helper;
   const displayValue = getCurvedDisplayValue(field, value);
   const showCurvedText =
     displayValue.length > 0 &&
@@ -768,9 +823,6 @@ function AuthMoodInput({
         <Text style={[styles.fieldLabel, { color: palette.text }]}>
           {field.label}
         </Text>
-        <Text style={[styles.fieldStateText, { color: moodColor }]}>
-          {stateLabel === "straight" ? "typing" : stateLabel}
-        </Text>
       </View>
 
       <View
@@ -788,7 +840,8 @@ function AuthMoodInput({
           style={[
             styles.inputFill,
             {
-              backgroundColor: palette.surfaceAlt,
+              backgroundColor: moodFillColor,
+              borderColor: moodBorderColor,
               transform: [
                 { translateY: fieldBodyTranslateY },
                 { scaleY: fieldBodyScaleY },
@@ -800,7 +853,7 @@ function AuthMoodInput({
           pointerEvents="none"
           style={[
             styles.inputStraightFrame,
-            { borderColor: moodColor, opacity: straightOpacity },
+            { borderColor: moodBorderColor, opacity: straightOpacity },
           ]}
         />
         <Animated.View
@@ -809,7 +862,7 @@ function AuthMoodInput({
             styles.inputCurveFrame,
             styles.inputSmileFrame,
             {
-              borderColor: moodColor,
+              borderColor: moodBorderColor,
               opacity: smileOpacity,
               transform: [
                 { scaleX: smileScaleX },
@@ -825,7 +878,7 @@ function AuthMoodInput({
             styles.inputCurveFrame,
             styles.inputFrownFrame,
             {
-              borderColor: moodColor,
+              borderColor: moodBorderColor,
               opacity: frownOpacity,
               transform: [{ scaleY: curveScale }],
             },
@@ -846,7 +899,10 @@ function AuthMoodInput({
           editable={state !== "submitting"}
           style={[
             styles.textInput,
-            { color: showCurvedText ? "transparent" : palette.text },
+            {
+              color: showCurvedText ? "transparent" : palette.text,
+              opacity: showCurvedText ? 0 : 1,
+            },
             Platform.OS === "web" ? styles.webTextInput : null,
           ]}
         />
@@ -854,10 +910,7 @@ function AuthMoodInput({
         {displayValue.length > 0 ? (
           <Animated.View
             pointerEvents="none"
-            style={[
-              styles.curvedTextLayer,
-              { opacity: textBendProgress },
-            ]}
+            style={[styles.curvedTextLayer, { opacity: textBendProgress }]}
           >
             <View style={styles.curvedTextRow}>
               {textCharacters.map((char, index) => {
@@ -865,10 +918,7 @@ function AuthMoodInput({
                   textCharacters.length <= 1
                     ? 0
                     : (index - textCenterIndex) / textCenterIndex;
-                const centerWeight = Math.max(
-                  0,
-                  1 - Math.abs(normalizedIndex),
-                );
+                const centerWeight = Math.max(0, 1 - Math.abs(normalizedIndex));
                 const translateY =
                   textCurveDirection * textCurveAmplitude * centerWeight;
                 const rotate =
@@ -881,7 +931,7 @@ function AuthMoodInput({
                     style={[
                       styles.curvedTextChar,
                       {
-                        color: moodColor,
+                        color: palette.text,
                         transform: [
                           {
                             translateY: textBendProgress.interpolate({
@@ -912,7 +962,7 @@ function AuthMoodInput({
         <Text
           style={[
             styles.helper,
-            { color: visualState === "frown" ? palette.danger : palette.muted },
+            { color: getHelperTextColor(visualState, palette) },
           ]}
         >
           {visibleHelper}
@@ -999,8 +1049,12 @@ function AuthMockFlowPanel({
       ]}
     >
       <View style={styles.flowTopRow}>
-        <Text style={[styles.flowTitle, { color: palette.text }]}>Mock flow</Text>
-        <Text style={[styles.flowBadge, { color: palette.muted }]}>LAB ONLY</Text>
+        <Text style={[styles.flowTitle, { color: palette.text }]}>
+          Mock flow
+        </Text>
+        <Text style={[styles.flowBadge, { color: palette.muted }]}>
+          LAB ONLY
+        </Text>
       </View>
       <Text style={[styles.flowDescription, { color: palette.muted }]}>
         {getModeFlowIntro(mode)}
@@ -1146,8 +1200,12 @@ function TuningToggle({
       ]}
     >
       <View style={styles.tuningToggleCopy}>
-        <Text style={[styles.tuningLabel, { color: palette.text }]}>{label}</Text>
-        <Text style={[styles.tuningToggleDescription, { color: palette.muted }]}>
+        <Text style={[styles.tuningLabel, { color: palette.text }]}>
+          {label}
+        </Text>
+        <Text
+          style={[styles.tuningToggleDescription, { color: palette.muted }]}
+        >
           {description}
         </Text>
       </View>
@@ -1276,25 +1334,38 @@ function AuthInlineStatus({
 
   const isErrorStage = stage === "localError" || stage === "serverError";
   const isSuccessStage = stage === "success";
+  const isLight = palette.mode === "light";
   const borderColor = isErrorStage
     ? "rgba(255,93,119,0.55)"
     : isSuccessStage
-      ? "rgba(53,230,198,0.5)"
+      ? "rgba(53,184,107,0.5)"
       : "rgba(255,183,77,0.5)";
   const backgroundColor = isErrorStage
-    ? "rgba(255,93,119,0.12)"
+    ? isLight
+      ? "#FFECEF"
+      : "rgba(255,93,119,0.12)"
     : isSuccessStage
-      ? "rgba(53,230,198,0.12)"
-      : "rgba(255,183,77,0.12)";
+      ? isLight
+        ? "#E8F9EE"
+        : "rgba(53,230,198,0.12)"
+      : isLight
+        ? "#FFF4D8"
+        : "rgba(255,183,77,0.12)";
   const textColor = isErrorStage
     ? palette.danger
     : isSuccessStage
-      ? "#B8FFF1"
-      : "#FFE0A3";
+      ? isLight
+        ? "#176C3B"
+        : "#B8FFF1"
+      : isLight
+        ? "#8A5200"
+        : "#FFE0A3";
 
   return (
     <View style={[styles.authStatusBox, { borderColor, backgroundColor }]}>
-      <Text style={[styles.authStatusText, { color: textColor }]}>{message}</Text>
+      <Text style={[styles.authStatusText, { color: textColor }]}>
+        {message}
+      </Text>
     </View>
   );
 }
@@ -1522,7 +1593,9 @@ function AuthPreview({
             pressed ? styles.pressed : null,
           ]}
         >
-          <Text style={[styles.backPillText, { color: palette.text }]}>‹ Labs</Text>
+          <Text style={[styles.backPillText, { color: palette.text }]}>
+            ‹ Labs
+          </Text>
         </Pressable>
 
         <Pressable
@@ -1558,8 +1631,12 @@ function AuthPreview({
         <Text style={styles.tradeBadgeText}>TRADE</Text>
       </View>
 
-      <Text style={[styles.previewTitle, { color: palette.text }]}>{copy.title}</Text>
-      <Text style={[styles.previewSubtitle, { color: palette.muted }]}>{copy.subtitle}</Text>
+      <Text style={[styles.previewTitle, { color: palette.text }]}>
+        {copy.title}
+      </Text>
+      <Text style={[styles.previewSubtitle, { color: palette.muted }]}>
+        {copy.subtitle}
+      </Text>
 
       <ModeTabs activeMode={mode} onChange={onModeChange} />
 
@@ -1567,13 +1644,27 @@ function AuthPreview({
         <View
           style={[
             styles.prototypeSettingsPanel,
-            { backgroundColor: palette.surfaceAlt, borderColor: palette.border },
+            {
+              backgroundColor: palette.surfaceAlt,
+              borderColor: palette.border,
+            },
           ]}
         >
           <View style={styles.prototypeSettingsHeader}>
             <View style={styles.prototypeSettingsCopy}>
-              <Text style={[styles.prototypeSettingsTitle, { color: palette.text }]}>Prototype settings</Text>
-              <Text style={[styles.prototypeSettingsSubtitle, { color: palette.muted }]}>Hidden lab controls; close this panel for the clean auth view.</Text>
+              <Text
+                style={[styles.prototypeSettingsTitle, { color: palette.text }]}
+              >
+                Prototype settings
+              </Text>
+              <Text
+                style={[
+                  styles.prototypeSettingsSubtitle,
+                  { color: palette.muted },
+                ]}
+              >
+                Hidden lab controls; close this panel for the clean auth view.
+              </Text>
             </View>
             <Pressable
               onPress={toggleMode}
@@ -1584,7 +1675,9 @@ function AuthPreview({
                 pressed ? styles.pressed : null,
               ]}
             >
-              <Text style={[styles.demoButtonText, { color: palette.text }]}>{themeMode === "dark" ? "Light" : "Dark"}</Text>
+              <Text style={[styles.demoButtonText, { color: palette.text }]}>
+                {themeMode === "dark" ? "Light" : "Dark"}
+              </Text>
             </Pressable>
           </View>
 
@@ -1604,7 +1697,9 @@ function AuthPreview({
                 pressed ? styles.pressed : null,
               ]}
             >
-              <Text style={[styles.demoButtonText, { color: palette.text }]}>Fill valid</Text>
+              <Text style={[styles.demoButtonText, { color: palette.text }]}>
+                Fill valid
+              </Text>
             </Pressable>
             <Pressable
               onPress={triggerErrorDemo}
@@ -1615,7 +1710,9 @@ function AuthPreview({
                 pressed ? styles.pressed : null,
               ]}
             >
-              <Text style={[styles.demoButtonText, { color: palette.text }]}>Trigger error</Text>
+              <Text style={[styles.demoButtonText, { color: palette.text }]}>
+                Trigger error
+              </Text>
             </Pressable>
             <Pressable
               onPress={fillServerCaseDemo}
@@ -1626,7 +1723,9 @@ function AuthPreview({
                 pressed ? styles.pressed : null,
               ]}
             >
-              <Text style={[styles.demoButtonText, { color: palette.text }]}>Server case</Text>
+              <Text style={[styles.demoButtonText, { color: palette.text }]}>
+                Server case
+              </Text>
             </Pressable>
             <Pressable
               onPress={resetDemo}
@@ -1637,7 +1736,9 @@ function AuthPreview({
                 pressed ? styles.pressed : null,
               ]}
             >
-              <Text style={[styles.demoButtonText, { color: palette.text }]}>Reset</Text>
+              <Text style={[styles.demoButtonText, { color: palette.text }]}>
+                Reset
+              </Text>
             </Pressable>
           </View>
 
@@ -1690,8 +1791,25 @@ function AuthPreview({
         </Text>
       </Pressable>
 
-      <View style={styles.noticeBox}>
-        <Text style={styles.noticeText}>
+      <View
+        style={[
+          styles.noticeBox,
+          {
+            borderColor:
+              themeMode === "light"
+                ? "rgba(35,151,190,0.4)"
+                : "rgba(44,229,255,0.45)",
+            backgroundColor:
+              themeMode === "light" ? "#E8F7FF" : "rgba(0,150,220,0.18)",
+          },
+        ]}
+      >
+        <Text
+          style={[
+            styles.noticeText,
+            { color: themeMode === "light" ? "#0A415A" : "#BFEFFF" },
+          ]}
+        >
           Google sign-in is disabled for the first launch. Please use email
           login or create an account with email for now.
         </Text>
@@ -1774,7 +1892,6 @@ export function AuthMotionLabScreen() {
   );
 }
 
-
 const styles = StyleSheet.create({
   screen: {
     flex: 1,
@@ -2081,9 +2198,10 @@ const styles = StyleSheet.create({
     justifyContent: "center",
     paddingHorizontal: 14,
     position: "relative",
-    overflow: "visible",
-    shadowRadius: 12,
-    shadowOffset: { width: 0, height: 6 },
+    overflow: "hidden",
+    borderRadius: 22,
+    shadowRadius: 8,
+    shadowOffset: { width: 0, height: 4 },
   },
   inputFill: {
     position: "absolute",
@@ -2092,6 +2210,7 @@ const styles = StyleSheet.create({
     bottom: 5,
     left: 0,
     borderRadius: 18,
+    borderWidth: 1,
   },
   inputStraightFrame: {
     position: "absolute",
@@ -2113,42 +2232,43 @@ const styles = StyleSheet.create({
   inputSmileFrame: {
     top: 1,
     borderBottomWidth: 1,
-    borderBottomLeftRadius: 46,
-    borderBottomRightRadius: 46,
+    borderBottomLeftRadius: 36,
+    borderBottomRightRadius: 36,
   },
   inputFrownFrame: {
     bottom: 1,
     borderTopWidth: 1,
-    borderTopLeftRadius: 46,
-    borderTopRightRadius: 46,
+    borderTopLeftRadius: 36,
+    borderTopRightRadius: 36,
   },
   textInput: {
     minHeight: 50,
     paddingVertical: 0,
     fontSize: 14,
+    lineHeight: 20,
     fontWeight: "800",
     zIndex: 2,
   },
   curvedTextLayer: {
     position: "absolute",
-    top: 3,
+    top: 6,
     right: 14,
-    bottom: 3,
+    bottom: 6,
     left: 14,
     zIndex: 3,
     justifyContent: "center",
     overflow: "hidden",
   },
   curvedTextRow: {
-    minHeight: 36,
+    minHeight: 28,
     flexDirection: "row",
     alignItems: "center",
     overflow: "hidden",
   },
   curvedTextChar: {
     fontSize: 14,
-    lineHeight: 22,
-    fontWeight: "900",
+    lineHeight: 20,
+    fontWeight: "800",
   },
   webTextInput: {
     outlineStyle: "none",
@@ -2275,13 +2395,10 @@ const styles = StyleSheet.create({
   },
   noticeBox: {
     borderWidth: 1,
-    borderColor: "rgba(44,229,255,0.45)",
     borderRadius: 16,
     padding: 14,
-    backgroundColor: "rgba(0,150,220,0.18)",
   },
   noticeText: {
-    color: "#BFEFFF",
     fontSize: 13,
     lineHeight: 19,
     fontWeight: "800",
