diff --git a/features/nav-lab/NavLabScreen.tsx b/features/nav-lab/NavLabScreen.tsx
index 77832c9..484d63d 100644
--- a/features/nav-lab/NavLabScreen.tsx
+++ b/features/nav-lab/NavLabScreen.tsx
@@ -6,6 +6,7 @@ import {
   ScrollView,
   StyleSheet,
   Text,
+  TextInput,
   useWindowDimensions,
   View,
 } from "react-native";
@@ -14,7 +15,7 @@ import { useSafeAreaInsets } from "react-native-safe-area-context";
 import { useTheme } from "@/providers/ThemeProvider";
 
 import { meHubSections, navLabTabs, planPreviews, tradeFilters, tradePreviews } from "./navLab.mockData";
-import type { MeHubSection, MeHubSectionId, NavLabTabId, PlanPlacePreview, PlanPreview, TradeFilterId, TradePreview } from "./navLab.types";
+import type { MeHubSection, MeHubSectionId, NavLabTabId, PlanMode, PlanPlacePreview, PlanPreview, TradeFilterId, TradePreview } from "./navLab.types";
 
 const desktopBreakpoint = 820;
 const meOrderStorageKey = "hellowhen-nav-lab-me-section-order-v1";
@@ -90,6 +91,127 @@ function getPlanModeLabel(plan: PlanPreview) {
   return plan.mode === "mixed" ? "Mixed" : plan.mode === "online" ? "Online" : "Local";
 }
 
+type PlanCreateStepId = "goal" | "mode" | "places" | "media" | "preview";
+
+type PlanCreateDraft = {
+  title: string;
+  summary: string;
+  category: string;
+  mode: PlanMode;
+  ownerName: string;
+  startLabel: string;
+  capacityLabel: string;
+  places: PlanPlacePreview[];
+};
+
+const planCreateSteps: { id: PlanCreateStepId; label: string; helper: string }[] = [
+  { id: "goal", label: "Goal", helper: "Name the activity." },
+  { id: "mode", label: "Mode", helper: "Local, online, or mixed." },
+  { id: "places", label: "Places", helper: "Order the stops." },
+  { id: "media", label: "Images", helper: "Polish images and notes." },
+  { id: "preview", label: "Preview", helper: "Create the mock plan." },
+];
+
+function getPlanModeDisplay(mode: PlanMode) {
+  return mode === "mixed" ? "Mixed" : mode === "online" ? "Online" : "Local";
+}
+
+function getDefaultPlaceKind(mode: PlanMode, index: number): PlanPlacePreview["kind"] {
+  if (mode === "online") {
+    return "online_place";
+  }
+
+  if (mode === "mixed" && index === 0) {
+    return "online_place";
+  }
+
+  return "local_place";
+}
+
+function getDefaultAddressOrPlatform(mode: PlanMode, index: number) {
+  const kind = getDefaultPlaceKind(mode, index);
+
+  if (kind === "online_place") {
+    return index === 0 ? "Discord" : "Google Meet";
+  }
+
+  return index === 0 ? "Paris" : "Add address";
+}
+
+function createDraftPlace(mode: PlanMode, index: number): PlanPlacePreview {
+  const order = index + 1;
+  const kind = getDefaultPlaceKind(mode, index);
+
+  return {
+    id: `draft-place-${Date.now()}-${order}-${Math.random().toString(16).slice(2, 7)}`,
+    kind,
+    order,
+    title: kind === "online_place" ? `Online stop ${order}` : `Local place ${order}`,
+    addressOrPlatform: getDefaultAddressOrPlatform(mode, index),
+    timeLabel: order === 1 ? "15:00" : `${15 + index}:00`,
+    note: order === 1 ? "Meet, say hello, and start the plan." : "Continue the plan in this ordered stop.",
+    imageLabel: kind === "online_place" ? "Platform visual" : "Place image",
+  };
+}
+
+function createInitialPlanDraft(): PlanCreateDraft {
+  return {
+    title: "Saturday creative plan",
+    summary: "Coffee, a short walk, and a calm open session that people can freely join.",
+    category: "Social",
+    mode: "local",
+    ownerName: "Kopy",
+    startLabel: "Sat 15:00",
+    capacityLabel: "Open spots",
+    places: [createDraftPlace("local", 0), createDraftPlace("local", 1)],
+  };
+}
+
+function normalizeDraftPlacesForMode(places: PlanPlacePreview[], mode: PlanMode) {
+  return places.map((place, index) => ({
+    ...place,
+    kind: getDefaultPlaceKind(mode, index),
+    order: index + 1,
+    addressOrPlatform: place.addressOrPlatform || getDefaultAddressOrPlatform(mode, index),
+    imageLabel: place.imageLabel || (getDefaultPlaceKind(mode, index) === "online_place" ? "Platform visual" : "Place image"),
+  }));
+}
+
+function getPlanPlaceSummary(places: PlanPlacePreview[]) {
+  return places.map((place) => place.title).filter(Boolean).slice(0, 4).join(" → ") || "Add places";
+}
+
+function buildPlanPreviewFromDraft(draft: PlanCreateDraft): PlanPreview {
+  const places = draft.places.map((place, index) => ({
+    ...place,
+    order: index + 1,
+    title: place.title.trim() || `Place ${index + 1}`,
+    addressOrPlatform: place.addressOrPlatform.trim() || getDefaultAddressOrPlatform(draft.mode, index),
+    timeLabel: place.timeLabel.trim() || "Flexible",
+    note: place.note.trim() || "Plan stop note.",
+    imageLabel: place.imageLabel.trim() || (place.kind === "online_place" ? "Platform visual" : "Place image"),
+  }));
+  const modeLabel = getPlanModeDisplay(draft.mode);
+  const slugBase = `${draft.title || "plan"}-${Date.now()}`.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "");
+
+  return {
+    id: `created-${slugBase}`,
+    category: draft.category.trim() || modeLabel,
+    title: draft.title.trim() || "Untitled open plan",
+    status: "open",
+    mode: draft.mode,
+    summary: draft.summary.trim() || "A new open plan created inside the lab prototype.",
+    ownerName: draft.ownerName.trim() || "Kopy",
+    startLabel: draft.startLabel.trim() || "Flexible",
+    placeSummary: getPlanPlaceSummary(places),
+    joinedCount: 0,
+    joinedPreview: [draft.ownerName.trim() || "Kopy"],
+    capacityLabel: draft.capacityLabel.trim() || "Open spots",
+    joinLabel: draft.mode === "online" ? "Join online" : "Join plan",
+    places,
+  };
+}
+
 function LabHeader({ activeTab, isDesktop }: { activeTab: NavLabTabId; isDesktop: boolean }) {
   const { mode, palette, toggleMode } = useTheme();
   const insets = useSafeAreaInsets();
@@ -115,7 +237,7 @@ function LabHeader({ activeTab, isDesktop }: { activeTab: NavLabTabId; isDesktop
         </Pressable>
 
         <View style={styles.headerPill}>
-          <Text style={[styles.headerPillText, { color: palette.muted }]}>PLAN-LAB3</Text>
+          <Text style={[styles.headerPillText, { color: palette.muted }]}>PLAN-LAB4</Text>
         </View>
 
         <Pressable
@@ -782,11 +904,13 @@ function PlanCard({
   expanded,
   onToggle,
   onOpenDetail,
+  onCreateSimilar,
 }: {
   plan: PlanPreview;
   expanded: boolean;
   onToggle: () => void;
   onOpenDetail: () => void;
+  onCreateSimilar?: () => void;
 }) {
   const { palette } = useTheme();
   const modeLabel = getPlanModeLabel(plan);
@@ -860,7 +984,7 @@ function PlanCard({
         <View style={styles.planActions}>
           <PlanActionButton label="View plan" primary onPress={onOpenDetail} />
           <PlanActionButton label={plan.joinLabel} />
-          <PlanActionButton label="Create similar" />
+          <PlanActionButton label="Create similar" onPress={onCreateSimilar} />
         </View>
       ) : null}
     </View>
@@ -990,7 +1114,7 @@ function PlanDetailPage({ plan, onBack }: { plan: PlanPreview; onBack: () => voi
         <View style={styles.heroTextBlock}>
           <Text style={[styles.cardEyebrow, { color: palette.muted }]}>NEXT LAB STEP</Text>
           <Text style={[styles.detailActionTitle, { color: palette.text }]}>Join flow will come after the detail page.</Text>
-          <Text style={[styles.detailHelpText, { color: palette.muted }]}>PLAN-LAB3 only proves the detail layout, timeline, images, and back navigation.</Text>
+          <Text style={[styles.detailHelpText, { color: palette.muted }]}>PLAN-LAB4 keeps the detail page independent while adding a mock create flow for new open plans.</Text>
         </View>
         <View style={styles.planActionsInline}>
           <PlanActionButton label={plan.joinLabel} primary />
@@ -1001,11 +1125,367 @@ function PlanDetailPage({ plan, onBack }: { plan: PlanPreview; onBack: () => voi
   );
 }
 
+function DraftTextField({
+  label,
+  value,
+  onChangeText,
+  multiline,
+  placeholder,
+}: {
+  label: string;
+  value: string;
+  onChangeText: (value: string) => void;
+  multiline?: boolean;
+  placeholder?: string;
+}) {
+  const { palette } = useTheme();
+
+  return (
+    <View style={styles.draftFieldBlock}>
+      <Text style={[styles.draftFieldLabel, { color: palette.muted }]}>{label}</Text>
+      <TextInput
+        value={value}
+        onChangeText={onChangeText}
+        placeholder={placeholder}
+        placeholderTextColor={palette.muted}
+        multiline={multiline}
+        style={[
+          styles.draftTextInput,
+          multiline ? styles.draftTextInputMultiline : null,
+          { color: palette.text, borderColor: palette.border, backgroundColor: palette.surfaceAlt },
+        ]}
+      />
+    </View>
+  );
+}
+
+function CreateStepIndicator({ activeStepId, onSelect }: { activeStepId: PlanCreateStepId; onSelect: (stepId: PlanCreateStepId) => void }) {
+  const { palette } = useTheme();
+
+  return (
+    <View style={styles.createStepRow}>
+      {planCreateSteps.map((step, index) => {
+        const isActive = activeStepId === step.id;
+        return (
+          <Pressable
+            key={step.id}
+            onPress={() => onSelect(step.id)}
+            accessibilityRole="button"
+            accessibilityState={{ selected: isActive }}
+            style={({ pressed }) => [
+              styles.createStepChip,
+              {
+                borderColor: palette.border,
+                backgroundColor: isActive ? palette.text : palette.surfaceAlt,
+              },
+              pressed ? styles.pressed : null,
+              Platform.OS === "web" ? ({ cursor: "pointer" } as any) : null,
+            ]}
+          >
+            <Text style={[styles.createStepNumber, { color: isActive ? palette.background : palette.muted }]}>{index + 1}</Text>
+            <Text style={[styles.createStepLabel, { color: isActive ? palette.background : palette.text }]}>{step.label}</Text>
+          </Pressable>
+        );
+      })}
+    </View>
+  );
+}
+
+function PlanModeChoice({
+  mode,
+  selected,
+  title,
+  helper,
+  onPress,
+}: {
+  mode: PlanMode;
+  selected: boolean;
+  title: string;
+  helper: string;
+  onPress: () => void;
+}) {
+  const { palette } = useTheme();
+
+  return (
+    <Pressable
+      onPress={onPress}
+      accessibilityRole="button"
+      accessibilityState={{ selected }}
+      style={({ pressed }) => [
+        styles.modeChoiceCard,
+        {
+          borderColor: selected ? palette.text : palette.border,
+          backgroundColor: selected ? palette.deckSurface1 : palette.surface,
+        },
+        pressed ? styles.pressed : null,
+        Platform.OS === "web" ? ({ cursor: "pointer" } as any) : null,
+      ]}
+    >
+      <View style={styles.modeChoiceTopRow}>
+        <Text style={[styles.modeChoiceTitle, { color: palette.text }]}>{title}</Text>
+        <View style={[styles.smallTag, { borderColor: palette.border, backgroundColor: palette.surfaceAlt }]}>
+          <Text style={[styles.smallTagText, { color: palette.text }]}>{mode.toUpperCase()}</Text>
+        </View>
+      </View>
+      <Text style={[styles.modeChoiceHelper, { color: palette.muted }]}>{helper}</Text>
+    </Pressable>
+  );
+}
+
+function DraftPlaceEditor({
+  place,
+  canRemove,
+  compact,
+  onChange,
+  onRemove,
+}: {
+  place: PlanPlacePreview;
+  canRemove: boolean;
+  compact?: boolean;
+  onChange: (place: PlanPlacePreview) => void;
+  onRemove: () => void;
+}) {
+  const { palette } = useTheme();
+
+  return (
+    <View style={[styles.draftPlaceCard, { borderColor: palette.border, backgroundColor: palette.surface }]}>
+      <View style={styles.draftPlaceHeaderRow}>
+        <View style={styles.draftPlaceTitleBlock}>
+          <Text style={[styles.cardEyebrow, { color: palette.muted }]}>PLACE {place.order} · {place.kind === "online_place" ? "ONLINE" : "LOCAL"}</Text>
+          <Text style={[styles.draftPlaceTitle, { color: palette.text }]}>{place.title || `Place ${place.order}`}</Text>
+        </View>
+        <Pressable
+          onPress={canRemove ? onRemove : undefined}
+          accessibilityRole="button"
+          accessibilityState={{ disabled: !canRemove }}
+          style={({ pressed }) => [
+            styles.removePlaceButton,
+            { borderColor: palette.border, opacity: canRemove ? 1 : 0.42 },
+            pressed && canRemove ? styles.pressed : null,
+            Platform.OS === "web" && canRemove ? ({ cursor: "pointer" } as any) : null,
+          ]}
+        >
+          <Text style={[styles.removePlaceText, { color: palette.text }]}>Remove</Text>
+        </Pressable>
+      </View>
+
+      {compact ? (
+        <View style={styles.draftFieldGrid}>
+          <DraftTextField label="Image label" value={place.imageLabel} onChangeText={(imageLabel) => onChange({ ...place, imageLabel })} />
+          <View style={styles.fullWidthField}>
+            <DraftTextField label="Short note" value={place.note} onChangeText={(note) => onChange({ ...place, note })} multiline />
+          </View>
+        </View>
+      ) : (
+        <>
+          <View style={styles.draftFieldGrid}>
+            <DraftTextField label="Place / platform" value={place.title} onChangeText={(title) => onChange({ ...place, title })} />
+            <DraftTextField label="Address / platform" value={place.addressOrPlatform} onChangeText={(addressOrPlatform) => onChange({ ...place, addressOrPlatform })} />
+            <DraftTextField label="Time" value={place.timeLabel} onChangeText={(timeLabel) => onChange({ ...place, timeLabel })} />
+            <DraftTextField label="Image label" value={place.imageLabel} onChangeText={(imageLabel) => onChange({ ...place, imageLabel })} />
+          </View>
+          <DraftTextField label="Short note" value={place.note} onChangeText={(note) => onChange({ ...place, note })} multiline />
+        </>
+      )}
+    </View>
+  );
+}
+
+function PlanCreatePreview({ draft }: { draft: PlanCreateDraft }) {
+  const { palette } = useTheme();
+  const previewPlan = buildPlanPreviewFromDraft(draft);
+  const modeLabel = getPlanModeDisplay(draft.mode);
+
+  return (
+    <View style={[styles.createPreviewCard, { borderColor: palette.border, backgroundColor: palette.surface }]}>
+      <View style={styles.cardTopLine}>
+        <Text style={[styles.cardEyebrow, { color: palette.muted }]}>PREVIEW · PLAN DECK</Text>
+        <PlanModeBadge label={modeLabel} />
+      </View>
+      <PlanDeckImageStrip places={previewPlan.places} modeLabel={modeLabel} />
+      <View style={styles.planDeckTitleBlock}>
+        <Text style={[styles.exchangeLabel, { color: palette.muted }]}>{previewPlan.category} plan</Text>
+        <Text style={[styles.exchangeTitle, styles.planDeckTitle, { color: palette.text }]}>{previewPlan.title}</Text>
+        <Text style={[styles.planDeckSummary, { color: palette.muted }]}>{previewPlan.summary}</Text>
+      </View>
+      <PlanPlaceRoute places={previewPlan.places} />
+      <Text style={[styles.tradeMeta, { color: palette.muted }]}>
+        {previewPlan.startLabel} · {previewPlan.places.length} places · mock draft
+      </Text>
+    </View>
+  );
+}
+
+function PlanCreateFlow({
+  draft,
+  stepIndex,
+  onBack,
+  onChangeDraft,
+  onChangeStepIndex,
+  onCreatePlan,
+}: {
+  draft: PlanCreateDraft;
+  stepIndex: number;
+  onBack: () => void;
+  onChangeDraft: (draft: PlanCreateDraft) => void;
+  onChangeStepIndex: (index: number) => void;
+  onCreatePlan: () => void;
+}) {
+  const { palette } = useTheme();
+  const activeStep = planCreateSteps[stepIndex] ?? planCreateSteps[0];
+  const canGoBack = stepIndex > 0;
+  const canGoNext = stepIndex < planCreateSteps.length - 1;
+
+  const updatePlace = (placeId: string, nextPlace: PlanPlacePreview) => {
+    onChangeDraft({
+      ...draft,
+      places: draft.places.map((place) => (place.id === placeId ? nextPlace : place)).map((place, index) => ({ ...place, order: index + 1 })),
+    });
+  };
+
+  const removePlace = (placeId: string) => {
+    onChangeDraft({
+      ...draft,
+      places: draft.places.filter((place) => place.id !== placeId).map((place, index) => ({ ...place, order: index + 1 })),
+    });
+  };
+
+  const addPlace = () => {
+    onChangeDraft({
+      ...draft,
+      places: [...draft.places, createDraftPlace(draft.mode, draft.places.length)],
+    });
+  };
+
+  const changeMode = (mode: PlanMode) => {
+    onChangeDraft({
+      ...draft,
+      mode,
+      category: getPlanModeDisplay(mode),
+      capacityLabel: mode === "online" ? "Open online" : "Open spots",
+      places: normalizeDraftPlacesForMode(draft.places, mode),
+    });
+  };
+
+  return (
+    <View style={styles.tabContent}>
+      <View style={[styles.detailHeaderPanel, { backgroundColor: palette.surface, borderColor: palette.border }]}>
+        <View style={styles.detailTopBar}>
+          <Pressable
+            onPress={onBack}
+            accessibilityRole="button"
+            style={({ pressed }) => [
+              styles.detailBackButton,
+              { borderColor: palette.border, backgroundColor: palette.surfaceAlt },
+              pressed ? styles.pressed : null,
+              Platform.OS === "web" ? ({ cursor: "pointer" } as any) : null,
+            ]}
+          >
+            <Text style={[styles.detailBackText, { color: palette.text }]}>‹ Plans</Text>
+          </Pressable>
+          <View style={styles.detailStatusRow}>
+            <PlanModeBadge label="Mock create" />
+            <PlanModeBadge label={`Step ${stepIndex + 1}/5`} />
+          </View>
+        </View>
+        <View style={styles.heroTextBlock}>
+          <Text style={[styles.heroEyebrow, { color: palette.muted }]}>PLAN-LAB4 · CREATE FLOW</Text>
+          <Text style={[styles.detailTitle, { color: palette.text }]}>Create an open plan.</Text>
+          <Text style={[styles.detailSummary, { color: palette.muted }]}>This is local lab state only: choose a goal, local/online mode, ordered places, image labels, notes, and preview the deck card.</Text>
+        </View>
+        <CreateStepIndicator activeStepId={activeStep.id} onSelect={(stepId) => onChangeStepIndex(planCreateSteps.findIndex((step) => step.id === stepId))} />
+      </View>
+
+      <View style={[styles.createStepPanel, { borderColor: palette.border, backgroundColor: palette.surface }]}>
+        <View style={styles.detailSectionHeader}>
+          <View>
+            <Text style={[styles.cardEyebrow, { color: palette.muted }]}>STEP {stepIndex + 1}</Text>
+            <Text style={[styles.detailSectionTitle, { color: palette.text }]}>{activeStep.label}</Text>
+          </View>
+          <Text style={[styles.detailSectionMeta, { color: palette.muted }]}>{activeStep.helper}</Text>
+        </View>
+
+        {activeStep.id === "goal" ? (
+          <View style={styles.draftFieldGrid}>
+            <DraftTextField label="Plan title" value={draft.title} onChangeText={(title) => onChangeDraft({ ...draft, title })} />
+            <DraftTextField label="Category" value={draft.category} onChangeText={(category) => onChangeDraft({ ...draft, category })} />
+            <DraftTextField label="Start label" value={draft.startLabel} onChangeText={(startLabel) => onChangeDraft({ ...draft, startLabel })} />
+            <DraftTextField label="Capacity label" value={draft.capacityLabel} onChangeText={(capacityLabel) => onChangeDraft({ ...draft, capacityLabel })} />
+            <View style={styles.fullWidthField}>
+              <DraftTextField label="Summary" value={draft.summary} onChangeText={(summary) => onChangeDraft({ ...draft, summary })} multiline />
+            </View>
+          </View>
+        ) : null}
+
+        {activeStep.id === "mode" ? (
+          <View style={styles.modeChoiceGrid}>
+            <PlanModeChoice mode="local" selected={draft.mode === "local"} title="Local places" helper="Cafes, parks, museums, coworking spots, walks, or real addresses." onPress={() => changeMode("local")} />
+            <PlanModeChoice mode="online" selected={draft.mode === "online"} title="Online platforms" helper="Discord, WhatsApp, Figma, Google Meet, Zoom, or other online stops." onPress={() => changeMode("online")} />
+            <PlanModeChoice mode="mixed" selected={draft.mode === "mixed"} title="Mixed plan" helper="Start online or offline, then continue with another ordered stop." onPress={() => changeMode("mixed")} />
+          </View>
+        ) : null}
+
+        {activeStep.id === "places" ? (
+          <View style={styles.draftPlacesStack}>
+            {draft.places.map((place) => (
+              <DraftPlaceEditor key={place.id} place={place} canRemove={draft.places.length > 1} onChange={(nextPlace) => updatePlace(place.id, nextPlace)} onRemove={() => removePlace(place.id)} />
+            ))}
+            <PlanActionButton label="+ Add ordered place" primary onPress={addPlace} />
+          </View>
+        ) : null}
+
+        {activeStep.id === "media" ? (
+          <View style={styles.draftPlacesStack}>
+            <Text style={[styles.detailHelpText, { color: palette.muted }]}>For the lab, images are represented by labels. Later this can become real user-uploaded place images.</Text>
+            {draft.places.map((place) => (
+              <DraftPlaceEditor key={place.id} compact place={place} canRemove={false} onChange={(nextPlace) => updatePlace(place.id, nextPlace)} onRemove={() => undefined} />
+            ))}
+          </View>
+        ) : null}
+
+        {activeStep.id === "preview" ? <PlanCreatePreview draft={draft} /> : null}
+      </View>
+
+      <View style={[styles.createFooterPanel, { borderColor: palette.border, backgroundColor: palette.surface }]}>
+        <PlanActionButton label={canGoBack ? "Back" : "Cancel"} onPress={canGoBack ? () => onChangeStepIndex(stepIndex - 1) : onBack} />
+        <View style={styles.createFooterRight}>
+          {canGoNext ? (
+            <PlanActionButton label="Next" primary onPress={() => onChangeStepIndex(stepIndex + 1)} />
+          ) : (
+            <PlanActionButton label="Create mock plan" primary onPress={onCreatePlan} />
+          )}
+        </View>
+      </View>
+    </View>
+  );
+}
+
+function createDraftFromPlan(plan: PlanPreview): PlanCreateDraft {
+  return {
+    title: plan.title,
+    summary: plan.summary,
+    category: plan.category,
+    mode: plan.mode,
+    ownerName: "Kopy",
+    startLabel: plan.startLabel,
+    capacityLabel: plan.capacityLabel,
+    places: plan.places.map((place, index) => ({
+      ...place,
+      id: `similar-${place.id}-${Date.now()}-${index}`,
+      order: index + 1,
+    })),
+  };
+}
+
 function PlansScreen() {
   const { palette } = useTheme();
+  const [plans, setPlans] = useState<PlanPreview[]>(planPreviews);
   const [expandedPlanIds, setExpandedPlanIds] = useState<string[]>([planPreviews[0]?.id ?? ""]);
   const [selectedPlanId, setSelectedPlanId] = useState<string | null>(null);
-  const selectedPlan = planPreviews.find((plan) => plan.id === selectedPlanId) ?? null;
+  const [createModeOpen, setCreateModeOpen] = useState(false);
+  const [createStepIndex, setCreateStepIndex] = useState(0);
+  const [createDraft, setCreateDraft] = useState<PlanCreateDraft>(() => createInitialPlanDraft());
+  const selectedPlan = plans.find((plan) => plan.id === selectedPlanId) ?? null;
 
   const togglePlan = (planId: string) => {
     setExpandedPlanIds((current) =>
@@ -1013,6 +1493,38 @@ function PlansScreen() {
     );
   };
 
+  const openCreateFlow = (draft?: PlanCreateDraft) => {
+    setCreateDraft(draft ?? createInitialPlanDraft());
+    setCreateStepIndex(0);
+    setSelectedPlanId(null);
+    setCreateModeOpen(true);
+  };
+
+  const closeCreateFlow = () => {
+    setCreateModeOpen(false);
+  };
+
+  const createPlan = () => {
+    const createdPlan = buildPlanPreviewFromDraft(createDraft);
+    setPlans((current) => [createdPlan, ...current]);
+    setExpandedPlanIds((current) => [createdPlan.id, ...current.filter((id) => id !== createdPlan.id)]);
+    setCreateModeOpen(false);
+    setSelectedPlanId(createdPlan.id);
+  };
+
+  if (createModeOpen) {
+    return (
+      <PlanCreateFlow
+        draft={createDraft}
+        stepIndex={createStepIndex}
+        onBack={closeCreateFlow}
+        onChangeDraft={setCreateDraft}
+        onChangeStepIndex={setCreateStepIndex}
+        onCreatePlan={createPlan}
+      />
+    );
+  }
+
   if (selectedPlan) {
     return <PlanDetailPage plan={selectedPlan} onBack={() => setSelectedPlanId(null)} />;
   }
@@ -1030,6 +1542,7 @@ function PlansScreen() {
             </Text>
           </View>
           <Pressable
+            onPress={() => openCreateFlow()}
             accessibilityRole="button"
             style={({ pressed }) => [
               styles.createPlanButton,
@@ -1043,15 +1556,16 @@ function PlansScreen() {
         </View>
       </View>
 
-      <SectionHeader label="MOCK PLAN FEED" title="Open a deck card to view the Plan detail page" />
+      <SectionHeader label="MOCK PLAN FEED" title="Create a local mock Plan, then open its detail page" />
       <View style={styles.cardStack}>
-        {planPreviews.map((plan) => (
+        {plans.map((plan) => (
           <PlanCard
             key={plan.id}
             plan={plan}
             expanded={expandedPlanIds.includes(plan.id)}
             onToggle={() => togglePlan(plan.id)}
             onOpenDetail={() => setSelectedPlanId(plan.id)}
+            onCreateSimilar={() => openCreateFlow(createDraftFromPlan(plan))}
           />
         ))}
       </View>
@@ -2373,6 +2887,152 @@ const styles = StyleSheet.create({
     justifyContent: "flex-end",
     gap: 8,
   },
+  createStepRow: {
+    flexDirection: "row",
+    flexWrap: "wrap",
+    gap: 8,
+  },
+  createStepChip: {
+    minHeight: 38,
+    borderRadius: 999,
+    borderWidth: 1,
+    paddingHorizontal: 11,
+    paddingVertical: 7,
+    flexDirection: "row",
+    alignItems: "center",
+    gap: 6,
+  },
+  createStepNumber: {
+    fontSize: 11,
+    fontWeight: "900",
+  },
+  createStepLabel: {
+    fontSize: 12,
+    fontWeight: "900",
+  },
+  createStepPanel: {
+    borderRadius: 24,
+    borderWidth: 1,
+    padding: 16,
+    gap: 16,
+  },
+  draftFieldGrid: {
+    flexDirection: "row",
+    flexWrap: "wrap",
+    gap: 12,
+  },
+  draftFieldBlock: {
+    flexGrow: 1,
+    flexBasis: 220,
+    gap: 6,
+  },
+  fullWidthField: {
+    width: "100%",
+  },
+  draftFieldLabel: {
+    fontSize: 11,
+    fontWeight: "900",
+    letterSpacing: 0.3,
+  },
+  draftTextInput: {
+    minHeight: 44,
+    borderRadius: 16,
+    borderWidth: 1,
+    paddingHorizontal: 12,
+    paddingVertical: 10,
+    fontSize: 14,
+    fontWeight: "800",
+  },
+  draftTextInputMultiline: {
+    minHeight: 92,
+    lineHeight: 20,
+    textAlignVertical: "top",
+  },
+  modeChoiceGrid: {
+    flexDirection: "row",
+    flexWrap: "wrap",
+    gap: 12,
+  },
+  modeChoiceCard: {
+    flexGrow: 1,
+    flexBasis: 220,
+    borderRadius: 22,
+    borderWidth: 1,
+    padding: 14,
+    gap: 10,
+  },
+  modeChoiceTopRow: {
+    flexDirection: "row",
+    alignItems: "center",
+    justifyContent: "space-between",
+    gap: 10,
+  },
+  modeChoiceTitle: {
+    flex: 1,
+    fontSize: 17,
+    fontWeight: "900",
+    letterSpacing: -0.2,
+  },
+  modeChoiceHelper: {
+    fontSize: 13,
+    lineHeight: 19,
+    fontWeight: "700",
+  },
+  draftPlacesStack: {
+    gap: 12,
+  },
+  draftPlaceCard: {
+    borderRadius: 22,
+    borderWidth: 1,
+    padding: 14,
+    gap: 12,
+  },
+  draftPlaceHeaderRow: {
+    flexDirection: "row",
+    alignItems: "flex-start",
+    justifyContent: "space-between",
+    gap: 12,
+  },
+  draftPlaceTitleBlock: {
+    flex: 1,
+  },
+  draftPlaceTitle: {
+    marginTop: 4,
+    fontSize: 18,
+    fontWeight: "900",
+    letterSpacing: -0.2,
+  },
+  removePlaceButton: {
+    minHeight: 34,
+    borderRadius: 999,
+    borderWidth: 1,
+    paddingHorizontal: 11,
+    alignItems: "center",
+    justifyContent: "center",
+  },
+  removePlaceText: {
+    fontSize: 11,
+    fontWeight: "900",
+  },
+  createPreviewCard: {
+    borderRadius: 24,
+    borderWidth: 1,
+    padding: 16,
+  },
+  createFooterPanel: {
+    borderRadius: 24,
+    borderWidth: 1,
+    padding: 14,
+    flexDirection: "row",
+    alignItems: "center",
+    justifyContent: "space-between",
+    gap: 12,
+  },
+  createFooterRight: {
+    flexDirection: "row",
+    justifyContent: "flex-end",
+    gap: 8,
+  },
   cardFooter: {
     marginTop: 16,
     paddingTop: 12,
