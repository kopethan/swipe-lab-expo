diff --git a/features/nav-lab/NavLabScreen.tsx b/features/nav-lab/NavLabScreen.tsx
index 8aeb467..672e203 100644
--- a/features/nav-lab/NavLabScreen.tsx
+++ b/features/nav-lab/NavLabScreen.tsx
@@ -14,7 +14,7 @@ import { useSafeAreaInsets } from "react-native-safe-area-context";
 import { useTheme } from "@/providers/ThemeProvider";
 
 import { meHubSections, navLabTabs, planPreviews, tradeFilters, tradePreviews } from "./navLab.mockData";
-import type { MeHubSection, MeHubSectionId, NavLabTabId, PlanPreview, TradeFilterId, TradePreview } from "./navLab.types";
+import type { MeHubSection, MeHubSectionId, NavLabTabId, PlanPlacePreview, PlanPreview, TradeFilterId, TradePreview } from "./navLab.types";
 
 const desktopBreakpoint = 820;
 const meOrderStorageKey = "hellowhen-nav-lab-me-section-order-v1";
@@ -111,7 +111,7 @@ function LabHeader({ activeTab, isDesktop }: { activeTab: NavLabTabId; isDesktop
         </Pressable>
 
         <View style={styles.headerPill}>
-          <Text style={[styles.headerPillText, { color: palette.muted }]}>NAV-LAB5</Text>
+          <Text style={[styles.headerPillText, { color: palette.muted }]}>PLAN-LAB1</Text>
         </View>
 
         <Pressable
@@ -678,16 +678,37 @@ function PlanMiniStat({ label, value }: { label: string; value: string | number
   );
 }
 
-function PlanPreviewList({ title, items }: { title: string; items: string[] }) {
+function PlanModeBadge({ label }: { label: string }) {
+  const { palette } = useTheme();
+
+  return (
+    <View style={[styles.planModeBadge, { borderColor: palette.border, backgroundColor: palette.surfaceAlt }]}>
+      <Text style={[styles.planModeBadgeText, { color: palette.text }]}>{label}</Text>
+    </View>
+  );
+}
+
+function PlanPlaceTimeline({ places }: { places: PlanPlacePreview[] }) {
   const { palette } = useTheme();
 
   return (
     <View style={[styles.planPreviewList, { borderColor: palette.border, backgroundColor: palette.surfaceAlt }]}>
-      <Text style={[styles.planColumnTitle, { color: palette.text }]}>{title}</Text>
-      {items.map((item) => (
-        <View key={item} style={styles.planBulletRow}>
-          <Text style={[styles.planBulletDot, { color: palette.muted }]}>•</Text>
-          <Text style={[styles.bulletText, { color: palette.muted }]}>{item}</Text>
+      <Text style={[styles.planColumnTitle, { color: palette.text }]}>Ordered places</Text>
+      {places.map((place) => (
+        <View key={place.id} style={styles.planPlaceRow}>
+          <View style={[styles.planPlaceOrderBadge, { borderColor: palette.border, backgroundColor: palette.surface }]}>
+            <Text style={[styles.planPlaceOrderText, { color: palette.text }]}>{place.order}</Text>
+          </View>
+          <View style={styles.planPlaceTextBlock}>
+            <View style={styles.planPlaceTitleRow}>
+              <Text style={[styles.planPlaceTitle, { color: palette.text }]}>{place.title}</Text>
+              <Text style={[styles.planPlaceTime, { color: palette.muted }]}>{place.timeLabel}</Text>
+            </View>
+            <Text style={[styles.planPlaceMeta, { color: palette.muted }]}>
+              {place.kind === "online_place" ? "Online" : "Local"} · {place.addressOrPlatform}
+            </Text>
+            <Text style={[styles.bulletText, { color: palette.muted }]}>{place.note}</Text>
+          </View>
         </View>
       ))}
     </View>
@@ -704,7 +725,8 @@ function PlanCard({
   onToggle: () => void;
 }) {
   const { palette } = useTheme();
-  const compactMeta = `${plan.needs.length} needs · ${plan.offers.length} offers · ${plan.joinedCount} joined`;
+  const modeLabel = plan.mode === "mixed" ? "Mixed" : plan.mode === "online" ? "Online" : "Local";
+  const compactMeta = `${modeLabel} · ${plan.places.length} places · ${plan.joinedCount} joined`;
 
   return (
     <View style={[styles.planCard, { backgroundColor: palette.surface, borderColor: palette.border }]}>
@@ -724,6 +746,10 @@ function PlanCard({
           </Text>
           <Text style={[styles.planTitle, { color: palette.text }]}>{plan.title}</Text>
           <Text style={[styles.planSummary, { color: palette.muted }]}>{plan.summary}</Text>
+          <View style={styles.planBadgeRow}>
+            <PlanModeBadge label={modeLabel} />
+            <PlanModeBadge label={plan.startLabel} />
+          </View>
         </View>
         <Text style={[styles.mockChevron, { color: palette.muted }]}>{expanded ? "▾" : "▸"}</Text>
       </Pressable>
@@ -736,25 +762,25 @@ function PlanCard({
       ) : (
         <>
           <View style={styles.planStatsRow}>
-            <PlanMiniStat label="needs" value={plan.needs.length} />
-            <PlanMiniStat label="offers" value={plan.offers.length} />
-            <PlanMiniStat label="interested" value={plan.interestedCount} />
+            <PlanMiniStat label="places" value={plan.places.length} />
             <PlanMiniStat label="joined" value={plan.joinedCount} />
+            <PlanMiniStat label="mode" value={modeLabel} />
+            <PlanMiniStat label="status" value={plan.status} />
           </View>
 
           <View style={styles.planColumns}>
-            <PlanPreviewList title="Needs preview" items={plan.needs} />
-            <PlanPreviewList title="Offers preview" items={plan.offers} />
+            <PlanPlaceTimeline places={plan.places} />
           </View>
 
           <View style={[styles.nextStepPanel, { borderColor: palette.border, backgroundColor: palette.surfaceAlt }]}>
-            <Text style={[styles.nextStepLabel, { color: palette.muted }]}>Suggested next step</Text>
-            <Text style={[styles.nextStepText, { color: palette.text }]}>{plan.nextStep}</Text>
+            <Text style={[styles.nextStepLabel, { color: palette.muted }]}>Plan route</Text>
+            <Text style={[styles.nextStepText, { color: palette.text }]}>{plan.placeSummary}</Text>
+            <Text style={[styles.nextStepMeta, { color: palette.muted }]}>Opened by {plan.ownerName} · {plan.capacityLabel}</Text>
           </View>
 
           <View style={[styles.planActions, { borderTopColor: palette.border }]}>
             <PlanActionButton label="View plan" primary />
-            <PlanActionButton label="Help / Join" />
+            <PlanActionButton label={plan.joinLabel} />
             <PlanActionButton label="Create similar" />
           </View>
         </>
@@ -779,10 +805,10 @@ function PlansScreen() {
         <View style={styles.plansHeroTopRow}>
           <View style={styles.heroTextBlock}>
             <Text style={[styles.heroEyebrow, { color: palette.muted }]}>FUTURE MAIN AREA</Text>
-            <Text style={[styles.heroTitle, { color: palette.text }]}>Plans are bigger goals.</Text>
+            <Text style={[styles.heroTitle, { color: palette.text }]}>Plans are open activities.</Text>
             <Text style={[styles.heroCopy, { color: palette.muted }]}>
-              A plan can contain needs, offers, people, and later trades. This lab keeps them as expandable
-              mock plan cards.
+              A plan is separate from trades: choose local places or online platform places, order them,
+              publish the plan, and let people join freely.
             </Text>
           </View>
           <Pressable
@@ -799,7 +825,7 @@ function PlansScreen() {
         </View>
       </View>
 
-      <SectionHeader label="MOCK PLAN FEED" title="Tap a plan to expand needs and offers" />
+      <SectionHeader label="MOCK PLAN FEED" title="Tap a plan to expand ordered places" />
       <View style={styles.cardStack}>
         {planPreviews.map((plan) => (
           <PlanCard
@@ -1641,6 +1667,22 @@ const styles = StyleSheet.create({
     fontSize: 13,
     lineHeight: 19,
   },
+  planBadgeRow: {
+    marginTop: 11,
+    flexDirection: "row",
+    flexWrap: "wrap",
+    gap: 8,
+  },
+  planModeBadge: {
+    borderRadius: 999,
+    borderWidth: 1,
+    paddingHorizontal: 10,
+    paddingVertical: 5,
+  },
+  planModeBadgeText: {
+    fontSize: 11,
+    fontWeight: "900",
+  },
   collapsedPlanMetaRow: {
     marginTop: 14,
     flexDirection: "row",
@@ -1704,6 +1746,46 @@ const styles = StyleSheet.create({
     lineHeight: 18,
     fontWeight: "900",
   },
+  planPlaceRow: {
+    flexDirection: "row",
+    gap: 11,
+    paddingVertical: 4,
+  },
+  planPlaceOrderBadge: {
+    width: 28,
+    height: 28,
+    borderRadius: 999,
+    borderWidth: 1,
+    alignItems: "center",
+    justifyContent: "center",
+  },
+  planPlaceOrderText: {
+    fontSize: 12,
+    fontWeight: "900",
+  },
+  planPlaceTextBlock: {
+    flex: 1,
+    gap: 4,
+  },
+  planPlaceTitleRow: {
+    flexDirection: "row",
+    alignItems: "flex-start",
+    justifyContent: "space-between",
+    gap: 10,
+  },
+  planPlaceTitle: {
+    flex: 1,
+    fontSize: 14,
+    fontWeight: "900",
+  },
+  planPlaceTime: {
+    fontSize: 12,
+    fontWeight: "900",
+  },
+  planPlaceMeta: {
+    fontSize: 12,
+    fontWeight: "700",
+  },
   bulletText: {
     flex: 1,
     fontSize: 13,
@@ -1725,6 +1807,11 @@ const styles = StyleSheet.create({
     fontSize: 14,
     fontWeight: "900",
   },
+  nextStepMeta: {
+    marginTop: 5,
+    fontSize: 12,
+    lineHeight: 17,
+  },
   planActions: {
     marginTop: 14,
     paddingTop: 12,
diff --git a/features/nav-lab/navLab.mockData.ts b/features/nav-lab/navLab.mockData.ts
index 429527b..450c0fc 100644
--- a/features/nav-lab/navLab.mockData.ts
+++ b/features/nav-lab/navLab.mockData.ts
@@ -4,7 +4,7 @@ export const navLabTabs: NavLabTab[] = [
   {
     id: "plans",
     label: "Plans",
-    tagline: "Find people around bigger goals.",
+    tagline: "Open local or online plans with people.",
   },
   {
     id: "me",
@@ -35,10 +35,10 @@ export const meHubSections: MeHubSection[] = [
     id: "plans",
     title: "Plans",
     eyebrow: "Bigger goals",
-    summary: "Created and joined plans can become the future home for multi-step social projects.",
+    summary: "Created and joined plans stay separate from trades: local places, online sessions, and open social activities.",
     items: [
-      { id: "created-plans", label: "Created plans", count: 3, subtitle: "Your public or private goals" },
-      { id: "joined-plans", label: "Joined plans", count: 1, subtitle: "Plans where you offered help" },
+      { id: "created-plans", label: "Created plans", count: 3, subtitle: "Your open activities and drafts" },
+      { id: "joined-plans", label: "Joined plans", count: 1, subtitle: "Plans you joined freely" },
     ],
   },
   {
@@ -67,40 +67,132 @@ export const meHubSections: MeHubSection[] = [
 
 export const planPreviews: PlanPreview[] = [
   {
-    id: "launch-app-paris",
-    category: "Startup",
-    title: "Launch my app in Paris",
-    status: "Open",
-    summary: "A public goal page for finding early testers, copy feedback, and simple launch help.",
-    needs: ["5 app testers", "French copy review", "Landing page feedback"],
-    offers: ["Product photo editing", "UX feedback"],
-    interestedCount: 3,
-    joinedCount: 1,
-    nextStep: "Ask for help on app testing",
-  },
-  {
-    id: "practice-french-locally",
-    category: "Social",
-    title: "Practice French locally",
-    status: "Open",
-    summary: "A calm social plan for language practice, walks, and small mutual support.",
-    needs: ["Conversation walk", "Cafe practice partner", "Help with French slang"],
-    offers: ["English conversation", "Portrait photos"],
-    interestedCount: 4,
-    joinedCount: 2,
-    nextStep: "Join as a conversation partner",
-  },
-  {
-    id: "move-apartment",
+    id: "paris-creative-afternoon",
     category: "Local",
-    title: "Move into a new apartment",
-    status: "Draft",
-    summary: "A practical plan that groups small local needs and useful offers before becoming trades.",
-    needs: ["Someone with a small car", "Borrow a drill", "Help carrying plants"],
-    offers: ["Website feedback", "Cook a homemade meal", "Photo editing"],
-    interestedCount: 0,
+    title: "Paris creative afternoon",
+    status: "open",
+    mode: "local",
+    summary: "Coffee, a short walk, and a calm work session arranged as an open plan people can freely join.",
+    ownerName: "Mina",
+    startLabel: "Sat 15:00",
+    placeSummary: "Republique → Canal → Coworking cafe",
+    joinedCount: 4,
+    capacityLabel: "Open spots",
+    joinLabel: "Join plan",
+    places: [
+      {
+        id: "republique-cafe",
+        kind: "local_place",
+        order: 1,
+        title: "Cafe near Republique",
+        addressOrPlatform: "Paris 11e",
+        timeLabel: "15:00",
+        note: "Meet, say hello, and choose a small table.",
+        imageLabel: "Cafe image",
+      },
+      {
+        id: "canal-walk",
+        kind: "local_place",
+        order: 2,
+        title: "Canal Saint-Martin walk",
+        addressOrPlatform: "Paris 10e",
+        timeLabel: "16:00",
+        note: "Walk, take photos, and continue the conversation.",
+        imageLabel: "Canal image",
+      },
+      {
+        id: "coworking-cafe",
+        kind: "local_place",
+        order: 3,
+        title: "Small coworking cafe",
+        addressOrPlatform: "Near Gare de l'Est",
+        timeLabel: "17:00",
+        note: "Quiet work block for anyone who wants to stay.",
+        imageLabel: "Workspace image",
+      },
+    ],
+  },
+  {
+    id: "online-design-review",
+    category: "Online",
+    title: "Online design review session",
+    status: "open",
+    mode: "online",
+    summary: "A lightweight online plan with ordered platform stops for intro, review, and final notes.",
+    ownerName: "Alex",
+    startLabel: "Tonight 20:30",
+    placeSummary: "Discord → Figma → Google Meet",
+    joinedCount: 6,
+    capacityLabel: "Open online",
+    joinLabel: "Join online",
+    places: [
+      {
+        id: "discord-intro",
+        kind: "online_place",
+        order: 1,
+        title: "Discord lobby",
+        addressOrPlatform: "Discord",
+        timeLabel: "20:30",
+        note: "Quick intro and link sharing before the review.",
+        imageLabel: "Discord visual",
+      },
+      {
+        id: "figma-review",
+        kind: "online_place",
+        order: 2,
+        title: "Figma board",
+        addressOrPlatform: "Figma",
+        timeLabel: "20:45",
+        note: "Everyone leaves short visual comments.",
+        imageLabel: "Figma visual",
+      },
+      {
+        id: "meet-wrap",
+        kind: "online_place",
+        order: 3,
+        title: "Wrap-up call",
+        addressOrPlatform: "Google Meet",
+        timeLabel: "21:15",
+        note: "Share the best feedback and next steps.",
+        imageLabel: "Meet visual",
+      },
+    ],
+  },
+  {
+    id: "sunday-language-walk",
+    category: "Mixed",
+    title: "Sunday language walk",
+    status: "draft",
+    mode: "mixed",
+    summary: "A mixed plan that starts online for introductions, then continues with a local language walk.",
+    ownerName: "Sara",
+    startLabel: "Sun 11:00",
+    placeSummary: "WhatsApp → Luxembourg Gardens",
     joinedCount: 0,
-    nextStep: "Create first open need",
+    capacityLabel: "Draft",
+    joinLabel: "Preview plan",
+    places: [
+      {
+        id: "whatsapp-intro",
+        kind: "online_place",
+        order: 1,
+        title: "WhatsApp intro",
+        addressOrPlatform: "WhatsApp",
+        timeLabel: "10:30",
+        note: "Confirm who is coming before meeting outside.",
+        imageLabel: "Chat visual",
+      },
+      {
+        id: "luxembourg-gardens",
+        kind: "local_place",
+        order: 2,
+        title: "Luxembourg Gardens",
+        addressOrPlatform: "Paris 6e",
+        timeLabel: "11:00",
+        note: "Walk and practice French / English in pairs.",
+        imageLabel: "Garden image",
+      },
+    ],
   },
 ];
 
diff --git a/features/nav-lab/navLab.types.ts b/features/nav-lab/navLab.types.ts
index 2da1011..65638dc 100644
--- a/features/nav-lab/navLab.types.ts
+++ b/features/nav-lab/navLab.types.ts
@@ -23,17 +23,37 @@ export type MeHubSection = {
   items: MeHubItem[];
 };
 
+export type PlanMode = "local" | "online" | "mixed";
+
+export type PlanStatus = "draft" | "open" | "full" | "started" | "completed" | "cancelled";
+
+export type PlanPlaceKind = "local_place" | "online_place";
+
+export type PlanPlacePreview = {
+  id: string;
+  kind: PlanPlaceKind;
+  order: number;
+  title: string;
+  addressOrPlatform: string;
+  timeLabel: string;
+  note: string;
+  imageLabel: string;
+};
+
 export type PlanPreview = {
   id: string;
   category: string;
   title: string;
-  status: string;
+  status: PlanStatus;
+  mode: PlanMode;
   summary: string;
-  needs: string[];
-  offers: string[];
-  interestedCount: number;
+  ownerName: string;
+  startLabel: string;
+  placeSummary: string;
+  places: PlanPlacePreview[];
   joinedCount: number;
-  nextStep: string;
+  capacityLabel: string;
+  joinLabel: string;
 };
 
 export type TradeFilterId = "all" | "trades" | "needs" | "offers";
