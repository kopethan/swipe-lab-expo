diff --git a/docs/SQUARE_STACK_DECK_LAB.md b/docs/SQUARE_STACK_DECK_LAB.md
index 800010d..cfcb216 100644
--- a/docs/SQUARE_STACK_DECK_LAB.md
+++ b/docs/SQUARE_STACK_DECK_LAB.md
@@ -1,4 +1,4 @@
-﻿# Square Stack Deck Lab
+# Square Stack Deck Lab
 
 This lab is intentionally separate from the existing `features/deck` implementation.
 
@@ -184,3 +184,11 @@ The intended result is not a frosted rectangle. It should feel like the image it
 - Increased the blur slice count and overlap so the blur grows more continuously from top to bottom.
 - Slightly expanded the atmosphere height again, with stronger protection on static-map cards.
 
+
+
+## LAB5f — Remove blur slice seam lines
+
+- Replaced the square-deck custom lower atmosphere stack with the same full-card slice model used by the image-card prototype.
+- Added slight slice overscan so adjacent blur slices overlap and no horizontal seam lines appear between them.
+- Removed extra bridge bands, feather overlays, accent wash, and the top shade layer from the square deck card blur treatment.
+- Static-map cards still begin the support zone slightly earlier than photo cards for readability.
diff --git a/features/square-stack-deck/components/HelloWhenPlaceCardDemo.tsx b/features/square-stack-deck/components/HelloWhenPlaceCardDemo.tsx
index ca6dc33..333acb9 100644
--- a/features/square-stack-deck/components/HelloWhenPlaceCardDemo.tsx
+++ b/features/square-stack-deck/components/HelloWhenPlaceCardDemo.tsx
@@ -1,4 +1,4 @@
-﻿import React, { memo, useMemo } from "react";
+import React, { memo, useMemo } from "react";
 import { Image } from "expo-image";
 import { StyleSheet, Text, View } from "react-native";
 
@@ -29,97 +29,49 @@ type Props = {
   total: number;
 };
 
-const BLUR_SLICE_COUNT = 28;
-const BRIDGE_BAND_COUNT = 4;
+const BLUR_SLICE_COUNT = 24;
 
 type LowerAtmosphereProps = {
   source?: { uri: string };
-  accent: string;
   isDark: boolean;
   mediaKind: DemoPlaceMediaKind;
 };
 
-function clamp(value: number, min: number, max: number) {
-  return Math.min(max, Math.max(min, value));
-}
-
-function LowerAtmosphere({ source, accent, isDark, mediaKind }: LowerAtmosphereProps) {
-  const slices = useMemo(() => Array.from({ length: BLUR_SLICE_COUNT }, (_, index) => index), []);
-  const bridgeBands = useMemo(() => Array.from({ length: BRIDGE_BAND_COUNT }, (_, index) => index), []);
+function LowerAtmosphere({ source, isDark, mediaKind }: LowerAtmosphereProps) {
   const isStaticMap = mediaKind === "static_map";
-  const atmosphereHeight = isStaticMap ? 62 : 58;
-  const tintBase = isDark
+  const blurStartPercent = isStaticMap ? 46 : 50;
+  const blurSlices = isStaticMap ? 26 : BLUR_SLICE_COUNT;
+  const maxBlurRadius = isStaticMap ? 30 : 26;
+  const bottomTint = isDark
     ? isStaticMap
-      ? "rgba(3,5,8,0.78)"
-      : "rgba(4,6,9,0.68)"
+      ? "rgba(6,10,16,0.52)"
+      : "rgba(6,10,16,0.42)"
     : isStaticMap
-      ? "rgba(255,255,255,0.80)"
-      : "rgba(255,255,255,0.58)";
-  const bottomVeil = isDark
-    ? isStaticMap
-      ? "rgba(2,4,7,0.84)"
-      : "rgba(2,4,7,0.68)"
-    : isStaticMap
-      ? "rgba(255,255,255,0.76)"
-      : "rgba(255,255,255,0.50)";
-  const midVeil = isDark
-    ? isStaticMap
-      ? "rgba(4,7,12,0.22)"
-      : "rgba(4,7,12,0.14)"
-    : isStaticMap
-      ? "rgba(255,255,255,0.14)"
-      : "rgba(255,255,255,0.08)";
+      ? "rgba(255,255,255,0.58)"
+      : "rgba(255,255,255,0.42)";
+  const tintStrength = isStaticMap ? 0.88 : 0.8;
 
-  return (
-    <View pointerEvents="none" style={[styles.lowerAtmosphere, { height: `${atmosphereHeight}%` }]}> 
-      {source ? (
-        <>
-          {bridgeBands.map((band) => {
-            const progress = band / Math.max(1, BRIDGE_BAND_COUNT - 1);
-            const top = 6 + progress * 26;
-            const height = 22 + progress * 10;
-            const opacity = 0.022 + progress * (isStaticMap ? 0.05 : 0.04);
-            const blurRadius = 6 + progress * (isStaticMap ? 9 : 7);
-
-            return (
-              <View
-                key={`atmosphere-bridge-${band}`}
-                style={[
-                  styles.atmosphereSlice,
-                  {
-                    top: `${top}%`,
-                    height: `${height}%`,
-                    opacity,
-                  },
-                ]}
-              >
-                <Image
-                  source={source}
-                  contentFit="cover"
-                  transition={0}
-                  blurRadius={blurRadius}
-                  style={StyleSheet.absoluteFillObject}
-                />
-              </View>
-            );
-          })}
+  const safeStart = Math.max(0, Math.min(92, blurStartPercent));
+  const overlayHeight = 100 - safeStart;
+  const sliceHeight = overlayHeight / blurSlices;
+  const overscan = Math.max(0.85, sliceHeight * 0.55);
 
-          {slices.map((slice) => {
-            const progress = (slice + 1) / BLUR_SLICE_COUNT;
+  return (
+    <View pointerEvents="none" style={StyleSheet.absoluteFillObject}>
+      {source
+        ? Array.from({ length: blurSlices }, (_, index) => {
+            const progress = (index + 1) / blurSlices;
             const eased = progress * progress * (3 - 2 * progress);
-            const tapered = clamp((eased - 0.1) / 0.9, 0, 1);
-            const lift = tapered * tapered * (3 - 2 * tapered);
-            const sliceHeight = 100 / BLUR_SLICE_COUNT;
-            const top = slice * sliceHeight - 2.4;
-            const height = sliceHeight + 6.5;
-            const blurOpacity = Math.pow(lift, 1.65) * (isStaticMap ? 0.92 : 0.84);
-            const tintOpacity = Math.pow(lift, 1.85) * (isStaticMap ? 0.72 : 0.60);
+            const top = safeStart + index * sliceHeight - overscan * 0.5;
+            const height = sliceHeight + overscan;
+            const blurOpacity = eased * (isStaticMap ? 0.94 : 0.88);
+            const tintOpacity = eased * tintStrength;
 
             return (
-              <React.Fragment key={`atmosphere-slice-${slice}`}>
+              <React.Fragment key={`blur-slice-${index}`}>
                 <View
                   style={[
-                    styles.atmosphereSlice,
+                    styles.slice,
                     {
                       top: `${top}%`,
                       height: `${height}%`,
@@ -131,46 +83,26 @@ function LowerAtmosphere({ source, accent, isDark, mediaKind }: LowerAtmosphereP
                     source={source}
                     contentFit="cover"
                     transition={0}
-                    blurRadius={6 + lift * (isStaticMap ? 30 : 24)}
+                    blurRadius={6 + eased * (maxBlurRadius - 6)}
                     style={StyleSheet.absoluteFillObject}
                   />
                 </View>
+
                 <View
                   style={[
-                    styles.atmosphereSlice,
+                    styles.slice,
                     {
                       top: `${top}%`,
                       height: `${height}%`,
-                      backgroundColor: tintBase,
+                      backgroundColor: bottomTint,
                       opacity: tintOpacity,
                     },
                   ]}
                 />
               </React.Fragment>
             );
-          })}
-        </>
-      ) : (
-        <View
-          style={[
-            StyleSheet.absoluteFillObject,
-            { backgroundColor: isDark ? "rgba(3,5,8,0.48)" : "rgba(255,255,255,0.42)" },
-          ]}
-        />
-      )}
-
-      <View style={[styles.atmosphereFeather, styles.atmosphereFeatherTop, { backgroundColor: midVeil }]} />
-      <View style={[styles.atmosphereFeather, styles.atmosphereFeatherMid, { backgroundColor: midVeil }]} />
-      <View style={[styles.atmosphereFeather, styles.atmosphereFeatherBottom, { backgroundColor: bottomVeil }]} />
-      <View
-        style={[
-          styles.atmosphereAccentWash,
-          {
-            backgroundColor: accent,
-            opacity: isStaticMap ? 0.05 : 0.04,
-          },
-        ]}
-      />
+          })
+        : null}
     </View>
   );
 }
@@ -237,7 +169,7 @@ function HelloWhenPlaceCardDemoInner({ card }: Props) {
   );
 
   return (
-    <View style={[styles.card, { backgroundColor: mediaSurface }]}>
+    <View style={[styles.card, { backgroundColor: mediaSurface }]}> 
       {mediaSource ? (
         <Image
           source={mediaSource}
@@ -247,7 +179,7 @@ function HelloWhenPlaceCardDemoInner({ card }: Props) {
           cachePolicy="memory-disk"
         />
       ) : (
-        <View style={[StyleSheet.absoluteFillObject, styles.fallbackMedia, { backgroundColor: mediaSurface }]}>
+        <View style={[StyleSheet.absoluteFillObject, styles.fallbackMedia, { backgroundColor: mediaSurface }]}> 
           {fallbackLines.map((line) => (
             <View
               key={`fallback-line-${card.id}-${line}`}
@@ -265,12 +197,11 @@ function HelloWhenPlaceCardDemoInner({ card }: Props) {
         </View>
       )}
 
-      <View style={styles.topShade} />
-      <LowerAtmosphere source={mediaSource} accent={card.accent} isDark={isDark} mediaKind={card.mediaKind} />
+      <LowerAtmosphere source={mediaSource} isDark={isDark} mediaKind={card.mediaKind} />
 
       <View style={styles.contentLayer}>
         <View style={styles.topBar}>
-          <View style={[styles.modeChip, { backgroundColor: modeChipBg, borderColor: modeChipBorder }]}>
+          <View style={[styles.modeChip, { backgroundColor: modeChipBg, borderColor: modeChipBorder }]}> 
             <Text style={[styles.modeChipText, { color: eyebrowColor }]}>{model.modeLabel}</Text>
           </View>
         </View>
@@ -295,7 +226,7 @@ function HelloWhenPlaceCardDemoInner({ card }: Props) {
                 </Text>
               ) : null}
               {model.contextBadgeLabel ? (
-                <View style={[styles.metaPill, { backgroundColor: directOverlayPillBg, borderColor: directOverlayPillBorder }]}>
+                <View style={[styles.metaPill, { backgroundColor: directOverlayPillBg, borderColor: directOverlayPillBorder }]}> 
                   <Text numberOfLines={1} style={[styles.metaPillText, { color: eyebrowColor }]}>
                     {model.contextBadgeLabel}
                   </Text>
@@ -340,14 +271,14 @@ function HelloWhenPlaceCardDemoInner({ card }: Props) {
           {model.visibleTags.length ? (
             <View style={styles.footerRow}>
               {model.visibleTags.map((tag) => (
-                <View key={`${card.id}-${tag}`} style={[styles.tagChip, { backgroundColor: directOverlayPillBg, borderColor: directOverlayPillBorder }]}>
+                <View key={`${card.id}-${tag}`} style={[styles.tagChip, { backgroundColor: directOverlayPillBg, borderColor: directOverlayPillBorder }]}> 
                   <Text numberOfLines={1} style={[styles.tagText, { color: eyebrowColor }]}>
                     {tag}
                   </Text>
                 </View>
               ))}
               {model.extraTagCount > 0 ? (
-                <View style={[styles.tagChip, { backgroundColor: directOverlayPillBg, borderColor: directOverlayPillBorder }]}>
+                <View style={[styles.tagChip, { backgroundColor: directOverlayPillBg, borderColor: directOverlayPillBorder }]}> 
                   <Text numberOfLines={1} style={[styles.tagText, { color: eyebrowColor }]}>
                     +{model.extraTagCount}
                   </Text>
@@ -389,55 +320,12 @@ const styles = StyleSheet.create({
     borderRadius: 999,
     opacity: 0.82,
   },
-  topShade: {
-    position: "absolute",
-    left: 0,
-    right: 0,
-    top: 0,
-    height: "26%",
-    backgroundColor: "rgba(0,0,0,0.10)",
-  },
-  lowerAtmosphere: {
-    position: "absolute",
-    left: 0,
-    right: 0,
-    bottom: 0,
-    overflow: "hidden",
-  },
-  atmosphereSlice: {
+  slice: {
     position: "absolute",
     left: 0,
     right: 0,
     overflow: "hidden",
   },
-  atmosphereFeather: {
-    position: "absolute",
-    left: 0,
-    right: 0,
-  },
-  atmosphereFeatherTop: {
-    top: 0,
-    height: "30%",
-    opacity: 0.06,
-  },
-  atmosphereFeatherMid: {
-    top: "22%",
-    height: "34%",
-    opacity: 0.10,
-  },
-  atmosphereFeatherBottom: {
-    bottom: 0,
-    height: "64%",
-    opacity: 0.76,
-  },
-  atmosphereAccentWash: {
-    position: "absolute",
-    left: "10%",
-    right: "10%",
-    bottom: "3%",
-    height: "56%",
-    borderRadius: 999,
-  },
   contentLayer: {
     ...StyleSheet.absoluteFillObject,
     justifyContent: "space-between",
@@ -532,4 +420,3 @@ const styles = StyleSheet.create({
     fontWeight: "700",
   },
 });
-
