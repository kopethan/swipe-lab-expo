diff --git a/features/auth-motion-lab/AuthMotionLabScreen.tsx b/features/auth-motion-lab/AuthMotionLabScreen.tsx
index 3b10825..3c8d1a8 100644
--- a/features/auth-motion-lab/AuthMotionLabScreen.tsx
+++ b/features/auth-motion-lab/AuthMotionLabScreen.tsx
@@ -74,9 +74,9 @@ const modes: { id: AuthMode; label: string }[] = [
 ];
 
 const curveChoices: TuningChoice<CurveIntensity>[] = [
-  { value: "soft", label: "Soft" },
-  { value: "standard", label: "Standard" },
-  { value: "deep", label: "Deep" },
+  { value: "soft", label: "Subtle" },
+  { value: "standard", label: "Medium" },
+  { value: "deep", label: "Strong" },
 ];
 
 const speedChoices: TuningChoice<MotionSpeed>[] = [
@@ -386,15 +386,15 @@ function getStateProgress(state: MoodState) {
   }
 }
 
-function getCurveScale(intensity: CurveIntensity) {
+function getBendMetrics(intensity: CurveIntensity) {
   switch (intensity) {
     case "soft":
-      return 0.94;
+      return { bulgeHeight: 20, bulgeInset: 74, bulgeOffset: 5 };
     case "deep":
-      return 1.08;
+      return { bulgeHeight: 28, bulgeInset: 48, bulgeOffset: 1 };
     case "standard":
     default:
-      return 1;
+      return { bulgeHeight: 24, bulgeInset: 60, bulgeOffset: 3 };
   }
 }
 
@@ -504,15 +504,15 @@ function getMoodFillColor(
   const isLight = palette.mode === "light";
 
   if (isTyping || state === "submitting") {
-    return isLight ? "#FFF4D8" : "#3B2A11";
+    return isLight ? "#FFF8E7" : "#34250F";
   }
 
   if (state === "smile") {
-    return isLight ? "#E8F9EE" : "#123321";
+    return isLight ? "#F0FBF4" : "#102719";
   }
 
   if (state === "frown") {
-    return isLight ? "#FFECEF" : "#3A151D";
+    return isLight ? "#FFF3F5" : "#35131A";
   }
 
   return palette.surfaceAlt;
@@ -525,15 +525,15 @@ function getMoodBorderColor(
   isTyping: boolean,
 ) {
   if (isTyping || state === "submitting") {
-    return "rgba(255,183,77,0.52)";
+    return "rgba(255,183,77,0.36)";
   }
 
   if (state === "smile") {
-    return "rgba(56,232,111,0.46)";
+    return "rgba(53,184,107,0.32)";
   }
 
   if (state === "frown") {
-    return "rgba(255,93,119,0.52)";
+    return "rgba(255,93,119,0.38)";
   }
 
   return isFocused ? "rgba(255,183,77,0.4)" : palette.border;
@@ -763,18 +763,14 @@ function AuthMoodInput({
     isFocused,
     isTyping,
   );
-  const curveScale = getCurveScale(tuning.curveIntensity);
-  const straightOpacity = progress.interpolate({
-    inputRange: [0, 1, 2, 3],
-    outputRange: [1, 0.2, 0.2, 0.5],
-  });
+  const bendMetrics = getBendMetrics(tuning.curveIntensity);
   const smileOpacity = progress.interpolate({
     inputRange: [0, 1, 2, 3],
-    outputRange: [0, 0.72, 0, 0.55],
+    outputRange: [0, 0.9, 0, 0.55],
   });
   const frownOpacity = progress.interpolate({
     inputRange: [0, 1, 2, 3],
-    outputRange: [0, 0, 0.72, 0],
+    outputRange: [0, 0, 0.9, 0],
   });
   const smileScaleY = submitPulse.interpolate({
     inputRange: [0, 1],
@@ -784,20 +780,9 @@ function AuthMoodInput({
     inputRange: [0, 1],
     outputRange: [0.94, 1.02],
   });
-  const fieldBodyTranslateY = progress.interpolate({
-    inputRange: [0, 1, 2, 3],
-    outputRange: [0, 2, -2, 1],
-  });
-  const fieldBodyScaleY = progress.interpolate({
-    inputRange: [0, 1, 2, 3],
-    outputRange: [1, 0.985, 0.985, 0.995],
-  });
   const visibleHelper = state === "straight" && !isTyping ? undefined : helper;
   const displayValue = getCurvedDisplayValue(field, value);
-  const showCurvedText =
-    displayValue.length > 0 &&
-    !isTyping &&
-    (visualState === "smile" || visualState === "frown");
+  const showCurvedText = false;
   const textCurveDirection = visualState === "smile" ? 1 : -1;
   const textCurveAmplitude = getTextCurveAmplitude(tuning.curveIntensity);
   const textCurveRotation = getTextCurveRotation(tuning.curveIntensity);
@@ -842,45 +827,39 @@ function AuthMoodInput({
             {
               backgroundColor: moodFillColor,
               borderColor: moodBorderColor,
-              transform: [
-                { translateY: fieldBodyTranslateY },
-                { scaleY: fieldBodyScaleY },
-              ],
             },
           ]}
         />
         <Animated.View
           pointerEvents="none"
           style={[
-            styles.inputStraightFrame,
-            { borderColor: moodBorderColor, opacity: straightOpacity },
-          ]}
-        />
-        <Animated.View
-          pointerEvents="none"
-          style={[
-            styles.inputCurveFrame,
-            styles.inputSmileFrame,
+            styles.inputBendBulge,
+            styles.inputSmileBulge,
             {
+              left: bendMetrics.bulgeInset,
+              right: bendMetrics.bulgeInset,
+              height: bendMetrics.bulgeHeight,
+              bottom: bendMetrics.bulgeOffset,
+              backgroundColor: moodFillColor,
               borderColor: moodBorderColor,
               opacity: smileOpacity,
-              transform: [
-                { scaleX: smileScaleX },
-                { scaleY: curveScale },
-                { scaleY: smileScaleY },
-              ],
+              transform: [{ scaleX: smileScaleX }, { scaleY: smileScaleY }],
             },
           ]}
         />
         <Animated.View
           pointerEvents="none"
           style={[
-            styles.inputCurveFrame,
-            styles.inputFrownFrame,
+            styles.inputBendBulge,
+            styles.inputFrownBulge,
             {
+              left: bendMetrics.bulgeInset,
+              right: bendMetrics.bulgeInset,
+              height: bendMetrics.bulgeHeight,
+              top: bendMetrics.bulgeOffset,
+              backgroundColor: moodFillColor,
               borderColor: moodBorderColor,
               opacity: frownOpacity,
-              transform: [{ scaleY: curveScale }],
             },
           ]}
         />
@@ -899,10 +878,7 @@ function AuthMoodInput({
           editable={state !== "submitting"}
           style={[
             styles.textInput,
-            {
-              color: showCurvedText ? "transparent" : palette.text,
-              opacity: showCurvedText ? 0 : 1,
-            },
+            { color: palette.text },
             Platform.OS === "web" ? styles.webTextInput : null,
           ]}
         />
@@ -1282,7 +1258,7 @@ function AuthMotionTuningPanel({
 
       <View style={styles.tuningGrid}>
         <TuningOptionGroup
-          label="Curve strength"
+          label="Field bend"
           value={tuning.curveIntensity}
           options={curveChoices}
           onChange={(curveIntensity) => update({ curveIntensity })}
@@ -2194,7 +2170,7 @@ const styles = StyleSheet.create({
     textTransform: "uppercase",
   },
   inputShell: {
-    minHeight: 56,
+    minHeight: 52,
     justifyContent: "center",
     paddingHorizontal: 14,
     position: "relative",
@@ -2205,44 +2181,30 @@ const styles = StyleSheet.create({
   },
   inputFill: {
     position: "absolute",
-    top: 5,
+    top: 6,
     right: 0,
-    bottom: 5,
+    bottom: 6,
     left: 0,
-    borderRadius: 18,
+    borderRadius: 17,
     borderWidth: 1,
   },
-  inputStraightFrame: {
+  inputBendBulge: {
     position: "absolute",
-    top: 5,
-    right: 0,
-    bottom: 5,
-    left: 0,
     borderWidth: 1,
-    borderRadius: 18,
-  },
-  inputCurveFrame: {
-    position: "absolute",
-    left: 0,
-    right: 0,
-    height: 56,
-    borderLeftWidth: 1,
-    borderRightWidth: 1,
+    borderRadius: 999,
   },
-  inputSmileFrame: {
-    top: 1,
-    borderBottomWidth: 1,
-    borderBottomLeftRadius: 36,
-    borderBottomRightRadius: 36,
+  inputSmileBulge: {
+    borderTopWidth: 0,
+    borderLeftWidth: 0,
+    borderRightWidth: 0,
   },
-  inputFrownFrame: {
-    bottom: 1,
-    borderTopWidth: 1,
-    borderTopLeftRadius: 36,
-    borderTopRightRadius: 36,
+  inputFrownBulge: {
+    borderBottomWidth: 0,
+    borderLeftWidth: 0,
+    borderRightWidth: 0,
   },
   textInput: {
-    minHeight: 50,
+    minHeight: 48,
     paddingVertical: 0,
     fontSize: 14,
     lineHeight: 20,
