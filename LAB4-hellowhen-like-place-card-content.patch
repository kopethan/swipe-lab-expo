diff --git a/app/square-deck.tsx b/app/square-deck.tsx
index c68dfa4..8d97fab 100644
--- a/app/square-deck.tsx
+++ b/app/square-deck.tsx
@@ -1,59 +1,16 @@
 import React, { useCallback, useMemo, useState } from "react";
-import { Image } from "expo-image";
 import { Stack } from "expo-router";
 import { type LayoutChangeEvent, Platform, StyleSheet, Text, View } from "react-native";
 
-import { DeckProbeCard } from "@/components/deck-probe-card";
-import { ContinuousSquareStackDeck, type SquareStackDeckCard } from "@/features/square-stack-deck";
+import {
+  ContinuousSquareStackDeck,
+  HelloWhenPlaceCardDemo,
+  type HelloWhenPlaceDemoCard,
+  type SquareStackDeckCard,
+} from "@/features/square-stack-deck";
 import { useTheme } from "@/providers/ThemeProvider";
 
-type DemoSquareCard = SquareStackDeckCard & {
-  title: string;
-  location: string;
-  imageUrl: string;
-  tags: string[];
-};
-
-function DemoCard({ card, index, total }: { card: DemoSquareCard; index: number; total: number }) {
-  const { palette } = useTheme();
-
-  return (
-    <View style={styles.cardBody}>
-      <Image
-        source={{ uri: card.imageUrl }}
-        style={StyleSheet.absoluteFill}
-        contentFit="cover"
-        transition={0}
-        cachePolicy="memory-disk"
-      />
-      <View style={styles.scrim} />
-
-      <View style={styles.topRow}>
-        <Text style={styles.kicker}>{`SQUARE ${String(index + 1).padStart(2, "0")} / ${String(total).padStart(2, "0")}`}</Text>
-        <Text style={styles.cardId}>{card.id.toUpperCase()}</Text>
-      </View>
-
-      <View style={styles.bottomPanel}>
-        <Text style={styles.title} numberOfLines={2}>
-          {card.title}
-        </Text>
-        <Text style={styles.location} numberOfLines={1}>
-          {card.location}
-        </Text>
-        <View style={styles.tagsRow}>
-          {card.tags.map((tag) => (
-            <View key={`${card.id}-${tag}`} style={styles.tag}>
-              <Text style={styles.tagText}>{tag}</Text>
-            </View>
-          ))}
-        </View>
-        <View style={[styles.probeWrap, { borderColor: palette.mode === "light" ? "rgba(0,0,0,0.10)" : "rgba(255,255,255,0.14)" }]}>
-          <DeckProbeCard cardId={`square-${card.id}`} label="SQUARE" />
-        </View>
-      </View>
-    </View>
-  );
-}
+type DemoSquareCard = SquareStackDeckCard & HelloWhenPlaceDemoCard;
 
 export default function SquareDeckScreen() {
   const { palette } = useTheme();
@@ -74,28 +31,114 @@ export default function SquareDeckScreen() {
   }, []);
 
   const cards = useMemo<DemoSquareCard[]>(
-    () =>
-      Array.from({ length: 9 }, (_, index) => {
-        const id = `sq${String(index + 1).padStart(2, "0")}`;
-        const seed = `hello-when-square-${index + 1}`;
-        return {
-          id,
-          title: [
-            "Rooftop sunset walk",
-            "Hidden garden coffee",
-            "Late-night ramen corner",
-            "Canal-side book market",
-            "Tiny gallery opening",
-            "Morning bakery queue",
-            "Old town photo loop",
-            "Quiet jazz basement",
-            "Sunday park picnic",
-          ][index],
-          location: ["Paris", "Lyon", "Tokyo", "Amsterdam", "Berlin", "Lisbon", "Seoul", "New York", "Montreal"][index],
-          imageUrl: `https://picsum.photos/seed/${encodeURIComponent(seed)}/900/900`,
-          tags: ["stable-id", "diagonal", index % 2 === 0 ? "image" : "stack"],
-        };
-      }),
+    () => [
+      {
+        id: "sq01",
+        title: "Rooftop sunset walk",
+        area: "Paris · 11th arrondissement",
+        descriptor: "Golden-hour terrace, short stairs, open sky, and a calm route back toward the canal.",
+        mediaKind: "photo",
+        imageUrl: "https://images.unsplash.com/photo-1500530855697-b586d89ba3ee?auto=format&fit=crop&w=1200&q=82",
+        distanceLabel: "12 min walk",
+        moodLabel: "quiet",
+        tags: ["sunset", "walk", "view"],
+        accent: "#ff715b",
+      },
+      {
+        id: "sq02",
+        title: "Hidden garden coffee",
+        area: "Lyon · Croix-Rousse",
+        descriptor: "Small courtyard tables with plants, soft shade, and a good reset stop between plans.",
+        mediaKind: "photo",
+        imageUrl: "https://images.unsplash.com/photo-1494526585095-c41746248156?auto=format&fit=crop&w=1200&q=82",
+        distanceLabel: "650 m",
+        moodLabel: "slow",
+        tags: ["coffee", "garden", "nearby"],
+        accent: "#6bbf8a",
+      },
+      {
+        id: "sq03",
+        title: "Late-night ramen corner",
+        area: "Tokyo · Shinjuku",
+        descriptor: "A compact food stop near the station, useful when the plan runs later than expected.",
+        mediaKind: "static_map",
+        imageUrl: "https://images.unsplash.com/photo-1542051841857-5f90071e7989?auto=format&fit=crop&w=1200&q=82",
+        distanceLabel: "open late",
+        moodLabel: "busy",
+        tags: ["food", "night", "map"],
+        accent: "#ffbd4a",
+      },
+      {
+        id: "sq04",
+        title: "Canal-side book market",
+        area: "Amsterdam · Centrum",
+        descriptor: "Outdoor stalls, water reflections, and a good low-pressure browse before dinner.",
+        mediaKind: "photo",
+        imageUrl: "https://images.unsplash.com/photo-1512470876302-972faa2aa9a4?auto=format&fit=crop&w=1200&q=82",
+        distanceLabel: "2.1 km",
+        moodLabel: "browse",
+        tags: ["market", "books", "canal"],
+        accent: "#6aa8ff",
+      },
+      {
+        id: "sq05",
+        title: "Tiny gallery opening",
+        area: "Berlin · Mitte",
+        descriptor: "A small indoor place with predictable lighting, useful as a weather-safe card type.",
+        mediaKind: "fallback",
+        distanceLabel: "indoor",
+        moodLabel: "art",
+        tags: ["gallery", "backup", "calm"],
+        accent: "#b48cff",
+      },
+      {
+        id: "sq06",
+        title: "Morning bakery queue",
+        area: "Lisbon · Alfama",
+        descriptor: "A simple early stop with warm light, quick service, and nearby benches for a pause.",
+        mediaKind: "photo",
+        imageUrl: "https://images.unsplash.com/photo-1509440159596-0249088772ff?auto=format&fit=crop&w=1200&q=82",
+        distanceLabel: "8 min walk",
+        moodLabel: "morning",
+        tags: ["bakery", "breakfast", "easy"],
+        accent: "#f3a15f",
+      },
+      {
+        id: "sq07",
+        title: "Old town photo loop",
+        area: "Seoul · Ikseon-dong",
+        descriptor: "A short loop of narrow streets, textures, small shops, and easy alternate exits.",
+        mediaKind: "static_map",
+        imageUrl: "https://images.unsplash.com/photo-1538485399081-7191377e8241?auto=format&fit=crop&w=1200&q=82",
+        distanceLabel: "loop",
+        moodLabel: "photo",
+        tags: ["streets", "map", "loop"],
+        accent: "#44c7c1",
+      },
+      {
+        id: "sq08",
+        title: "Quiet jazz basement",
+        area: "New York · West Village",
+        descriptor: "Dim room, small tables, later timing, and a card that needs readable lower text.",
+        mediaKind: "photo",
+        imageUrl: "https://images.unsplash.com/photo-1514525253161-7a46d19cd819?auto=format&fit=crop&w=1200&q=82",
+        distanceLabel: "after 9 PM",
+        moodLabel: "music",
+        tags: ["jazz", "night", "indoors"],
+        accent: "#d76083",
+      },
+      {
+        id: "sq09",
+        title: "Sunday park picnic",
+        area: "Montreal · Plateau",
+        descriptor: "Fallback-friendly open area with simple text, no hard dependency on a perfect photo.",
+        mediaKind: "fallback",
+        distanceLabel: "weekend",
+        moodLabel: "open air",
+        tags: ["park", "picnic", "fallback"],
+        accent: "#7bc96f",
+      },
+    ],
     []
   );
 
@@ -104,8 +147,8 @@ export default function SquareDeckScreen() {
       <Stack.Screen options={{ title: "Square Deck Lab" }} />
 
       <View style={styles.header}>
-        <Text style={[styles.h1, { color: palette.text }]}>Square stack deck</Text>
-        <Text style={[styles.sub, { color: palette.muted }]}>Swipe up-left for next. Swipe down-right for previous.</Text>
+        <Text style={[styles.h1, { color: palette.text }]}>Place card stack lab</Text>
+        <Text style={[styles.sub, { color: palette.muted }]}>HelloWhen-like fake place cards using the clean diagonal square-deck model.</Text>
         <Text style={[styles.meta, { color: palette.muted }]}>Active: {activeLabel}</Text>
       </View>
 
@@ -115,12 +158,12 @@ export default function SquareDeckScreen() {
           availableWidth={stageBounds.width}
           availableHeight={stageBounds.height}
           onIndexChange={(_, card) => setActiveLabel(card.id)}
-          renderCard={({ card, index, total }) => <DemoCard card={card} index={index} total={total} />}
+          renderCard={({ card, index, total }) => <HelloWhenPlaceCardDemo card={card} index={index} total={total} />}
         />
       </View>
 
       <View style={styles.footer}>
-        <Text style={[styles.footerText, { color: palette.muted }]}>Model: one stable card key per item, visualOffset = cardIndex - activeIndex - progress.</Text>
+        <Text style={[styles.footerText, { color: palette.muted }]}>LAB4: place-like content only. Real blur and static OSM simulation stay for LAB5/LAB6.</Text>
       </View>
     </View>
   );
@@ -155,8 +198,8 @@ const styles = StyleSheet.create({
     flex: 1,
     alignItems: "center",
     justifyContent: "center",
-    paddingHorizontal: 18,
     overflow: "visible",
+    paddingHorizontal: 18,
   },
   footer: {
     paddingHorizontal: 18,
@@ -167,78 +210,4 @@ const styles = StyleSheet.create({
     fontSize: 12,
     lineHeight: 17,
   },
-  cardBody: {
-    flex: 1,
-    backgroundColor: "#111",
-  },
-  scrim: {
-    ...StyleSheet.absoluteFillObject,
-    backgroundColor: "rgba(0,0,0,0.22)",
-  },
-  topRow: {
-    position: "absolute",
-    top: 16,
-    left: 16,
-    right: 16,
-    flexDirection: "row",
-    alignItems: "center",
-    justifyContent: "space-between",
-    gap: 12,
-  },
-  kicker: {
-    color: "rgba(255,255,255,0.82)",
-    fontSize: 10,
-    fontWeight: "900",
-    letterSpacing: 1.1,
-  },
-  cardId: {
-    color: "#fff",
-    fontSize: 18,
-    fontWeight: "900",
-    letterSpacing: -0.4,
-  },
-  bottomPanel: {
-    position: "absolute",
-    left: 14,
-    right: 14,
-    bottom: 14,
-    borderRadius: 22,
-    padding: 14,
-    gap: 8,
-    backgroundColor: "rgba(0,0,0,0.42)",
-    borderWidth: StyleSheet.hairlineWidth,
-    borderColor: "rgba(255,255,255,0.22)",
-  },
-  title: {
-    color: "#fff",
-    fontSize: 25,
-    fontWeight: "900",
-    letterSpacing: -0.6,
-    lineHeight: 29,
-  },
-  location: {
-    color: "rgba(255,255,255,0.74)",
-    fontSize: 13,
-    fontWeight: "700",
-  },
-  tagsRow: {
-    flexDirection: "row",
-    flexWrap: "wrap",
-    gap: 6,
-  },
-  tag: {
-    borderRadius: 999,
-    paddingHorizontal: 9,
-    paddingVertical: 5,
-    backgroundColor: "rgba(255,255,255,0.16)",
-  },
-  tagText: {
-    color: "rgba(255,255,255,0.88)",
-    fontSize: 11,
-    fontWeight: "800",
-  },
-  probeWrap: {
-    borderTopWidth: StyleSheet.hairlineWidth,
-    paddingTop: 6,
-  },
 });
diff --git a/docs/SQUARE_STACK_DECK_LAB.md b/docs/SQUARE_STACK_DECK_LAB.md
index a1d70f2..c63a62e 100644
--- a/docs/SQUARE_STACK_DECK_LAB.md
+++ b/docs/SQUARE_STACK_DECK_LAB.md
@@ -74,3 +74,20 @@ The deck should size from the route's measured stage whenever possible, not only
 ## LAB3 four-card subtle depth rule
 
 The deck still mounts one previous card and one hidden forward tail card, but the idle visual stack should read as four cards only.
+
+
+## LAB4 HelloWhen-like fake PlaceCard content
+
+The square deck now uses fake place cards instead of probe/input cards. This keeps the clean continuous deck engine isolated while making the visual payload closer to HelloWhen mobile Places feed.
+
+The LAB4 card simulates:
+
+```txt
+- full-bleed photo/map/fallback media
+- top media/status chrome
+- lower readable information panel
+- title, area, descriptor, distance/mood pills, and tags
+- light/dark adaptive surfaces
+```
+
+This slice intentionally does not copy real HelloWhen feed code and does not add the expensive lower-third blur yet. LAB5 should bring in the image-card blur/fill behavior in a controlled way, and LAB6 should add a more explicit static OSM/map simulation.
diff --git a/features/square-stack-deck/components/HelloWhenPlaceCardDemo.tsx b/features/square-stack-deck/components/HelloWhenPlaceCardDemo.tsx
new file mode 100644
index 0000000..20b9a75
--- /dev/null
+++ b/features/square-stack-deck/components/HelloWhenPlaceCardDemo.tsx
@@ -0,0 +1,271 @@
+import React, { memo, useMemo } from "react";
+import { Image } from "expo-image";
+import { StyleSheet, Text, View } from "react-native";
+
+import { useTheme } from "@/providers/ThemeProvider";
+
+export type DemoPlaceMediaKind = "photo" | "static_map" | "fallback";
+
+export type HelloWhenPlaceDemoCard = {
+  id: string;
+  title: string;
+  area: string;
+  descriptor: string;
+  mediaKind: DemoPlaceMediaKind;
+  imageUrl?: string;
+  distanceLabel: string;
+  moodLabel: string;
+  tags: string[];
+  accent: string;
+};
+
+type Props = {
+  card: HelloWhenPlaceDemoCard;
+  index: number;
+  total: number;
+};
+
+function mediaLabel(kind: DemoPlaceMediaKind) {
+  switch (kind) {
+    case "photo":
+      return "Photo";
+    case "static_map":
+      return "Map view";
+    case "fallback":
+      return "Fallback";
+    default:
+      return "Media";
+  }
+}
+
+function HelloWhenPlaceCardDemoInner({ card, index, total }: Props) {
+  const { palette } = useTheme();
+  const isDark = palette.mode === "dark";
+  const mediaSurface = isDark ? "#0c1116" : "#dfe6dc";
+  const lowerPanelColor = isDark ? "rgba(7,9,12,0.68)" : "rgba(255,255,255,0.76)";
+  const lowerPanelBorder = isDark ? "rgba(255,255,255,0.14)" : "rgba(0,0,0,0.08)";
+  const titleColor = isDark ? "#fff" : "#101010";
+  const bodyColor = isDark ? "rgba(255,255,255,0.76)" : "rgba(0,0,0,0.62)";
+  const pillBg = isDark ? "rgba(255,255,255,0.13)" : "rgba(255,255,255,0.84)";
+  const pillText = isDark ? "rgba(255,255,255,0.86)" : "rgba(0,0,0,0.68)";
+
+  const fallbackLines = useMemo(() => Array.from({ length: 8 }, (_, lineIndex) => lineIndex), []);
+
+  return (
+    <View style={[styles.card, { backgroundColor: mediaSurface }]}>
+      {card.imageUrl ? (
+        <Image
+          source={{ uri: card.imageUrl }}
+          style={StyleSheet.absoluteFillObject}
+          contentFit="cover"
+          transition={0}
+          cachePolicy="memory-disk"
+        />
+      ) : (
+        <View style={[StyleSheet.absoluteFillObject, styles.fallbackMedia, { backgroundColor: mediaSurface }]}>
+          {fallbackLines.map((line) => (
+            <View
+              key={`fallback-line-${card.id}-${line}`}
+              style={[
+                styles.fallbackLine,
+                {
+                  top: 22 + line * 29,
+                  left: `${8 + ((line * 11) % 42)}%`,
+                  backgroundColor: isDark ? "rgba(255,255,255,0.07)" : "rgba(0,0,0,0.06)",
+                },
+              ]}
+            />
+          ))}
+          <View style={[styles.fallbackDot, { backgroundColor: card.accent }]} />
+        </View>
+      )}
+
+      <View style={styles.topShade} />
+      <View style={styles.bottomShade} />
+
+      <View style={styles.topChrome}>
+        <View style={[styles.statusPill, { backgroundColor: pillBg }]}>
+          <Text style={[styles.statusText, { color: pillText }]}>{mediaLabel(card.mediaKind)}</Text>
+        </View>
+        <Text style={styles.counter}>{`${String(index + 1).padStart(2, "0")} / ${String(total).padStart(2, "0")}`}</Text>
+      </View>
+
+      <View style={[styles.lowerPanel, { backgroundColor: lowerPanelColor, borderColor: lowerPanelBorder }]}>
+        <View style={styles.placeHeaderRow}>
+          <View style={[styles.placeAccent, { backgroundColor: card.accent }]} />
+          <View style={styles.placeTitleBlock}>
+            <Text style={[styles.title, { color: titleColor }]} numberOfLines={2}>
+              {card.title}
+            </Text>
+            <Text style={[styles.area, { color: bodyColor }]} numberOfLines={1}>
+              {card.area}
+            </Text>
+          </View>
+        </View>
+
+        <Text style={[styles.descriptor, { color: bodyColor }]} numberOfLines={2}>
+          {card.descriptor}
+        </Text>
+
+        <View style={styles.infoRow}>
+          <View style={[styles.infoPill, { backgroundColor: pillBg }]}>
+            <Text style={[styles.infoText, { color: pillText }]}>{card.distanceLabel}</Text>
+          </View>
+          <View style={[styles.infoPill, { backgroundColor: pillBg }]}>
+            <Text style={[styles.infoText, { color: pillText }]}>{card.moodLabel}</Text>
+          </View>
+        </View>
+
+        <View style={styles.tagsRow}>
+          {card.tags.slice(0, 3).map((tag) => (
+            <View key={`${card.id}-${tag}`} style={[styles.tag, { backgroundColor: isDark ? "rgba(255,255,255,0.10)" : "rgba(0,0,0,0.06)" }]}>
+              <Text style={[styles.tagText, { color: bodyColor }]}>{tag}</Text>
+            </View>
+          ))}
+        </View>
+      </View>
+    </View>
+  );
+}
+
+export const HelloWhenPlaceCardDemo = memo(HelloWhenPlaceCardDemoInner);
+
+const styles = StyleSheet.create({
+  card: {
+    flex: 1,
+    overflow: "hidden",
+  },
+  fallbackMedia: {
+    overflow: "hidden",
+  },
+  fallbackLine: {
+    position: "absolute",
+    width: "62%",
+    height: 18,
+    borderRadius: 999,
+    transform: [{ rotate: "-18deg" }],
+  },
+  fallbackDot: {
+    position: "absolute",
+    left: "50%",
+    top: "37%",
+    width: 52,
+    height: 52,
+    marginLeft: -26,
+    marginTop: -26,
+    borderRadius: 999,
+    opacity: 0.82,
+  },
+  topShade: {
+    position: "absolute",
+    left: 0,
+    right: 0,
+    top: 0,
+    height: "28%",
+    backgroundColor: "rgba(0,0,0,0.18)",
+  },
+  bottomShade: {
+    position: "absolute",
+    left: 0,
+    right: 0,
+    bottom: 0,
+    height: "54%",
+    backgroundColor: "rgba(0,0,0,0.22)",
+  },
+  topChrome: {
+    position: "absolute",
+    top: 14,
+    left: 14,
+    right: 14,
+    flexDirection: "row",
+    alignItems: "center",
+    justifyContent: "space-between",
+    gap: 12,
+  },
+  statusPill: {
+    borderRadius: 999,
+    paddingHorizontal: 10,
+    paddingVertical: 6,
+  },
+  statusText: {
+    fontSize: 11,
+    fontWeight: "900",
+    letterSpacing: 0.35,
+  },
+  counter: {
+    color: "rgba(255,255,255,0.88)",
+    fontSize: 11,
+    fontWeight: "900",
+    letterSpacing: 1.2,
+  },
+  lowerPanel: {
+    position: "absolute",
+    left: 12,
+    right: 12,
+    bottom: 12,
+    minHeight: "34%",
+    borderRadius: 24,
+    borderWidth: StyleSheet.hairlineWidth,
+    padding: 13,
+    gap: 8,
+  },
+  placeHeaderRow: {
+    flexDirection: "row",
+    alignItems: "flex-start",
+    gap: 10,
+  },
+  placeAccent: {
+    marginTop: 5,
+    width: 9,
+    height: 38,
+    borderRadius: 999,
+  },
+  placeTitleBlock: {
+    flex: 1,
+    minWidth: 0,
+  },
+  title: {
+    fontSize: 24,
+    fontWeight: "900",
+    lineHeight: 27,
+    letterSpacing: -0.7,
+  },
+  area: {
+    marginTop: 2,
+    fontSize: 13,
+    fontWeight: "800",
+  },
+  descriptor: {
+    fontSize: 12,
+    lineHeight: 16,
+    fontWeight: "650",
+  },
+  infoRow: {
+    flexDirection: "row",
+    gap: 7,
+  },
+  infoPill: {
+    borderRadius: 999,
+    paddingHorizontal: 9,
+    paddingVertical: 5,
+  },
+  infoText: {
+    fontSize: 11,
+    fontWeight: "850",
+  },
+  tagsRow: {
+    flexDirection: "row",
+    flexWrap: "wrap",
+    gap: 6,
+  },
+  tag: {
+    borderRadius: 999,
+    paddingHorizontal: 9,
+    paddingVertical: 5,
+  },
+  tagText: {
+    fontSize: 10,
+    fontWeight: "850",
+    letterSpacing: 0.2,
+  },
+});
diff --git a/features/square-stack-deck/index.ts b/features/square-stack-deck/index.ts
index 3b2d1cd..4abeb21 100644
--- a/features/square-stack-deck/index.ts
+++ b/features/square-stack-deck/index.ts
@@ -1,2 +1,3 @@
 export { ContinuousSquareStackDeck } from "./components/ContinuousSquareStackDeck";
 export type { SquareStackDeckCard, SquareStackDeckProps } from "./squareStackDeck.types";
+export { HelloWhenPlaceCardDemo, type HelloWhenPlaceDemoCard, type DemoPlaceMediaKind } from "./components/HelloWhenPlaceCardDemo";
