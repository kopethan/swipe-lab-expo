diff -ruN a/docs/SQUARE_STACK_DECK_LAB.md b/docs/SQUARE_STACK_DECK_LAB.md
--- a/docs/SQUARE_STACK_DECK_LAB.md	2026-05-02 13:06:51.000000000 +0000
+++ b/docs/SQUARE_STACK_DECK_LAB.md	2026-05-02 13:08:22.750477487 +0000
@@ -184,3 +184,12 @@
 - Increased the blur slice count and overlap so the blur grows more continuously from top to bottom.
 - Slightly expanded the atmosphere height again, with stronger protection on static-map cards.
 
+
+
+## LAB5d — Align square deck blur with Image Card design
+
+- Replaced the square-card custom local lower-atmosphere model with the same full-card sliced fade model used by `FullBleedBlurCard`.
+- The blur/tint now starts from a card-level percentage, not from a nested bottom container, so the transition matches the `/image-card` reference more closely.
+- Removed bridge bands, feather veils, and accent washes that were creating a visible frosted-panel feeling.
+- Kept static-map cards slightly stronger, starting around 46% of card height, while photo cards start around 50%.
+- The intended look is the Image Card rule: almost invisible in the middle, softly visible lower down, strongest near the bottom edge.
diff -ruN a/features/square-stack-deck/components/HelloWhenPlaceCardDemo.tsx b/features/square-stack-deck/components/HelloWhenPlaceCardDemo.tsx
--- a/features/square-stack-deck/components/HelloWhenPlaceCardDemo.tsx	2026-05-02 13:06:51.000000000 +0000
+++ b/features/square-stack-deck/components/HelloWhenPlaceCardDemo.tsx	2026-05-02 13:08:22.749743005 +0000
@@ -29,67 +29,50 @@
   total: number;
 };
 
-const BLUR_SLICE_COUNT = 28;
-const BRIDGE_BAND_COUNT = 4;
+const BLUR_SLICE_COUNT = 24;
 
 type LowerAtmosphereProps = {
   source?: { uri: string };
-  accent: string;
   isDark: boolean;
   mediaKind: DemoPlaceMediaKind;
 };
 
-function clamp(value: number, min: number, max: number) {
-  return Math.min(max, Math.max(min, value));
-}
-
-function LowerAtmosphere({ source, accent, isDark, mediaKind }: LowerAtmosphereProps) {
+function LowerAtmosphere({ source, isDark, mediaKind }: LowerAtmosphereProps) {
   const slices = useMemo(() => Array.from({ length: BLUR_SLICE_COUNT }, (_, index) => index), []);
-  const bridgeBands = useMemo(() => Array.from({ length: BRIDGE_BAND_COUNT }, (_, index) => index), []);
   const isStaticMap = mediaKind === "static_map";
-  const atmosphereHeight = isStaticMap ? 62 : 58;
-  const tintBase = isDark
-    ? isStaticMap
-      ? "rgba(3,5,8,0.78)"
-      : "rgba(4,6,9,0.68)"
-    : isStaticMap
-      ? "rgba(255,255,255,0.80)"
-      : "rgba(255,255,255,0.58)";
-  const bottomVeil = isDark
-    ? isStaticMap
-      ? "rgba(2,4,7,0.84)"
-      : "rgba(2,4,7,0.68)"
-    : isStaticMap
-      ? "rgba(255,255,255,0.76)"
-      : "rgba(255,255,255,0.50)";
-  const midVeil = isDark
+  const blurStartPercent = isStaticMap ? 46 : 50;
+  const safeStart = Math.max(0, Math.min(92, blurStartPercent));
+  const overlayHeight = 100 - safeStart;
+  const sliceHeight = overlayHeight / BLUR_SLICE_COUNT;
+  const maxBlurRadius = isStaticMap ? 32 : 28;
+  const bottomTint = isDark
     ? isStaticMap
-      ? "rgba(4,7,12,0.22)"
-      : "rgba(4,7,12,0.14)"
+      ? "rgba(5,7,10,0.52)"
+      : "rgba(8,10,12,0.46)"
     : isStaticMap
-      ? "rgba(255,255,255,0.14)"
-      : "rgba(255,255,255,0.08)";
+      ? "rgba(255,255,255,0.66)"
+      : "rgba(255,255,255,0.46)";
+  const tintStrength = isStaticMap ? 0.92 : 0.82;
 
   return (
-    <View pointerEvents="none" style={[styles.lowerAtmosphere, { height: `${atmosphereHeight}%` }]}> 
+    <View pointerEvents="none" style={StyleSheet.absoluteFillObject}>
       {source ? (
-        <>
-          {bridgeBands.map((band) => {
-            const progress = band / Math.max(1, BRIDGE_BAND_COUNT - 1);
-            const top = 6 + progress * 26;
-            const height = 22 + progress * 10;
-            const opacity = 0.022 + progress * (isStaticMap ? 0.05 : 0.04);
-            const blurRadius = 6 + progress * (isStaticMap ? 9 : 7);
+        slices.map((slice) => {
+          const progress = (slice + 1) / BLUR_SLICE_COUNT;
+          const eased = progress * progress * (3 - 2 * progress);
+          const top = safeStart + slice * sliceHeight;
+          const blurOpacity = eased * 0.9;
+          const tintOpacity = eased * tintStrength;
 
-            return (
+          return (
+            <React.Fragment key={`blur-slice-${slice}`}>
               <View
-                key={`atmosphere-bridge-${band}`}
                 style={[
                   styles.atmosphereSlice,
                   {
                     top: `${top}%`,
-                    height: `${height}%`,
-                    opacity,
+                    height: `${sliceHeight}%`,
+                    opacity: blurOpacity,
                   },
                 ]}
               >
@@ -97,80 +80,32 @@
                   source={source}
                   contentFit="cover"
                   transition={0}
-                  blurRadius={blurRadius}
+                  blurRadius={6 + eased * (maxBlurRadius - 6)}
                   style={StyleSheet.absoluteFillObject}
                 />
               </View>
-            );
-          })}
-
-          {slices.map((slice) => {
-            const progress = (slice + 1) / BLUR_SLICE_COUNT;
-            const eased = progress * progress * (3 - 2 * progress);
-            const tapered = clamp((eased - 0.1) / 0.9, 0, 1);
-            const lift = tapered * tapered * (3 - 2 * tapered);
-            const sliceHeight = 100 / BLUR_SLICE_COUNT;
-            const top = slice * sliceHeight - 2.4;
-            const height = sliceHeight + 6.5;
-            const blurOpacity = Math.pow(lift, 1.65) * (isStaticMap ? 0.92 : 0.84);
-            const tintOpacity = Math.pow(lift, 1.85) * (isStaticMap ? 0.72 : 0.60);
-
-            return (
-              <React.Fragment key={`atmosphere-slice-${slice}`}>
-                <View
-                  style={[
-                    styles.atmosphereSlice,
-                    {
-                      top: `${top}%`,
-                      height: `${height}%`,
-                      opacity: blurOpacity,
-                    },
-                  ]}
-                >
-                  <Image
-                    source={source}
-                    contentFit="cover"
-                    transition={0}
-                    blurRadius={6 + lift * (isStaticMap ? 30 : 24)}
-                    style={StyleSheet.absoluteFillObject}
-                  />
-                </View>
-                <View
-                  style={[
-                    styles.atmosphereSlice,
-                    {
-                      top: `${top}%`,
-                      height: `${height}%`,
-                      backgroundColor: tintBase,
-                      opacity: tintOpacity,
-                    },
-                  ]}
-                />
-              </React.Fragment>
-            );
-          })}
-        </>
+              <View
+                style={[
+                  styles.atmosphereSlice,
+                  {
+                    top: `${top}%`,
+                    height: `${sliceHeight}%`,
+                    backgroundColor: bottomTint,
+                    opacity: tintOpacity,
+                  },
+                ]}
+              />
+            </React.Fragment>
+          );
+        })
       ) : (
         <View
           style={[
             StyleSheet.absoluteFillObject,
-            { backgroundColor: isDark ? "rgba(3,5,8,0.48)" : "rgba(255,255,255,0.42)" },
+            { backgroundColor: isDark ? "rgba(3,5,8,0.42)" : "rgba(255,255,255,0.40)" },
           ]}
         />
       )}
-
-      <View style={[styles.atmosphereFeather, styles.atmosphereFeatherTop, { backgroundColor: midVeil }]} />
-      <View style={[styles.atmosphereFeather, styles.atmosphereFeatherMid, { backgroundColor: midVeil }]} />
-      <View style={[styles.atmosphereFeather, styles.atmosphereFeatherBottom, { backgroundColor: bottomVeil }]} />
-      <View
-        style={[
-          styles.atmosphereAccentWash,
-          {
-            backgroundColor: accent,
-            opacity: isStaticMap ? 0.05 : 0.04,
-          },
-        ]}
-      />
     </View>
   );
 }
@@ -266,7 +201,7 @@
       )}
 
       <View style={styles.topShade} />
-      <LowerAtmosphere source={mediaSource} accent={card.accent} isDark={isDark} mediaKind={card.mediaKind} />
+      <LowerAtmosphere source={mediaSource} isDark={isDark} mediaKind={card.mediaKind} />
 
       <View style={styles.contentLayer}>
         <View style={styles.topBar}>
@@ -397,53 +332,18 @@
     height: "26%",
     backgroundColor: "rgba(0,0,0,0.10)",
   },
-  lowerAtmosphere: {
-    position: "absolute",
-    left: 0,
-    right: 0,
-    bottom: 0,
-    overflow: "hidden",
-  },
   atmosphereSlice: {
     position: "absolute",
     left: 0,
     right: 0,
     overflow: "hidden",
   },
-  atmosphereFeather: {
-    position: "absolute",
-    left: 0,
-    right: 0,
-  },
-  atmosphereFeatherTop: {
-    top: 0,
-    height: "30%",
-    opacity: 0.06,
-  },
-  atmosphereFeatherMid: {
-    top: "22%",
-    height: "34%",
-    opacity: 0.10,
-  },
-  atmosphereFeatherBottom: {
-    bottom: 0,
-    height: "64%",
-    opacity: 0.76,
-  },
-  atmosphereAccentWash: {
-    position: "absolute",
-    left: "10%",
-    right: "10%",
-    bottom: "3%",
-    height: "56%",
-    borderRadius: 999,
-  },
   contentLayer: {
     ...StyleSheet.absoluteFillObject,
     justifyContent: "space-between",
     paddingHorizontal: 14,
     paddingTop: 14,
-    paddingBottom: 18,
+    paddingBottom: 16,
   },
   topBar: {
     flexDirection: "row",
@@ -468,7 +368,7 @@
   },
   bottomOverlay: {
     alignSelf: "stretch",
-    gap: 8,
+    gap: 7,
     paddingRight: 8,
   },
   bottomOverlayMap: {
