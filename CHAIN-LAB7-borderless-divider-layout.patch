diff --git a/features/chain-lab/ChainLabScreen.tsx b/features/chain-lab/ChainLabScreen.tsx
index b542e94..cdf1465 100644
--- a/features/chain-lab/ChainLabScreen.tsx
+++ b/features/chain-lab/ChainLabScreen.tsx
@@ -581,6 +581,21 @@ function InfoChip({ label }: { label: string }) {
   );
 }
 
+function SectionDivider({ inset = false }: { inset?: boolean }) {
+  const { palette } = useTheme();
+
+  return (
+    <View
+      pointerEvents="none"
+      style={[
+        styles.sectionDivider,
+        inset ? styles.sectionDividerInset : null,
+        { backgroundColor: palette.border },
+      ]}
+    />
+  );
+}
+
 function FlowNode({
   participant,
   selected,
@@ -622,7 +637,7 @@ function RoleFlow({
   const { palette } = useTheme();
 
   return (
-    <View style={[styles.roleFlow, { borderColor: palette.border, backgroundColor: palette.surfaceAlt }]}>
+    <View style={styles.roleFlow}>
       <Text style={[styles.roleFlowLabel, { color: palette.muted }]}>CHAIN PATH</Text>
       <View style={styles.roleFlowRow}>
         <FlowNode participant={receivesFrom} />
@@ -651,9 +666,14 @@ function RoleExchangeBlock({
   const { palette } = useTheme();
 
   return (
-    <View style={[styles.exchangeBlock, { borderColor: palette.border, backgroundColor: palette.surfaceAlt }]}>
-      <View style={[styles.exchangeIcon, { backgroundColor: palette.text }]}>
-        <Text style={[styles.exchangeIconText, { color: palette.background }]}>{icon}</Text>
+    <View style={styles.exchangeBlock}>
+      <View
+        style={[
+          styles.exchangeIcon,
+          { borderColor: palette.border, backgroundColor: palette.surfaceAlt },
+        ]}
+      >
+        <Text style={[styles.exchangeIconText, { color: palette.text }]}>{icon}</Text>
       </View>
       <View style={styles.exchangeCopy}>
         <Text style={[styles.exchangeLabel, { color: palette.muted }]}>{kind}</Text>
@@ -670,7 +690,7 @@ function MatchFit({ participant }: { participant: ChainParticipant }) {
   const { palette } = useTheme();
 
   return (
-    <View style={[styles.matchFitCard, { borderColor: palette.border, backgroundColor: palette.surface }]}>
+    <View style={styles.matchFitCard}>
       <View style={styles.matchFitHeader}>
         <Text style={[styles.matchFitTitle, { color: palette.text }]}>Match fit</Text>
         <Text style={[styles.matchFitCaption, { color: palette.muted }]}>Why this role may work</Text>
@@ -691,7 +711,7 @@ function SelectedParticipantCard({ participant }: { participant: ChainParticipan
   const isCurrentUser = participant.id === chainLabScenario.currentUserId;
 
   return (
-    <View style={[styles.detailCard, { borderColor: palette.border, backgroundColor: palette.surface }]}>
+    <View style={styles.detailCard}>
       <View style={styles.detailHeaderRow}>
         <View style={[styles.miniAvatar, { borderColor: palette.border, backgroundColor: palette.surfaceAlt }]}>
           <Text style={[styles.miniAvatarText, { color: palette.text }]}>{participant.initials}</Text>
@@ -705,8 +725,12 @@ function SelectedParticipantCard({ participant }: { participant: ChainParticipan
         </View>
       </View>
 
+      <SectionDivider />
+
       <RoleFlow participant={participant} receivesFrom={receivesFrom} givesTo={givesTo} />
 
+      <SectionDivider />
+
       <View style={styles.exchangeStack}>
         <RoleExchangeBlock
           kind="Receives"
@@ -715,6 +739,7 @@ function SelectedParticipantCard({ participant }: { participant: ChainParticipan
           peerLabel="from"
           peerName={getDisplayName(receivesFrom)}
         />
+        <SectionDivider inset />
         <RoleExchangeBlock
           kind="Gives"
           icon="↑"
@@ -724,6 +749,8 @@ function SelectedParticipantCard({ participant }: { participant: ChainParticipan
         />
       </View>
 
+      <SectionDivider />
+
       <MatchFit participant={participant} />
     </View>
   );
@@ -843,7 +870,7 @@ function ChainStateCard({
   const { palette } = useTheme();
 
   return (
-    <View style={[styles.stateCard, { borderColor: palette.border, backgroundColor: palette.surface }]}>
+    <View style={styles.stateCard}>
       <View style={styles.stateHeaderRow}>
         <View style={styles.stateHeaderCopy}>
           <Text style={[styles.stateEyebrow, { color: palette.muted }]}>{stageConfig.label}</Text>
@@ -856,6 +883,8 @@ function ChainStateCard({
 
       <Text style={[styles.stateDescription, { color: palette.muted }]}>{stageConfig.description}</Text>
 
+      <SectionDivider />
+
       <View style={styles.participantStatusRow}>
         {chainLabScenario.order.map((participantId) => {
           const participant = getParticipant(participantId);
@@ -974,21 +1003,8 @@ export function ChainLabScreen() {
       >
         {optionsOpen ? <LabOptionsPanel /> : null}
 
-        <View
-          style={[
-            styles.labPanel,
-            isDesktop ? styles.labPanelDesktop : null,
-            { borderColor: palette.border, backgroundColor: palette.surface },
-          ]}
-        >
+        <View style={[styles.labPanel, isDesktop ? styles.labPanelDesktop : null]}>
           <View style={[styles.mapPane, isDesktop ? styles.mapPaneDesktop : null]}>
-            <View
-              pointerEvents="none"
-              style={[
-                styles.mapPaneBackdrop,
-                { borderColor: palette.border, backgroundColor: palette.surface },
-              ]}
-            />
             <View pointerEvents="none" style={[styles.mapPaneGlow, { backgroundColor: palette.surfaceAlt }]} />
             <ChainCircle
               selectedId={selectedId}
@@ -1001,7 +1017,9 @@ export function ChainLabScreen() {
 
           <View style={[styles.detailsPane, isDesktop ? styles.detailsPaneDesktop : null]}>
             <ChainStateCard stageConfig={stageConfig} confirmedIds={confirmedIds} />
+            <SectionDivider />
             <SelectedParticipantCard participant={selectedParticipant} />
+            <SectionDivider />
             <ActionFooter
               stage={chainStage}
               onPrimaryPress={handlePrimaryAction}
@@ -1179,35 +1197,20 @@ const styles = StyleSheet.create({
     lineHeight: 20,
   },
   labPanel: {
-    borderRadius: 32,
-    borderWidth: 1,
-    gap: 16,
-    overflow: "hidden",
-    padding: 14,
-    shadowColor: "#000",
-    shadowOpacity: 0.08,
-    shadowRadius: 24,
-    shadowOffset: { height: 18, width: 0 },
+    gap: 18,
   },
   labPanelDesktop: {
     alignItems: "stretch",
     flexDirection: "row",
-    gap: 20,
-    padding: 20,
+    gap: 28,
   },
   mapPane: {
     alignItems: "center",
     justifyContent: "center",
     minHeight: 348,
-    overflow: "hidden",
+    overflow: "visible",
     position: "relative",
   },
-  mapPaneBackdrop: {
-    ...StyleSheet.absoluteFillObject,
-    borderRadius: 28,
-    borderWidth: 1,
-    opacity: 0.72,
-  },
   mapPaneGlow: {
     borderRadius: 180,
     height: 360,
@@ -1221,7 +1224,7 @@ const styles = StyleSheet.create({
     minHeight: 440,
   },
   detailsPane: {
-    gap: 12,
+    gap: 16,
   },
   detailsPaneDesktop: {
     flex: 1,
@@ -1402,10 +1405,7 @@ const styles = StyleSheet.create({
     lineHeight: 13,
   },
   detailCard: {
-    borderRadius: 26,
-    borderWidth: 1,
-    gap: 14,
-    padding: 16,
+    gap: 16,
   },
   detailHeaderRow: {
     alignItems: "center",
@@ -1446,11 +1446,8 @@ const styles = StyleSheet.create({
     fontWeight: "900",
   },
   roleFlow: {
-    borderRadius: 18,
-    borderWidth: 1,
     gap: 10,
-    paddingHorizontal: 12,
-    paddingVertical: 12,
+    paddingVertical: 2,
   },
   roleFlowLabel: {
     fontSize: 10,
@@ -1485,15 +1482,14 @@ const styles = StyleSheet.create({
   },
   exchangeBlock: {
     alignItems: "flex-start",
-    borderRadius: 20,
-    borderWidth: 1,
     flexDirection: "row",
     gap: 12,
-    padding: 14,
+    paddingVertical: 2,
   },
   exchangeIcon: {
     alignItems: "center",
     borderRadius: 18,
+    borderWidth: 1,
     height: 36,
     justifyContent: "center",
     width: 36,
@@ -1529,10 +1525,7 @@ const styles = StyleSheet.create({
     fontWeight: "900",
   },
   matchFitCard: {
-    borderRadius: 18,
-    borderWidth: 1,
     gap: 10,
-    padding: 12,
   },
   matchFitHeader: {
     gap: 2,
@@ -1562,10 +1555,7 @@ const styles = StyleSheet.create({
     fontWeight: "900",
   },
   stateCard: {
-    borderRadius: 24,
-    borderWidth: 1,
-    gap: 12,
-    padding: 15,
+    gap: 13,
   },
   stateHeaderRow: {
     alignItems: "flex-start",
@@ -1628,6 +1618,15 @@ const styles = StyleSheet.create({
     fontWeight: "900",
     lineHeight: 14,
   },
+  sectionDivider: {
+    height: StyleSheet.hairlineWidth,
+    opacity: 0.9,
+    width: "100%",
+  },
+  sectionDividerInset: {
+    marginLeft: 48,
+    width: "auto",
+  },
   actionStack: {
     gap: 8,
   },
