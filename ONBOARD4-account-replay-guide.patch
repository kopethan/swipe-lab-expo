diff --git a/apps/mobile/src/features/account/AccountScreen.tsx b/apps/mobile/src/features/account/AccountScreen.tsx
index 13abe50..5617a70 100644
--- a/apps/mobile/src/features/account/AccountScreen.tsx
+++ b/apps/mobile/src/features/account/AccountScreen.tsx
@@ -20,7 +20,7 @@ import { DetailInfoList, DetailSection } from '../../components/detail';
 import { resolveMediaUrl } from '../trade/mediaUrls';
 
 type WalletResponse = { wallet: (WalletDto & { entries?: LedgerEntryDto[] }) | null };
-type AccountRoute = 'AccountProfile' | 'Notifications' | 'ProPlans' | 'BusinessAccounts' | 'Wallet' | 'Payouts' | 'Settings' | 'LegalPolicy' | 'SupportCenter' | 'AccountDeletion' | 'BuyCredits';
+type AccountRoute = 'AccountProfile' | 'Notifications' | 'OnboardingGuide' | 'ProPlans' | 'BusinessAccounts' | 'Wallet' | 'Payouts' | 'Settings' | 'LegalPolicy' | 'SupportCenter' | 'AccountDeletion' | 'BuyCredits';
 type AccountGroupKey = 'activity' | 'settings' | 'future';
 
 type AccountAction = {
@@ -37,6 +37,7 @@ const accountActions: AccountAction[] = [
   { titleKey: 'account.items.profile.title', descriptionKey: 'account.items.profile.bodyNative', badgeKey: 'account.items.profile.badge', tone: 'info', route: 'AccountProfile', icon: 'profile', group: 'activity' },
   { titleKey: 'account.items.notifications.title', descriptionKey: 'account.items.notifications.bodyNative', badgeKey: 'account.items.notifications.badge', tone: 'proposal', route: 'Notifications', icon: 'bell', group: 'activity' },
   { titleKey: 'account.items.support.title', descriptionKey: 'account.items.support.bodyNative', badgeKey: 'account.items.support.badge', tone: 'success', route: 'SupportCenter', icon: 'help', group: 'activity' },
+  ...(betaFeatures.onboardingEnabled ? [{ titleKey: 'account.items.guide.title', descriptionKey: 'account.items.guide.bodyNative', badgeKey: 'account.items.guide.badge', tone: 'instruction' as SemanticColorName, route: 'OnboardingGuide' as AccountRoute, icon: 'help' as MobileIconName, group: 'activity' as AccountGroupKey }] : []),
   { titleKey: 'account.items.settings.title', descriptionKey: 'account.items.settings.bodyNative', badgeKey: 'account.items.settings.badge', tone: 'instruction', route: 'Settings', icon: 'settings', group: 'settings' },
   { titleKey: 'account.items.legal.title', descriptionKey: 'account.items.legal.bodyNative', badgeKey: 'account.items.legal.badge', tone: 'warning', route: 'LegalPolicy', icon: 'warning', group: 'settings' },
   { titleKey: 'account.items.delete.title', descriptionKey: 'account.items.delete.bodyNative', badgeKey: 'account.items.delete.badge', tone: 'warning', route: 'AccountDeletion', icon: 'warning', group: 'settings' },
@@ -135,6 +136,7 @@ export function AccountScreen() {
   function navigate(route: AccountRoute) {
     if (route === 'AccountProfile') navigation.navigate('AccountProfile');
     else if (route === 'Notifications') navigation.navigate('Notifications');
+    else if (route === 'OnboardingGuide') navigation.navigate('OnboardingGuide');
     else if (route === 'ProPlans') navigation.navigate('ProPlans');
     else if (route === 'BusinessAccounts') navigation.navigate('BusinessAccounts');
     else if (route === 'Wallet') navigation.navigate('Wallet');
diff --git a/apps/mobile/src/features/onboarding/OnboardingCarousel.tsx b/apps/mobile/src/features/onboarding/OnboardingCarousel.tsx
index 8b32d3a..615aea4 100644
--- a/apps/mobile/src/features/onboarding/OnboardingCarousel.tsx
+++ b/apps/mobile/src/features/onboarding/OnboardingCarousel.tsx
@@ -9,9 +9,10 @@ import { OnboardingIllustration } from './OnboardingIllustrations';
 
 type OnboardingCarouselProps = {
   onClose: () => void;
+  markSeenOnClose?: boolean;
 };
 
-export function OnboardingCarousel({ onClose }: OnboardingCarouselProps) {
+export function OnboardingCarousel({ onClose, markSeenOnClose = true }: OnboardingCarouselProps) {
   const theme = useThemeTokens();
   const { t } = useTranslation();
   const insets = useSafeAreaInsets();
@@ -27,7 +28,7 @@ export function OnboardingCarousel({ onClose }: OnboardingCarouselProps) {
   const maxContentWidth = Math.min(width - 32, 520);
 
   async function close() {
-    await markMobileOnboardingSeen();
+    if (markSeenOnClose) await markMobileOnboardingSeen();
     onClose();
   }
 
diff --git a/apps/mobile/src/features/onboarding/OnboardingGuideScreen.tsx b/apps/mobile/src/features/onboarding/OnboardingGuideScreen.tsx
new file mode 100644
index 0000000..baec849
--- /dev/null
+++ b/apps/mobile/src/features/onboarding/OnboardingGuideScreen.tsx
@@ -0,0 +1,20 @@
+import React from 'react';
+import { useNavigation } from '@react-navigation/native';
+import type { NativeStackNavigationProp } from '@react-navigation/native-stack';
+import type { RootStackParamList } from '../../navigation/RootNavigator';
+import { OnboardingCarousel } from './OnboardingCarousel';
+
+export function OnboardingGuideScreen() {
+  const navigation = useNavigation<NativeStackNavigationProp<RootStackParamList>>();
+
+  function close() {
+    if (navigation.canGoBack()) {
+      navigation.goBack();
+      return;
+    }
+
+    navigation.navigate('TradeTabs');
+  }
+
+  return <OnboardingCarousel markSeenOnClose={false} onClose={close} />;
+}
diff --git a/apps/mobile/src/navigation/RootNavigator.tsx b/apps/mobile/src/navigation/RootNavigator.tsx
index c7ba029..dd46758 100644
--- a/apps/mobile/src/navigation/RootNavigator.tsx
+++ b/apps/mobile/src/navigation/RootNavigator.tsx
@@ -46,6 +46,7 @@ import { AppScreen } from '../components/AppScreen';
 import { AppText } from '../components/AppText';
 import { useTranslation } from '../providers/MobileI18nProvider';
 import { OnboardingGate } from '../features/onboarding/OnboardingGate';
+import { OnboardingGuideScreen } from '../features/onboarding/OnboardingGuideScreen';
 import type { LegalPolicyKey } from '@hellowhen/i18n';
 
 export type RootStackParamList = {
@@ -77,6 +78,7 @@ export type RootStackParamList = {
   TradeDetail: { tradeId: string; title?: string; description?: string; amountCents?: number; currency?: string; creditAmount?: number; status?: string; expiresAt?: string | null; selectedProposalSide?: TradeCreateSideSelection; selectedProposalNeedId?: string; selectedProposalOfferId?: string };
   UserProfile: { userId: string; displayName?: string };
   Login: undefined;
+  OnboardingGuide: undefined;
 };
 
 type MainTabParamList = { Trades: undefined; Needs: undefined; Offers: undefined; Account: undefined };
@@ -214,6 +216,7 @@ export function RootNavigator() {
         <Stack.Screen name="UserProfile" component={PublicUserProfileScreen} />
         <Stack.Screen name="Login" component={LoginScreen} />
         <Stack.Screen name="LegalPolicy" component={LegalPolicyScreen} />
+        {betaFeatures.onboardingEnabled ? <Stack.Screen name="OnboardingGuide" component={OnboardingGuideScreen} options={{ presentation: 'fullScreenModal' }} /> : null}
         <Stack.Screen name="AccountProfile" component={ProtectedProfileScreen} />
         <Stack.Screen name="Notifications" component={ProtectedNotificationsScreen} />
         {betaFeatures.plusSubscriptionFeatures.plusPublic ? <Stack.Screen name="ProPlans" component={ProtectedPlanSelectionScreen} /> : null}
diff --git a/apps/web/src/app/globals.css b/apps/web/src/app/globals.css
index 9f0c377..f0d1197 100644
--- a/apps/web/src/app/globals.css
+++ b/apps/web/src/app/globals.css
@@ -155,6 +155,7 @@ h3 { font-size: 17px; line-height: 1.2; letter-spacing: -0.02em; margin-bottom:
 .mobile-card p:last-child, .card p:last-child { margin-bottom: 0; }
 .app-loading-logo { display: block; width: 64px; height: 64px; margin-bottom: 10px; border: 1px solid var(--app-border); border-radius: 18px; background: #FFFFFF; object-fit: contain; }
 .mobile-link-card { display: flex; align-items: center; justify-content: space-between; gap: 12px; border: 1px solid color-mix(in srgb, var(--app-border) 88%, transparent); border-radius: 24px; background: var(--app-surface); padding: 16px; box-shadow: var(--app-card-shadow); }
+button.mobile-link-card { width: 100%; color: inherit; font: inherit; text-align: left; cursor: pointer; }
 .mobile-link-card span { color: var(--app-text-muted); font-weight: 800; }
 .mobile-link-card__body { min-width: 0; flex: 1 1 auto; }
 .mobile-link-card__icon { color: var(--app-text); }
diff --git a/apps/web/src/features/account/AccountHubClient.tsx b/apps/web/src/features/account/AccountHubClient.tsx
index 6ee187e..6e5c33b 100644
--- a/apps/web/src/features/account/AccountHubClient.tsx
+++ b/apps/web/src/features/account/AccountHubClient.tsx
@@ -5,6 +5,7 @@ import { useEffect, useMemo, useState } from 'react';
 import type { PayoutSummaryDto, WalletDto } from '@hellowhen/contracts';
 import { WebIcon, type WebIconName } from '../../components/WebIcon';
 import { api } from '../../lib/api';
+import { OnboardingCarousel } from '../onboarding/OnboardingCarousel';
 import { betaFeatures } from '../../lib/betaFeatures';
 import { useWebAuth } from '../../providers/WebAuthProvider';
 import { useWebTranslation } from '../../providers/WebI18nProvider';
@@ -24,6 +25,7 @@ export function AccountHubClient() {
   const [wallet, setWallet] = useState<WalletDto | null>(null);
   const [summary, setSummary] = useState<PayoutSummaryDto | null>(null);
   const [notificationUnreadCount, setNotificationUnreadCount] = useState(0);
+  const [showGuide, setShowGuide] = useState(false);
 
   const accountItems = useMemo<AccountHubItem[]>(() => [
     { href: '/account/profile', titleKey: 'account.items.profile.title', bodyKey: 'account.items.profile.body', icon: 'profile' },
@@ -115,6 +117,19 @@ export function AccountHubClient() {
         </section>
       ) : null}
 
+
+      {betaFeatures.onboardingEnabled ? (
+        <button type="button" className="mobile-link-card" onClick={() => setShowGuide(true)}>
+          <WebIcon name="help" size={22} decorative className="mobile-link-card__icon" />
+          <span className="mobile-link-card__body">
+            <strong>{t('account.items.guide.title')}</strong>
+            <br />
+            {t('account.items.guide.body')}
+          </span>
+          <WebIcon name="arrow-right" size={17} decorative className="mobile-link-card__arrow" />
+        </button>
+      ) : null}
+
       <div className="mobile-list">
         {accountItems.map((item) => {
           const href = auth.hydrated && !auth.isAuthenticated && !item.publicAccess ? `/auth?next=${encodeURIComponent(item.href)}` : item.href;
@@ -134,6 +149,7 @@ export function AccountHubClient() {
           );
         })}
       </div>
+      {showGuide ? <OnboardingCarousel markSeenOnClose={false} onClose={() => setShowGuide(false)} /> : null}
     </div>
   );
 }
diff --git a/apps/web/src/features/onboarding/OnboardingCarousel.tsx b/apps/web/src/features/onboarding/OnboardingCarousel.tsx
index 66c2869..5bbc57d 100644
--- a/apps/web/src/features/onboarding/OnboardingCarousel.tsx
+++ b/apps/web/src/features/onboarding/OnboardingCarousel.tsx
@@ -8,9 +8,10 @@ import { OnboardingIllustration } from './OnboardingIllustrations';
 
 type OnboardingCarouselProps = {
   onClose: () => void;
+  markSeenOnClose?: boolean;
 };
 
-export function OnboardingCarousel({ onClose }: OnboardingCarouselProps) {
+export function OnboardingCarousel({ onClose, markSeenOnClose = true }: OnboardingCarouselProps) {
   const { t } = useWebTranslation();
   const [index, setIndex] = useState(0);
   const titleRef = useRef<HTMLHeadingElement | null>(null);
@@ -44,7 +45,7 @@ export function OnboardingCarousel({ onClose }: OnboardingCarouselProps) {
   });
 
   function close() {
-    markWebOnboardingSeen();
+    if (markSeenOnClose) markWebOnboardingSeen();
     onClose();
   }
 
diff --git a/packages/i18n/src/locales/en/account.ts b/packages/i18n/src/locales/en/account.ts
index e6ec74a..54e9b06 100644
--- a/packages/i18n/src/locales/en/account.ts
+++ b/packages/i18n/src/locales/en/account.ts
@@ -431,6 +431,12 @@ export const account = {
       bodyNative: 'Proposal decisions, private replies, and trade updates.',
       badge: 'Inbox',
     },
+    guide: {
+      title: 'User Guide',
+      body: 'Replay the first-run guide for Needs, Offers, Trades, proposals, safety, and Account.',
+      bodyNative: 'Replay the first-run guide anytime.',
+      badge: 'Guide',
+    },
     settings: {
       title: 'Settings',
       body: 'Appearance, language, dark mode, and notification preferences.',
diff --git a/packages/i18n/src/locales/fr/account.ts b/packages/i18n/src/locales/fr/account.ts
index eb0c9a8..7a80264 100644
--- a/packages/i18n/src/locales/fr/account.ts
+++ b/packages/i18n/src/locales/fr/account.ts
@@ -431,6 +431,12 @@ export const account = {
       bodyNative: 'Décisions de proposition, réponses privées et mises à jour.',
       badge: 'Boîte',
     },
+    guide: {
+      title: 'Guide utilisateur',
+      body: 'Revoir le guide de démarrage pour les besoins, offres, échanges, propositions, sécurité et compte.',
+      bodyNative: 'Revoir le guide de démarrage à tout moment.',
+      badge: 'Guide',
+    },
     settings: {
       title: 'Paramètres',
       body: 'Apparence, langue, mode sombre et préférences de notification.',
