diff --git a/features/auth-motion-lab/AuthMotionLabScreen.tsx b/features/auth-motion-lab/AuthMotionLabScreen.tsx
index 3cd8f3b..aa3b67d 100644
--- a/features/auth-motion-lab/AuthMotionLabScreen.tsx
+++ b/features/auth-motion-lab/AuthMotionLabScreen.tsx
@@ -449,14 +449,14 @@ function getMoodColor(
   }
 
   if (state === "smile") {
-    return "#35E6C6";
+    return "#38E86F";
   }
 
   if (state === "submitting") {
-    return "#4CC9FF";
+    return "#FFB74D";
   }
 
-  return isFocused ? "#8CCBFF" : palette.border;
+  return isFocused ? "#FFB74D" : palette.border;
 }
 
 function ModeTabs({
@@ -1056,16 +1056,58 @@ function AuthMotionTuningPanel({
   );
 }
 
+function AuthInlineStatus({
+  stage,
+  message,
+}: {
+  stage: MockFlowStage;
+  message: string | null;
+}) {
+  const { palette } = useTheme();
+
+  if (!message) {
+    return null;
+  }
+
+  const isErrorStage = stage === "localError" || stage === "serverError";
+  const isSuccessStage = stage === "success";
+  const borderColor = isErrorStage
+    ? "rgba(255,93,119,0.55)"
+    : isSuccessStage
+      ? "rgba(53,230,198,0.5)"
+      : "rgba(255,183,77,0.5)";
+  const backgroundColor = isErrorStage
+    ? "rgba(255,93,119,0.12)"
+    : isSuccessStage
+      ? "rgba(53,230,198,0.12)"
+      : "rgba(255,183,77,0.12)";
+  const textColor = isErrorStage
+    ? palette.danger
+    : isSuccessStage
+      ? "#B8FFF1"
+      : "#FFE0A3";
+
+  return (
+    <View style={[styles.authStatusBox, { borderColor, backgroundColor }]}>
+      <Text style={[styles.authStatusText, { color: textColor }]}>{message}</Text>
+    </View>
+  );
+}
+
 function AuthPreview({
   mode,
   tuning,
   onModeChange,
+  onTuningChange,
+  onResetTuning,
 }: {
   mode: AuthMode;
   tuning: MotionTuning;
   onModeChange: (mode: AuthMode) => void;
+  onTuningChange: (tuning: MotionTuning) => void;
+  onResetTuning: () => void;
 }) {
-  const { palette } = useTheme();
+  const { mode: themeMode, palette, toggleMode } = useTheme();
   const copy = getModeCopy(mode);
   const fields = useMemo(() => getFieldConfigs(mode), [mode]);
   const [values, setValues] = useState<AuthValues>(emptyValues);
@@ -1076,6 +1118,7 @@ function AuthPreview({
   const [flowMessage, setFlowMessage] = useState<string | null>(null);
   const [fieldErrors, setFieldErrors] = useState<FieldErrors>({});
   const [shakeKey, setShakeKey] = useState(0);
+  const [showPrototypeSettings, setShowPrototypeSettings] = useState(false);
   const submitTimer = useRef<ReturnType<typeof setTimeout> | null>(null);
 
   const clearSubmitTimer = () => {
@@ -1158,7 +1201,7 @@ function AuthPreview({
     }
 
     if (state === "submitting") {
-      return "Checking shape and state transition…";
+      return "Checking…";
     }
 
     return field.helper;
@@ -1176,9 +1219,7 @@ function AuthPreview({
 
     if (!activeFieldsValid) {
       setFlowStage("localError");
-      setFlowMessage(
-        "Local validation stopped the mock request. Fix the frown fields first.",
-      );
+      setFlowMessage("Fix the red fields first.");
       setShakeKey((current) => current + 1);
       return;
     }
@@ -1186,7 +1227,7 @@ function AuthPreview({
     clearSubmitTimer();
     setIsSubmitting(true);
     setFlowStage("requesting");
-    setFlowMessage("Mock request in progress. No production auth call is made.");
+    setFlowMessage("Checking…");
     submitTimer.current = setTimeout(() => {
       const result = getMockFlowResult(mode, values);
       setIsSubmitting(false);
@@ -1220,9 +1261,7 @@ function AuthPreview({
     setAttempted(true);
     setSuccessMessage(null);
     setFlowStage("localError");
-    setFlowMessage(
-      "Demo local error loaded. The form never reaches the mock request step.",
-    );
+    setFlowMessage("Demo local error loaded.");
     setFieldErrors({});
     setIsSubmitting(false);
     clearSubmitTimer();
@@ -1236,8 +1275,8 @@ function AuthPreview({
     setFlowStage("idle");
     setFlowMessage(
       mode === "reset"
-        ? "Reset uses neutral success copy even for unknown-looking emails. Press submit to test it."
-        : "Server-case values loaded. Press the primary button to see the mocked response.",
+        ? "Reset uses neutral success copy. Press submit to test it."
+        : "Server-case values loaded. Press submit to test it.",
     );
     setFieldErrors({});
     setIsSubmitting(false);
@@ -1256,6 +1295,8 @@ function AuthPreview({
     setShakeKey(0);
   };
 
+  const statusMessage = flowMessage ?? successMessage;
+
   return (
     <View
       style={[
@@ -1264,19 +1305,46 @@ function AuthPreview({
       ]}
     >
       <View style={styles.previewTopRow}>
-        <View
-          style={[
+        <Pressable
+          onPress={() => router.back()}
+          accessibilityRole="button"
+          style={({ pressed }) => [
             styles.backPill,
             {
               borderColor: palette.border,
               backgroundColor: palette.surfaceAlt,
             },
+            pressed ? styles.pressed : null,
+          ]}
+        >
+          <Text style={[styles.backPillText, { color: palette.text }]}>‹ Labs</Text>
+        </Pressable>
+
+        <Pressable
+          onPress={() => setShowPrototypeSettings((current) => !current)}
+          accessibilityRole="button"
+          accessibilityState={{ expanded: showPrototypeSettings }}
+          accessibilityLabel="Toggle prototype settings"
+          style={({ pressed }) => [
+            styles.settingsPill,
+            {
+              borderColor: showPrototypeSettings ? "#35E6C6" : palette.border,
+              backgroundColor: showPrototypeSettings
+                ? "rgba(53,230,198,0.14)"
+                : palette.surfaceAlt,
+            },
+            pressed ? styles.pressed : null,
           ]}
         >
-          <Text style={[styles.backPillText, { color: palette.text }]}>
-            Back
+          <Text
+            style={[
+              styles.settingsPillText,
+              { color: showPrototypeSettings ? "#B8FFF1" : palette.text },
+            ]}
+          >
+            {showPrototypeSettings ? "Close" : "⚙"}
           </Text>
-        </View>
+        </Pressable>
       </View>
 
       <View
@@ -1285,15 +1353,98 @@ function AuthPreview({
         <Text style={styles.tradeBadgeText}>TRADE</Text>
       </View>
 
-      <Text style={[styles.previewTitle, { color: palette.text }]}>
-        {copy.title}
-      </Text>
-      <Text style={[styles.previewSubtitle, { color: palette.muted }]}>
-        {copy.subtitle}
-      </Text>
+      <Text style={[styles.previewTitle, { color: palette.text }]}>{copy.title}</Text>
+      <Text style={[styles.previewSubtitle, { color: palette.muted }]}>{copy.subtitle}</Text>
 
       <ModeTabs activeMode={mode} onChange={onModeChange} />
 
+      {showPrototypeSettings ? (
+        <View
+          style={[
+            styles.prototypeSettingsPanel,
+            { backgroundColor: palette.surfaceAlt, borderColor: palette.border },
+          ]}
+        >
+          <View style={styles.prototypeSettingsHeader}>
+            <View style={styles.prototypeSettingsCopy}>
+              <Text style={[styles.prototypeSettingsTitle, { color: palette.text }]}>Prototype settings</Text>
+              <Text style={[styles.prototypeSettingsSubtitle, { color: palette.muted }]}>Hidden lab controls; close this panel for the clean auth view.</Text>
+            </View>
+            <Pressable
+              onPress={toggleMode}
+              accessibilityRole="button"
+              style={({ pressed }) => [
+                styles.demoButton,
+                { borderColor: palette.border, flexGrow: 0 },
+                pressed ? styles.pressed : null,
+              ]}
+            >
+              <Text style={[styles.demoButtonText, { color: palette.text }]}>{themeMode === "dark" ? "Light" : "Dark"}</Text>
+            </Pressable>
+          </View>
+
+          <AuthMotionTuningPanel
+            tuning={tuning}
+            onChange={onTuningChange}
+            onReset={onResetTuning}
+          />
+
+          <View style={styles.demoControls}>
+            <Pressable
+              onPress={fillValidDemo}
+              accessibilityRole="button"
+              style={({ pressed }) => [
+                styles.demoButton,
+                { borderColor: palette.border },
+                pressed ? styles.pressed : null,
+              ]}
+            >
+              <Text style={[styles.demoButtonText, { color: palette.text }]}>Fill valid</Text>
+            </Pressable>
+            <Pressable
+              onPress={triggerErrorDemo}
+              accessibilityRole="button"
+              style={({ pressed }) => [
+                styles.demoButton,
+                { borderColor: palette.border },
+                pressed ? styles.pressed : null,
+              ]}
+            >
+              <Text style={[styles.demoButtonText, { color: palette.text }]}>Trigger error</Text>
+            </Pressable>
+            <Pressable
+              onPress={fillServerCaseDemo}
+              accessibilityRole="button"
+              style={({ pressed }) => [
+                styles.demoButton,
+                { borderColor: palette.border },
+                pressed ? styles.pressed : null,
+              ]}
+            >
+              <Text style={[styles.demoButtonText, { color: palette.text }]}>Server case</Text>
+            </Pressable>
+            <Pressable
+              onPress={resetDemo}
+              accessibilityRole="button"
+              style={({ pressed }) => [
+                styles.demoButton,
+                { borderColor: palette.border },
+                pressed ? styles.pressed : null,
+              ]}
+            >
+              <Text style={[styles.demoButtonText, { color: palette.text }]}>Reset</Text>
+            </Pressable>
+          </View>
+
+          <AuthMockFlowPanel
+            mode={mode}
+            stage={flowStage}
+            message={flowMessage}
+            fieldErrors={fieldErrors}
+          />
+        </View>
+      ) : null}
+
       <View style={styles.previewFields}>
         {fields.map((field) => {
           const state = getMoodState(field.id);
@@ -1312,18 +1463,7 @@ function AuthPreview({
         })}
       </View>
 
-      <AuthMockFlowPanel
-        mode={mode}
-        stage={flowStage}
-        message={flowMessage}
-        fieldErrors={fieldErrors}
-      />
-
-      {successMessage ? (
-        <View style={styles.successBox}>
-          <Text style={styles.successText}>{successMessage}</Text>
-        </View>
-      ) : null}
+      <AuthInlineStatus stage={flowStage} message={statusMessage} />
 
       <Pressable
         onPress={handleSubmit}
@@ -1331,75 +1471,20 @@ function AuthPreview({
         accessibilityState={{ disabled: isSubmitting }}
         style={({ pressed }) => [
           styles.primaryButton,
-          { backgroundColor: isSubmitting ? "#4CC9FF" : palette.text },
+          { backgroundColor: isSubmitting ? "#FFB74D" : palette.text },
           pressed && !isSubmitting ? styles.pressed : null,
         ]}
       >
         <Text
           style={[
             styles.primaryButtonText,
-            { color: isSubmitting ? "#031018" : palette.background },
+            { color: isSubmitting ? "#1B1001" : palette.background },
           ]}
         >
           {isSubmitting ? "Checking…" : copy.button}
         </Text>
       </Pressable>
 
-      <View style={styles.demoControls}>
-        <Pressable
-          onPress={fillValidDemo}
-          accessibilityRole="button"
-          style={({ pressed }) => [
-            styles.demoButton,
-            { borderColor: palette.border },
-            pressed ? styles.pressed : null,
-          ]}
-        >
-          <Text style={[styles.demoButtonText, { color: palette.text }]}>
-            Fill valid
-          </Text>
-        </Pressable>
-        <Pressable
-          onPress={triggerErrorDemo}
-          accessibilityRole="button"
-          style={({ pressed }) => [
-            styles.demoButton,
-            { borderColor: palette.border },
-            pressed ? styles.pressed : null,
-          ]}
-        >
-          <Text style={[styles.demoButtonText, { color: palette.text }]}>
-            Trigger error
-          </Text>
-        </Pressable>
-        <Pressable
-          onPress={fillServerCaseDemo}
-          accessibilityRole="button"
-          style={({ pressed }) => [
-            styles.demoButton,
-            { borderColor: palette.border },
-            pressed ? styles.pressed : null,
-          ]}
-        >
-          <Text style={[styles.demoButtonText, { color: palette.text }]}>
-            Server case
-          </Text>
-        </Pressable>
-        <Pressable
-          onPress={resetDemo}
-          accessibilityRole="button"
-          style={({ pressed }) => [
-            styles.demoButton,
-            { borderColor: palette.border },
-            pressed ? styles.pressed : null,
-          ]}
-        >
-          <Text style={[styles.demoButtonText, { color: palette.text }]}>
-            Reset
-          </Text>
-        </Pressable>
-      </View>
-
       <View style={styles.noticeBox}>
         <Text style={styles.noticeText}>
           Google sign-in is disabled for the first launch. Please use email
@@ -1454,7 +1539,7 @@ function MotionStateCard({
 }
 
 export function AuthMotionLabScreen() {
-  const { mode: themeMode, palette, toggleMode } = useTheme();
+  const { palette } = useTheme();
   const insets = useSafeAreaInsets();
   const [activeMode, setActiveMode] = useState<AuthMode>("login");
   const [motionTuning, setMotionTuning] =
@@ -1466,112 +1551,25 @@ export function AuthMotionLabScreen() {
         options={{ title: "Auth Motion Lab", headerShown: false }}
       />
 
-      <View
-        style={[
-          styles.header,
-          {
-            paddingTop: insets.top + 16,
-            borderBottomColor: palette.border,
-            backgroundColor: palette.background,
-          },
-        ]}
-      >
-        <View style={styles.headerTopRow}>
-          <Pressable
-            onPress={() => router.back()}
-            accessibilityRole="button"
-            style={({ pressed }) => [
-              styles.headerButton,
-              { borderColor: palette.border },
-              pressed ? styles.pressed : null,
-            ]}
-          >
-            <Text style={[styles.headerButtonText, { color: palette.text }]}>
-              ‹ Labs
-            </Text>
-          </Pressable>
-
-          <Pressable
-            onPress={toggleMode}
-            accessibilityRole="button"
-            style={({ pressed }) => [
-              styles.headerButton,
-              { borderColor: palette.border },
-              pressed ? styles.pressed : null,
-            ]}
-          >
-            <Text style={[styles.headerButtonText, { color: palette.text }]}>
-              {themeMode === "dark" ? "Light" : "Dark"}
-            </Text>
-          </Pressable>
-        </View>
-
-        <Text style={[styles.eyebrow, { color: palette.muted }]}>
-          AUTH MOTION LAB
-        </Text>
-        <Text style={[styles.title, { color: palette.text }]}>
-          Animated smile and frown inputs
-        </Text>
-        <Text style={[styles.subtitle, { color: palette.muted }]}>
-          Isolated playground for the playful auth-page curve idea. Type into
-          the fields or use the demo buttons to test straight, smile, frown,
-          shake, submitting, tuning, and reduced-motion states without touching
-          production auth.
-        </Text>
-      </View>
-
       <ScrollView
         contentContainerStyle={[
           styles.content,
-          { paddingBottom: insets.bottom + 32 },
+          { paddingTop: insets.top + 18, paddingBottom: insets.bottom + 32 },
         ]}
       >
-        <View style={styles.sectionHeader}>
-          <Text style={[styles.sectionTitle, { color: palette.text }]}>
-            Live animated prototype
-          </Text>
-          <Text style={[styles.sectionText, { color: palette.muted }]}>
-            The input value stays straight while empty, smiles when valid,
-            frowns when invalid, and breathes during submit. The mock flows now
-            show local validation, server response, success copy, field-level
-            server errors, motion tuning, and reduced-motion behavior without
-            touching production auth.
-          </Text>
-        </View>
-
-        <ModeTabs activeMode={activeMode} onChange={setActiveMode} />
-        <AuthMotionTuningPanel
-          tuning={motionTuning}
-          onChange={setMotionTuning}
-          onReset={() => setMotionTuning(defaultMotionTuning)}
-        />
         <AuthPreview
           mode={activeMode}
           tuning={motionTuning}
           onModeChange={setActiveMode}
+          onTuningChange={setMotionTuning}
+          onResetTuning={() => setMotionTuning(defaultMotionTuning)}
         />
-
-        <View style={styles.sectionHeader}>
-          <Text style={[styles.sectionTitle, { color: palette.text }]}>
-            Mood states
-          </Text>
-          <Text style={[styles.sectionText, { color: palette.muted }]}>
-            These cards define the three baseline field shapes. The live
-            prototype adds a temporary submitting pulse on top of the smile
-            shape.
-          </Text>
-        </View>
-
-        <View style={styles.stateGrid}>
-          {stateCards.map((card) => (
-            <MotionStateCard key={card.state} {...card} />
-          ))}
-        </View>
       </ScrollView>
     </View>
   );
 }
 
+
 const styles = StyleSheet.create({
   screen: {
     flex: 1,
@@ -1619,10 +1617,9 @@ const styles = StyleSheet.create({
   },
   content: {
     paddingHorizontal: 16,
-    paddingTop: 18,
     gap: 16,
     width: "100%",
-    maxWidth: 920,
+    maxWidth: 560,
     alignSelf: "center",
   },
   sectionHeader: {
@@ -1776,6 +1773,9 @@ const styles = StyleSheet.create({
   },
   previewTopRow: {
     flexDirection: "row",
+    alignItems: "center",
+    justifyContent: "space-between",
+    gap: 12,
   },
   backPill: {
     borderWidth: 1,
@@ -1787,6 +1787,19 @@ const styles = StyleSheet.create({
     fontSize: 13,
     fontWeight: "900",
   },
+  settingsPill: {
+    minWidth: 44,
+    minHeight: 38,
+    borderWidth: 1,
+    borderRadius: 999,
+    alignItems: "center",
+    justifyContent: "center",
+    paddingHorizontal: 13,
+  },
+  settingsPillText: {
+    fontSize: 13,
+    fontWeight: "900",
+  },
   tradeBadge: {
     alignSelf: "flex-start",
     borderWidth: 1,
@@ -1812,6 +1825,30 @@ const styles = StyleSheet.create({
     fontSize: 15,
     lineHeight: 22,
   },
+  prototypeSettingsPanel: {
+    borderWidth: 1,
+    borderRadius: 22,
+    padding: 12,
+    gap: 12,
+  },
+  prototypeSettingsHeader: {
+    flexDirection: "row",
+    alignItems: "flex-start",
+    justifyContent: "space-between",
+    gap: 12,
+  },
+  prototypeSettingsCopy: {
+    flex: 1,
+    gap: 3,
+  },
+  prototypeSettingsTitle: {
+    fontSize: 14,
+    fontWeight: "900",
+  },
+  prototypeSettingsSubtitle: {
+    fontSize: 12,
+    lineHeight: 16,
+  },
   previewFields: {
     gap: 14,
   },
@@ -1956,6 +1993,16 @@ const styles = StyleSheet.create({
     fontSize: 11,
     lineHeight: 15,
   },
+  authStatusBox: {
+    borderWidth: 1,
+    borderRadius: 16,
+    padding: 12,
+  },
+  authStatusText: {
+    fontSize: 13,
+    lineHeight: 18,
+    fontWeight: "800",
+  },
   successBox: {
     borderWidth: 1,
     borderColor: "rgba(53,230,198,0.5)",
