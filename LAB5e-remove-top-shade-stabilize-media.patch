diff --git a/app/square-deck.tsx b/app/square-deck.tsx
index d9b8e78..9075fc4 100644
--- a/app/square-deck.tsx
+++ b/app/square-deck.tsx
@@ -199,7 +199,7 @@ export default function SquareDeckScreen() {
       </View>
 
       <View style={styles.footer}>
-        <Text style={[styles.footerText, { color: palette.muted }]}>LAB5b: blur now covers the full text zone and the text overlay matches the main HelloWhen PlaceCard layout more closely.</Text>
+        <Text style={[styles.footerText, { color: palette.muted }]}>LAB5e: top shade removed; remote image failures now fall back cleanly while blur stays tied to visible media.</Text>
       </View>
     </View>
   );
diff --git a/docs/SQUARE_STACK_DECK_LAB.md b/docs/SQUARE_STACK_DECK_LAB.md
index 410ced0..aff372e 100644
--- a/docs/SQUARE_STACK_DECK_LAB.md
+++ b/docs/SQUARE_STACK_DECK_LAB.md
@@ -193,3 +193,10 @@ The intended result is not a frosted rectangle. It should feel like the image it
 - Removed bridge bands, feather veils, and accent washes that were creating a visible frosted-panel feeling.
 - Kept static-map cards slightly stronger, starting around 46% of card height, while photo cards start around 50%.
 - The intended look is the Image Card rule: almost invisible in the middle, softly visible lower down, strongest near the bottom edge.
+
+
+## LAB5e — Remove top shade and stabilize image fallback
+
+- Removed the separate top shade layer from the square demo card; it was visible as a gray rectangle over every card.
+- Added per-card image failure handling so a failed remote image cleanly falls back to the lab fallback surface instead of leaving the blur/image layers in a half-loaded state.
+- Added a stable recycling key to the main image layer so card identity stays tied to the card id while swiping.
diff --git a/features/square-stack-deck/components/HelloWhenPlaceCardDemo.tsx b/features/square-stack-deck/components/HelloWhenPlaceCardDemo.tsx
index 2dafa1c..20919fc 100644
--- a/features/square-stack-deck/components/HelloWhenPlaceCardDemo.tsx
+++ b/features/square-stack-deck/components/HelloWhenPlaceCardDemo.tsx
@@ -1,4 +1,4 @@
-﻿import React, { memo, useMemo } from "react";
+﻿import React, { memo, useEffect, useMemo, useState } from "react";
 import { Image } from "expo-image";
 import { StyleSheet, Text, View } from "react-native";
 
@@ -120,6 +120,12 @@ function HelloWhenPlaceCardDemoInner({ card }: Props) {
   const isStaticMap = card.mediaKind === "static_map";
   const mediaSurface = isDark ? "#0c1116" : "#dfe6dc";
   const mediaSource = useMemo(() => (card.imageUrl ? { uri: card.imageUrl } : undefined), [card.imageUrl]);
+  const [mediaFailed, setMediaFailed] = useState(false);
+  const visibleMediaSource = mediaFailed ? undefined : mediaSource;
+
+  useEffect(() => {
+    setMediaFailed(false);
+  }, [card.id, card.imageUrl]);
 
   const overlayTextShadow = isDark ? "rgba(0,0,0,0.44)" : "rgba(255,255,255,0.18)";
   const overlayTextShadowRadius = isDark ? 7 : 4;
@@ -173,13 +179,15 @@ function HelloWhenPlaceCardDemoInner({ card }: Props) {
 
   return (
     <View style={[styles.card, { backgroundColor: mediaSurface }]}>
-      {mediaSource ? (
+      {visibleMediaSource ? (
         <Image
-          source={mediaSource}
+          source={visibleMediaSource}
           style={StyleSheet.absoluteFillObject}
           contentFit="cover"
           transition={0}
           cachePolicy="memory-disk"
+          recyclingKey={card.id}
+          onError={() => setMediaFailed(true)}
         />
       ) : (
         <View style={[StyleSheet.absoluteFillObject, styles.fallbackMedia, { backgroundColor: mediaSurface }]}>
@@ -200,8 +208,7 @@ function HelloWhenPlaceCardDemoInner({ card }: Props) {
         </View>
       )}
 
-      <View style={styles.topShade} />
-      <LowerAtmosphere source={mediaSource} isDark={isDark} mediaKind={card.mediaKind} />
+      <LowerAtmosphere source={visibleMediaSource} isDark={isDark} mediaKind={card.mediaKind} />
 
       <View style={styles.contentLayer}>
         <View style={styles.topBar}>
@@ -324,14 +331,6 @@ const styles = StyleSheet.create({
     borderRadius: 999,
     opacity: 0.82,
   },
-  topShade: {
-    position: "absolute",
-    left: 0,
-    right: 0,
-    top: 0,
-    height: "26%",
-    backgroundColor: "rgba(0,0,0,0.10)",
-  },
   atmosphereSlice: {
     position: "absolute",
     left: 0,
