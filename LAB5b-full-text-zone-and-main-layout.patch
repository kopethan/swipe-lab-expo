diff --git a/app/square-deck.tsx b/app/square-deck.tsx
index 414b74d..910470b 100644
--- a/app/square-deck.tsx
+++ b/app/square-deck.tsx
@@ -37,6 +37,10 @@ export default function SquareDeckScreen() {
         title: "Rooftop sunset walk",
         area: "Paris · 11th arrondissement",
         descriptor: "Golden-hour terrace, short stairs, open sky, and a calm route back toward the canal.",
+        subtitle: "Paris 11e, near Canal Saint-Martin, rooftop access, open sky, and an easy return route.",
+        primaryContext: "Paris",
+        contextBadgeLabel: "sunset",
+        modeLabel: "offline",
         mediaKind: "photo",
         imageUrl: "https://images.unsplash.com/photo-1500530855697-b586d89ba3ee?auto=format&fit=crop&w=1200&q=82",
         distanceLabel: "12 min walk",
@@ -49,6 +53,10 @@ export default function SquareDeckScreen() {
         title: "Hidden garden coffee",
         area: "Lyon · Croix-Rousse",
         descriptor: "Small courtyard tables with plants, soft shade, and a good reset stop between plans.",
+        subtitle: "Croix-Rousse courtyard tables, plants, soft shade, and a calm coffee reset.",
+        primaryContext: "Lyon",
+        contextBadgeLabel: "coffee",
+        modeLabel: "offline",
         mediaKind: "photo",
         imageUrl: "https://images.unsplash.com/photo-1494526585095-c41746248156?auto=format&fit=crop&w=1200&q=82",
         distanceLabel: "650 m",
@@ -59,8 +67,12 @@ export default function SquareDeckScreen() {
       {
         id: "sq03",
         title: "Late-night ramen corner",
-        area: "Tokyo · Shinjuku",
+        area: "Alfortville, Créteil, Val-de-Marne, Île-de-France, France métropolitaine",
         descriptor: "A compact food stop near the station, useful when the plan runs later than expected.",
+        subtitle: "Alfortville, Créteil, Val-de-Marne, Île-de-France, France métropolitaine, 94140, France",
+        primaryContext: "94140",
+        contextBadgeLabel: "France",
+        modeLabel: "offline",
         mediaKind: "static_map",
         imageUrl: "https://images.unsplash.com/photo-1542051841857-5f90071e7989?auto=format&fit=crop&w=1200&q=82",
         distanceLabel: "open late",
@@ -73,6 +85,10 @@ export default function SquareDeckScreen() {
         title: "Canal-side book market",
         area: "Amsterdam · Centrum",
         descriptor: "Outdoor stalls, water reflections, and a good low-pressure browse before dinner.",
+        subtitle: "Canal edge, book stalls, calm browsing, and an easy stop before dinner.",
+        primaryContext: "Amsterdam",
+        contextBadgeLabel: "market",
+        modeLabel: "offline",
         mediaKind: "photo",
         imageUrl: "https://images.unsplash.com/photo-1512470876302-972faa2aa9a4?auto=format&fit=crop&w=1200&q=82",
         distanceLabel: "2.1 km",
@@ -85,6 +101,10 @@ export default function SquareDeckScreen() {
         title: "Tiny gallery opening",
         area: "Berlin · Mitte",
         descriptor: "A small indoor place with predictable lighting, useful as a weather-safe card type.",
+        subtitle: "Berlin Mitte, indoor fallback-friendly stop with predictable light and calm pacing.",
+        primaryContext: "Berlin",
+        contextBadgeLabel: "indoor",
+        modeLabel: "offline",
         mediaKind: "fallback",
         distanceLabel: "indoor",
         moodLabel: "art",
@@ -96,6 +116,10 @@ export default function SquareDeckScreen() {
         title: "Morning bakery queue",
         area: "Lisbon · Alfama",
         descriptor: "A simple early stop with warm light, quick service, and nearby benches for a pause.",
+        subtitle: "Alfama bakery line, warm light, quick service, and benches nearby for a pause.",
+        primaryContext: "Lisbon",
+        contextBadgeLabel: "bakery",
+        modeLabel: "offline",
         mediaKind: "photo",
         imageUrl: "https://images.unsplash.com/photo-1509440159596-0249088772ff?auto=format&fit=crop&w=1200&q=82",
         distanceLabel: "8 min walk",
@@ -108,6 +132,10 @@ export default function SquareDeckScreen() {
         title: "Old town photo loop",
         area: "Seoul · Ikseon-dong",
         descriptor: "A short loop of narrow streets, textures, small shops, and easy alternate exits.",
+        subtitle: "Ikseon-dong side streets, texture-heavy corners, and a short map-guided loop.",
+        primaryContext: "Seoul",
+        contextBadgeLabel: "photo",
+        modeLabel: "offline",
         mediaKind: "static_map",
         imageUrl: "https://images.unsplash.com/photo-1538485399081-7191377e8241?auto=format&fit=crop&w=1200&q=82",
         distanceLabel: "loop",
@@ -120,6 +148,10 @@ export default function SquareDeckScreen() {
         title: "Quiet jazz basement",
         area: "New York · West Village",
         descriptor: "Dim room, small tables, later timing, and a card that needs readable lower text.",
+        subtitle: "West Village basement, dim room, small tables, and reliable late-evening timing.",
+        primaryContext: "New York",
+        contextBadgeLabel: "music",
+        modeLabel: "offline",
         mediaKind: "photo",
         imageUrl: "https://images.unsplash.com/photo-1514525253161-7a46d19cd819?auto=format&fit=crop&w=1200&q=82",
         distanceLabel: "after 9 PM",
@@ -132,6 +164,10 @@ export default function SquareDeckScreen() {
         title: "Sunday park picnic",
         area: "Montreal · Plateau",
         descriptor: "Fallback-friendly open area with simple text, no hard dependency on a perfect photo.",
+        subtitle: "Plateau open area, easy fallback card, and simple readable outdoor text treatment.",
+        primaryContext: "Montreal",
+        contextBadgeLabel: "weekend",
+        modeLabel: "offline",
         mediaKind: "fallback",
         distanceLabel: "weekend",
         moodLabel: "open air",
@@ -163,7 +199,7 @@ export default function SquareDeckScreen() {
       </View>
 
       <View style={styles.footer}>
-        <Text style={[styles.footerText, { color: palette.muted }]}>LAB5a: bottom-up blur fade is active with the hard lower-third seam removed. Static OSM-specific simulation stays for LAB6.</Text>
+        <Text style={[styles.footerText, { color: palette.muted }]}>LAB5b: blur now covers the full text zone and the text overlay matches the main HelloWhen PlaceCard layout more closely.</Text>
       </View>
     </View>
   );
diff --git a/docs/SQUARE_STACK_DECK_LAB.md b/docs/SQUARE_STACK_DECK_LAB.md
index 9c3ae86..dd80c67 100644
--- a/docs/SQUARE_STACK_DECK_LAB.md
+++ b/docs/SQUARE_STACK_DECK_LAB.md
@@ -127,3 +127,11 @@ Implementation rules:
 ```
 
 The intended result is not a frosted rectangle. It should feel like the image itself becomes softer and more readable near the bottom.
+
+
+## LAB5b — Full text-zone atmosphere + HelloWhen text layout
+
+- Expanded the atmospheric blur/fill field so the full visible text area sits inside the protected zone.
+- Removed the boxed lower panel styling from the lab card.
+- Replaced the prototype text layout with the main HelloWhen PlaceCard overlay structure: top mode chip, eyebrow row, title, subtitle, and footer tags.
+- Static-map demo cards receive slightly stronger readability protection than photo cards.
diff --git a/features/square-stack-deck/components/HelloWhenPlaceCardDemo.tsx b/features/square-stack-deck/components/HelloWhenPlaceCardDemo.tsx
index a148010..a1f9bfa 100644
--- a/features/square-stack-deck/components/HelloWhenPlaceCardDemo.tsx
+++ b/features/square-stack-deck/components/HelloWhenPlaceCardDemo.tsx
@@ -17,6 +17,10 @@ export type HelloWhenPlaceDemoCard = {
   moodLabel: string;
   tags: string[];
   accent: string;
+  modeLabel?: string;
+  primaryContext?: string;
+  contextBadgeLabel?: string;
+  subtitle?: string;
 };
 
 type Props = {
@@ -25,27 +29,53 @@ type Props = {
   total: number;
 };
 
-const BLUR_SLICE_COUNT = 18;
+const BLUR_SLICE_COUNT = 20;
 
 type LowerAtmosphereProps = {
   source?: { uri: string };
   accent: string;
   isDark: boolean;
+  mediaKind: DemoPlaceMediaKind;
 };
 
-function LowerAtmosphere({ source, accent, isDark }: LowerAtmosphereProps) {
+function clamp(value: number, min: number, max: number) {
+  return Math.min(max, Math.max(min, value));
+}
+
+function LowerAtmosphere({ source, accent, isDark, mediaKind }: LowerAtmosphereProps) {
   const slices = useMemo(() => Array.from({ length: BLUR_SLICE_COUNT }, (_, index) => index), []);
-  const tintBase = isDark ? "rgba(4,6,9,0.64)" : "rgba(255,255,255,0.50)";
-  const bottomVeil = isDark ? "rgba(2,4,7,0.58)" : "rgba(255,255,255,0.42)";
-  const midVeil = isDark ? "rgba(2,4,7,0.20)" : "rgba(255,255,255,0.16)";
+  const isStaticMap = mediaKind === "static_map";
+  const tintBase = isDark
+    ? isStaticMap
+      ? "rgba(3,5,8,0.74)"
+      : "rgba(4,6,9,0.64)"
+    : isStaticMap
+      ? "rgba(255,255,255,0.76)"
+      : "rgba(255,255,255,0.56)";
+  const bottomVeil = isDark
+    ? isStaticMap
+      ? "rgba(2,4,7,0.80)"
+      : "rgba(2,4,7,0.64)"
+    : isStaticMap
+      ? "rgba(255,255,255,0.72)"
+      : "rgba(255,255,255,0.48)";
+  const midVeil = isDark
+    ? isStaticMap
+      ? "rgba(4,7,12,0.24)"
+      : "rgba(4,7,12,0.16)"
+    : isStaticMap
+      ? "rgba(255,255,255,0.18)"
+      : "rgba(255,255,255,0.10)";
 
   return (
-    <View pointerEvents="none" style={styles.lowerAtmosphere}>
+    <View pointerEvents="none" style={[styles.lowerAtmosphere, { height: isStaticMap ? "58%" : "54%" }]}>
       {source ? (
         slices.map((slice) => {
           const progress = (slice + 1) / BLUR_SLICE_COUNT;
           const eased = progress * progress * (3 - 2 * progress);
           const sliceHeight = 100 / BLUR_SLICE_COUNT;
+          const blurOpacity = clamp((eased - 0.03) * (isStaticMap ? 0.84 : 0.76), 0, 1);
+          const tintOpacity = clamp((eased - 0.06) * (isStaticMap ? 0.72 : 0.58), 0, 1);
 
           return (
             <React.Fragment key={`atmosphere-slice-${slice}`}>
@@ -54,8 +84,8 @@ function LowerAtmosphere({ source, accent, isDark }: LowerAtmosphereProps) {
                   styles.atmosphereSlice,
                   {
                     top: `${slice * sliceHeight}%`,
-                    height: `${sliceHeight + 4}%`,
-                    opacity: eased * 0.58,
+                    height: `${sliceHeight + 5}%`,
+                    opacity: blurOpacity,
                   },
                 ]}
               >
@@ -63,7 +93,7 @@ function LowerAtmosphere({ source, accent, isDark }: LowerAtmosphereProps) {
                   source={source}
                   contentFit="cover"
                   transition={0}
-                  blurRadius={4 + eased * 24}
+                  blurRadius={5 + eased * (isStaticMap ? 28 : 22)}
                   style={StyleSheet.absoluteFillObject}
                 />
               </View>
@@ -72,9 +102,9 @@ function LowerAtmosphere({ source, accent, isDark }: LowerAtmosphereProps) {
                   styles.atmosphereSlice,
                   {
                     top: `${slice * sliceHeight}%`,
-                    height: `${sliceHeight + 4}%`,
+                    height: `${sliceHeight + 5}%`,
                     backgroundColor: tintBase,
-                    opacity: eased * 0.48,
+                    opacity: tintOpacity,
                   },
                 ]}
               />
@@ -82,12 +112,25 @@ function LowerAtmosphere({ source, accent, isDark }: LowerAtmosphereProps) {
           );
         })
       ) : (
-        <View style={[StyleSheet.absoluteFillObject, { backgroundColor: isDark ? "rgba(3,5,8,0.42)" : "rgba(255,255,255,0.34)" }]} />
+        <View
+          style={[
+            StyleSheet.absoluteFillObject,
+            { backgroundColor: isDark ? "rgba(3,5,8,0.46)" : "rgba(255,255,255,0.40)" },
+          ]}
+        />
       )}
 
       <View style={[styles.atmosphereFeather, styles.atmosphereFeatherTop, { backgroundColor: midVeil }]} />
       <View style={[styles.atmosphereFeather, styles.atmosphereFeatherBottom, { backgroundColor: bottomVeil }]} />
-      <View style={[styles.atmosphereAccentWash, { backgroundColor: accent }]} />
+      <View
+        style={[
+          styles.atmosphereAccentWash,
+          {
+            backgroundColor: accent,
+            opacity: isStaticMap ? 0.06 : 0.05,
+          },
+        ]}
+      />
     </View>
   );
 }
@@ -95,32 +138,75 @@ function LowerAtmosphere({ source, accent, isDark }: LowerAtmosphereProps) {
 function mediaLabel(kind: DemoPlaceMediaKind) {
   switch (kind) {
     case "photo":
-      return "Photo";
+      return "offline";
     case "static_map":
-      return "Map view";
+      return "offline";
     case "fallback":
-      return "Fallback";
+      return "offline";
     default:
-      return "Media";
+      return "offline";
   }
 }
 
-function HelloWhenPlaceCardDemoInner({ card, index, total }: Props) {
+function HelloWhenPlaceCardDemoInner({ card }: Props) {
   const { palette } = useTheme();
   const isDark = palette.mode === "dark";
+  const isStaticMap = card.mediaKind === "static_map";
   const mediaSurface = isDark ? "#0c1116" : "#dfe6dc";
   const mediaSource = useMemo(() => (card.imageUrl ? { uri: card.imageUrl } : undefined), [card.imageUrl]);
-  const lowerPanelColor = isDark ? "rgba(7,9,12,0.22)" : "rgba(255,255,255,0.22)";
-  const lowerPanelBorder = "rgba(255,255,255,0)";
-  const titleColor = isDark ? "#fff" : "#101010";
-  const bodyColor = isDark ? "rgba(255,255,255,0.76)" : "rgba(0,0,0,0.62)";
-  const pillBg = isDark ? "rgba(255,255,255,0.13)" : "rgba(255,255,255,0.84)";
-  const pillText = isDark ? "rgba(255,255,255,0.86)" : "rgba(0,0,0,0.68)";
+
+  const overlayTextShadow = isDark ? "rgba(0,0,0,0.44)" : "rgba(255,255,255,0.18)";
+  const overlayTextShadowRadius = isDark ? 7 : 4;
+  const titleColor = isDark ? "#ffffff" : "#101010";
+  const bodyColor = isDark ? "rgba(255,255,255,0.84)" : "rgba(18,24,32,0.80)";
+  const eyebrowColor = isDark ? "rgba(255,255,255,0.90)" : "rgba(12,18,28,0.78)";
+
+  const directOverlayPillBg = isStaticMap
+    ? isDark
+      ? "rgba(6,12,22,0.44)"
+      : "rgba(255,255,255,0.84)"
+    : isDark
+      ? "rgba(7,10,14,0.24)"
+      : "rgba(255,255,255,0.18)";
+  const directOverlayPillBorder = isStaticMap
+    ? isDark
+      ? "rgba(255,255,255,0.12)"
+      : "rgba(15,23,42,0.09)"
+    : isDark
+      ? "rgba(255,255,255,0.12)"
+      : "rgba(255,255,255,0.12)";
+
+  const modeChipBg = isStaticMap
+    ? isDark
+      ? "rgba(6,12,22,0.52)"
+      : "rgba(255,255,255,0.92)"
+    : isDark
+      ? "rgba(10,16,24,0.36)"
+      : "rgba(255,255,255,0.86)";
+  const modeChipBorder = isStaticMap
+    ? isDark
+      ? "rgba(255,255,255,0.14)"
+      : "rgba(15,23,42,0.08)"
+    : isDark
+      ? "rgba(255,255,255,0.16)"
+      : "rgba(15,23,42,0.06)";
 
   const fallbackLines = useMemo(() => Array.from({ length: 8 }, (_, lineIndex) => lineIndex), []);
 
+  const model = useMemo(
+    () => ({
+      modeLabel: card.modeLabel ?? mediaLabel(card.mediaKind),
+      primaryContext: card.primaryContext ?? card.area,
+      contextBadgeLabel: card.contextBadgeLabel ?? card.moodLabel,
+      subtitle: card.subtitle ?? card.descriptor,
+      visibleTags: card.tags.slice(0, 3),
+      extraTagCount: Math.max(0, card.tags.length - 3),
+    }),
+    [card.area, card.contextBadgeLabel, card.descriptor, card.mediaKind, card.modeLabel, card.moodLabel, card.primaryContext, card.subtitle, card.tags]
+  );
+
   return (
-    <View style={[styles.card, { backgroundColor: mediaSurface }]}>
+    <View style={[styles.card, { backgroundColor: mediaSurface }]}>      
       {mediaSource ? (
         <Image
           source={mediaSource}
@@ -130,7 +216,7 @@ function HelloWhenPlaceCardDemoInner({ card, index, total }: Props) {
           cachePolicy="memory-disk"
         />
       ) : (
-        <View style={[StyleSheet.absoluteFillObject, styles.fallbackMedia, { backgroundColor: mediaSurface }]}>
+        <View style={[StyleSheet.absoluteFillObject, styles.fallbackMedia, { backgroundColor: mediaSurface }]}>          
           {fallbackLines.map((line) => (
             <View
               key={`fallback-line-${card.id}-${line}`}
@@ -149,47 +235,95 @@ function HelloWhenPlaceCardDemoInner({ card, index, total }: Props) {
       )}
 
       <View style={styles.topShade} />
-      <LowerAtmosphere source={mediaSource} accent={card.accent} isDark={isDark} />
+      <LowerAtmosphere source={mediaSource} accent={card.accent} isDark={isDark} mediaKind={card.mediaKind} />
 
-      <View style={styles.topChrome}>
-        <View style={[styles.statusPill, { backgroundColor: pillBg }]}>
-          <Text style={[styles.statusText, { color: pillText }]}>{mediaLabel(card.mediaKind)}</Text>
-        </View>
-        <Text style={styles.counter}>{`${String(index + 1).padStart(2, "0")} / ${String(total).padStart(2, "0")}`}</Text>
-      </View>
-
-      <View style={[styles.lowerPanel, { backgroundColor: lowerPanelColor, borderColor: lowerPanelBorder }]}>
-        <View style={styles.placeHeaderRow}>
-          <View style={[styles.placeAccent, { backgroundColor: card.accent }]} />
-          <View style={styles.placeTitleBlock}>
-            <Text style={[styles.title, { color: titleColor }]} numberOfLines={2}>
-              {card.title}
-            </Text>
-            <Text style={[styles.area, { color: bodyColor }]} numberOfLines={1}>
-              {card.area}
-            </Text>
+      <View style={styles.contentLayer}>
+        <View style={styles.topBar}>
+          <View style={[styles.modeChip, { backgroundColor: modeChipBg, borderColor: modeChipBorder }]}>            
+            <Text style={[styles.modeChipText, { color: eyebrowColor }]}>{model.modeLabel}</Text>
           </View>
         </View>
 
-        <Text style={[styles.descriptor, { color: bodyColor }]} numberOfLines={2}>
-          {card.descriptor}
-        </Text>
+        <View style={[styles.bottomOverlay, isStaticMap ? styles.bottomOverlayMap : null]}>
+          {model.primaryContext || model.contextBadgeLabel ? (
+            <View style={styles.eyebrowRow}>
+              {model.primaryContext ? (
+                <Text
+                  numberOfLines={1}
+                  style={[
+                    styles.eyebrow,
+                    {
+                      color: eyebrowColor,
+                      textShadowColor: overlayTextShadow,
+                      textShadowOffset: { width: 0, height: 1 },
+                      textShadowRadius: overlayTextShadowRadius,
+                    },
+                  ]}
+                >
+                  {model.primaryContext}
+                </Text>
+              ) : null}
+              {model.contextBadgeLabel ? (
+                <View style={[styles.metaPill, { backgroundColor: directOverlayPillBg, borderColor: directOverlayPillBorder }]}>                  
+                  <Text numberOfLines={1} style={[styles.metaPillText, { color: eyebrowColor }]}>
+                    {model.contextBadgeLabel}
+                  </Text>
+                </View>
+              ) : null}
+            </View>
+          ) : null}
 
-        <View style={styles.infoRow}>
-          <View style={[styles.infoPill, { backgroundColor: pillBg }]}>
-            <Text style={[styles.infoText, { color: pillText }]}>{card.distanceLabel}</Text>
-          </View>
-          <View style={[styles.infoPill, { backgroundColor: pillBg }]}>
-            <Text style={[styles.infoText, { color: pillText }]}>{card.moodLabel}</Text>
+          <View style={styles.captionBlock}>
+            <Text
+              numberOfLines={2}
+              style={[
+                styles.title,
+                {
+                  color: titleColor,
+                  textShadowColor: overlayTextShadow,
+                  textShadowOffset: { width: 0, height: 1 },
+                  textShadowRadius: overlayTextShadowRadius,
+                },
+              ]}
+            >
+              {card.title}
+            </Text>
+            {model.subtitle ? (
+              <Text
+                numberOfLines={2}
+                style={[
+                  styles.subtitle,
+                  {
+                    color: bodyColor,
+                    textShadowColor: overlayTextShadow,
+                    textShadowOffset: { width: 0, height: 1 },
+                    textShadowRadius: overlayTextShadowRadius,
+                  },
+                ]}
+              >
+                {model.subtitle}
+              </Text>
+            ) : null}
           </View>
-        </View>
 
-        <View style={styles.tagsRow}>
-          {card.tags.slice(0, 3).map((tag) => (
-            <View key={`${card.id}-${tag}`} style={[styles.tag, { backgroundColor: isDark ? "rgba(255,255,255,0.11)" : "rgba(255,255,255,0.46)" }]}>
-              <Text style={[styles.tagText, { color: bodyColor }]}>{tag}</Text>
+          {model.visibleTags.length ? (
+            <View style={styles.footerRow}>
+              {model.visibleTags.map((tag) => (
+                <View key={`${card.id}-${tag}`} style={[styles.tagChip, { backgroundColor: directOverlayPillBg, borderColor: directOverlayPillBorder }]}>                  
+                  <Text numberOfLines={1} style={[styles.tagText, { color: eyebrowColor }]}>
+                    {tag}
+                  </Text>
+                </View>
+              ))}
+              {model.extraTagCount > 0 ? (
+                <View style={[styles.tagChip, { backgroundColor: directOverlayPillBg, borderColor: directOverlayPillBorder }]}>                  
+                  <Text numberOfLines={1} style={[styles.tagText, { color: eyebrowColor }]}>
+                    +{model.extraTagCount}
+                  </Text>
+                </View>
+              ) : null}
             </View>
-          ))}
+          ) : null}
         </View>
       </View>
     </View>
@@ -229,15 +363,14 @@ const styles = StyleSheet.create({
     left: 0,
     right: 0,
     top: 0,
-    height: "28%",
-    backgroundColor: "rgba(0,0,0,0.18)",
+    height: "26%",
+    backgroundColor: "rgba(0,0,0,0.12)",
   },
   lowerAtmosphere: {
     position: "absolute",
     left: 0,
     right: 0,
     bottom: 0,
-    height: "68%",
     overflow: "hidden",
   },
   atmosphereSlice: {
@@ -253,117 +386,113 @@ const styles = StyleSheet.create({
   },
   atmosphereFeatherTop: {
     top: 0,
-    height: "34%",
-    opacity: 0.10,
+    height: "36%",
+    opacity: 0.08,
   },
   atmosphereFeatherBottom: {
     bottom: 0,
-    height: "56%",
-    opacity: 0.72,
+    height: "62%",
+    opacity: 0.74,
   },
   atmosphereAccentWash: {
     position: "absolute",
     left: "10%",
     right: "10%",
-    bottom: "4%",
-    height: "52%",
+    bottom: "3%",
+    height: "56%",
     borderRadius: 999,
-    opacity: 0.08,
   },
-  topChrome: {
-    position: "absolute",
-    top: 14,
-    left: 14,
-    right: 14,
+  contentLayer: {
+    ...StyleSheet.absoluteFillObject,
+    justifyContent: "space-between",
+    paddingHorizontal: 14,
+    paddingTop: 14,
+    paddingBottom: 18,
+  },
+  topBar: {
     flexDirection: "row",
+    justifyContent: "flex-start",
     alignItems: "center",
-    justifyContent: "space-between",
-    gap: 12,
   },
-  statusPill: {
+  modeChip: {
+    minHeight: 26,
+    paddingHorizontal: 11,
+    paddingVertical: 5,
     borderRadius: 999,
-    paddingHorizontal: 10,
-    paddingVertical: 6,
-  },
-  statusText: {
-    fontSize: 11,
-    fontWeight: "900",
-    letterSpacing: 0.35,
+    borderWidth: StyleSheet.hairlineWidth,
+    alignItems: "center",
+    justifyContent: "center",
+    maxWidth: "58%",
   },
-  counter: {
-    color: "rgba(255,255,255,0.88)",
+  modeChipText: {
     fontSize: 11,
-    fontWeight: "900",
-    letterSpacing: 1.2,
+    lineHeight: 14,
+    fontWeight: "800",
+    textTransform: "lowercase",
   },
-  lowerPanel: {
-    position: "absolute",
-    left: 12,
-    right: 12,
-    bottom: 12,
-    minHeight: "34%",
-    borderRadius: 24,
-    borderWidth: 0,
-    padding: 13,
+  bottomOverlay: {
+    alignSelf: "stretch",
     gap: 8,
+    paddingRight: 8,
+  },
+  bottomOverlayMap: {
+    paddingBottom: 2,
   },
-  placeHeaderRow: {
+  eyebrowRow: {
     flexDirection: "row",
-    alignItems: "flex-start",
-    gap: 10,
+    alignItems: "center",
+    gap: 6,
+  },
+  eyebrow: {
+    flex: 1,
+    fontSize: 11,
+    lineHeight: 14,
+    fontWeight: "700",
+    textTransform: "uppercase",
+    letterSpacing: 0.6,
   },
-  placeAccent: {
-    marginTop: 5,
-    width: 9,
-    height: 38,
+  metaPill: {
+    paddingHorizontal: 9,
+    paddingVertical: 5,
     borderRadius: 999,
+    borderWidth: StyleSheet.hairlineWidth,
+    maxWidth: "42%",
   },
-  placeTitleBlock: {
-    flex: 1,
-    minWidth: 0,
+  metaPillText: {
+    fontSize: 11,
+    lineHeight: 13,
+    fontWeight: "700",
+  },
+  captionBlock: {
+    gap: 4,
   },
   title: {
     fontSize: 24,
+    lineHeight: 28,
     fontWeight: "900",
-    lineHeight: 27,
     letterSpacing: -0.7,
   },
-  area: {
-    marginTop: 2,
+  subtitle: {
     fontSize: 13,
-    fontWeight: "800",
-  },
-  descriptor: {
-    fontSize: 12,
-    lineHeight: 16,
-    fontWeight: "650",
-  },
-  infoRow: {
-    flexDirection: "row",
-    gap: 7,
-  },
-  infoPill: {
-    borderRadius: 999,
-    paddingHorizontal: 9,
-    paddingVertical: 5,
-  },
-  infoText: {
-    fontSize: 11,
-    fontWeight: "850",
+    lineHeight: 17,
+    fontWeight: "600",
   },
-  tagsRow: {
+  footerRow: {
     flexDirection: "row",
     flexWrap: "wrap",
     gap: 6,
+    marginTop: 2,
   },
-  tag: {
-    borderRadius: 999,
-    paddingHorizontal: 9,
+  tagChip: {
+    maxWidth: 132,
+    paddingHorizontal: 10,
     paddingVertical: 5,
+    borderRadius: 999,
+    borderWidth: StyleSheet.hairlineWidth,
   },
   tagText: {
-    fontSize: 10,
-    fontWeight: "850",
-    letterSpacing: 0.2,
+    fontSize: 11,
+    lineHeight: 13,
+    fontWeight: "700",
   },
 });
