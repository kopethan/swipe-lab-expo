diff --git a/docs/SQUARE_STACK_DECK_LAB.md b/docs/SQUARE_STACK_DECK_LAB.md
index 6011f2d..206ef74 100644
--- a/docs/SQUARE_STACK_DECK_LAB.md
+++ b/docs/SQUARE_STACK_DECK_LAB.md
@@ -217,6 +217,14 @@ The intended result is not a frosted rectangle. It should feel like the image it
 - Static-map cards still receive earlier and stronger protection than photo cards because map tiles can be brighter and lower contrast.
 - The intended result is closer to the reference linear blur cards: the image itself softens behind the text without looking like a gray panel or footer container.
 
+## LAB5i — Targeted text-zone blur and mood preservation
+
+- Tightened the photo blur ramp so the strongest duplicate-image blur sits closer to the actual title, subtitle, and tag area instead of fogging the whole lower half.
+- Reduced photo-card blur opacity, tint opacity, and bottom feather strength again to preserve more of the source image color, especially on neon/night cards.
+- Strengthened text shadow and chip readability slightly so text remains readable without needing a broad gray wash behind it.
+- Kept static-map cards stronger than photo cards, but reduced their bottom feather a little so maps do not become flat panels.
+- The intended result is a more selective text-zone atmosphere: readable type, preserved image mood, no stripe seams, and no visible footer/container rectangle.
+
 ## LAB6 — Vertical/page scroll test routes
 
 Adds three routes that reuse the same square deck engine while placing it under real vertical-scroll pressure:
diff --git a/features/square-stack-deck/components/HelloWhenPlaceCardDemo.tsx b/features/square-stack-deck/components/HelloWhenPlaceCardDemo.tsx
index 24fe0f4..e77a928 100644
--- a/features/square-stack-deck/components/HelloWhenPlaceCardDemo.tsx
+++ b/features/square-stack-deck/components/HelloWhenPlaceCardDemo.tsx
@@ -35,39 +35,39 @@ type LowerAtmosphereProps = {
   mediaKind: DemoPlaceMediaKind;
 };
 
-const LINEAR_BLUR_BANDS = [
-  { top: 40, height: 48, blur: 8, opacity: 0.04, tintOpacity: 0 },
-  { top: 50, height: 44, blur: 14, opacity: 0.08, tintOpacity: 0.012 },
-  { top: 60, height: 40, blur: 22, opacity: 0.13, tintOpacity: 0.032 },
-  { top: 70, height: 34, blur: 32, opacity: 0.20, tintOpacity: 0.058 },
+const TEXT_ZONE_BLUR_BANDS = [
+  { top: 45, height: 40, blur: 6, opacity: 0.024, tintOpacity: 0 },
+  { top: 56, height: 36, blur: 12, opacity: 0.052, tintOpacity: 0.004 },
+  { top: 66, height: 32, blur: 20, opacity: 0.088, tintOpacity: 0.014 },
+  { top: 76, height: 25, blur: 30, opacity: 0.148, tintOpacity: 0.032 },
 ] as const;
 
 function LowerAtmosphere({ source, isDark, mediaKind }: LowerAtmosphereProps) {
   const isStaticMap = mediaKind === "static_map";
-  const staticMapLift = isStaticMap ? -4 : 0;
+  const staticMapLift = isStaticMap ? -5 : 0;
   const imageTint = isDark
     ? isStaticMap
-      ? "rgba(5,7,10,0.36)"
-      : "rgba(8,10,12,0.18)"
+      ? "rgba(5,7,10,0.34)"
+      : "rgba(8,10,12,0.14)"
     : isStaticMap
-      ? "rgba(255,255,255,0.42)"
-      : "rgba(255,255,255,0.12)";
+      ? "rgba(255,255,255,0.38)"
+      : "rgba(255,255,255,0.10)";
   const bottomFeather = isDark
     ? isStaticMap
-      ? "rgba(3,5,8,0.50)"
-      : "rgba(3,5,8,0.22)"
+      ? "rgba(3,5,8,0.46)"
+      : "rgba(3,5,8,0.16)"
     : isStaticMap
-      ? "rgba(255,255,255,0.54)"
-      : "rgba(255,255,255,0.16)";
+      ? "rgba(255,255,255,0.50)"
+      : "rgba(255,255,255,0.10)";
 
   return (
     <View pointerEvents="none" style={StyleSheet.absoluteFillObject}>
       {source
-        ? LINEAR_BLUR_BANDS.map((band, index) => {
+        ? TEXT_ZONE_BLUR_BANDS.map((band, index) => {
             const top = Math.max(0, band.top + staticMapLift);
             const height = Math.min(100 - top, band.height - staticMapLift * 0.25);
-            const blurOpacity = Math.min(0.34, band.opacity * (isStaticMap ? 1.22 : 1));
-            const tintOpacity = Math.min(0.48, band.tintOpacity * (isStaticMap ? 1.82 : 1));
+            const blurOpacity = Math.min(0.32, band.opacity * (isStaticMap ? 1.46 : 1));
+            const tintOpacity = Math.min(0.48, band.tintOpacity * (isStaticMap ? 2.28 : 1));
 
             return (
               <React.Fragment key={`blur-wash-band-${index}`}>
@@ -109,8 +109,8 @@ function LowerAtmosphere({ source, isDark, mediaKind }: LowerAtmosphereProps) {
           styles.bottomWash,
           {
             backgroundColor: bottomFeather,
-            height: isStaticMap ? "32%" : "22%",
-            opacity: isStaticMap ? 0.36 : 0.12,
+            height: isStaticMap ? "30%" : "16%",
+            opacity: isStaticMap ? 0.32 : 0.08,
           },
         ]}
       />
@@ -135,11 +135,11 @@ function HelloWhenPlaceCardDemoInner({ card }: Props) {
     setMediaFailed(false);
   }, [card.id, card.imageUrl]);
 
-  const overlayTextShadow = isDark ? "rgba(0,0,0,0.44)" : "rgba(255,255,255,0.18)";
-  const overlayTextShadowRadius = isDark ? 7 : 4;
+  const overlayTextShadow = isDark ? "rgba(0,0,0,0.50)" : "rgba(255,255,255,0.38)";
+  const overlayTextShadowRadius = isDark ? 8 : 5;
   const titleColor = isDark ? "#ffffff" : "#101010";
-  const bodyColor = isDark ? "rgba(255,255,255,0.84)" : "rgba(18,24,32,0.80)";
-  const eyebrowColor = isDark ? "rgba(255,255,255,0.90)" : "rgba(12,18,28,0.78)";
+  const bodyColor = isDark ? "rgba(255,255,255,0.86)" : "rgba(15,20,28,0.86)";
+  const eyebrowColor = isDark ? "rgba(255,255,255,0.92)" : "rgba(12,18,28,0.84)";
 
   const directOverlayPillBg = isStaticMap
     ? isDark
@@ -147,14 +147,14 @@ function HelloWhenPlaceCardDemoInner({ card }: Props) {
       : "rgba(255,255,255,0.84)"
     : isDark
       ? "rgba(7,10,14,0.24)"
-      : "rgba(255,255,255,0.18)";
+      : "rgba(255,255,255,0.28)";
   const directOverlayPillBorder = isStaticMap
     ? isDark
       ? "rgba(255,255,255,0.12)"
       : "rgba(15,23,42,0.09)"
     : isDark
       ? "rgba(255,255,255,0.12)"
-      : "rgba(255,255,255,0.12)";
+      : "rgba(255,255,255,0.16)";
 
   const modeChipBg = isStaticMap
     ? isDark
