diff --git a/features/square-stack-deck/components/ContinuousSquareStackDeck.tsx b/features/square-stack-deck/components/ContinuousSquareStackDeck.tsx
index 7376136..5f8ef90 100644
--- a/features/square-stack-deck/components/ContinuousSquareStackDeck.tsx
+++ b/features/square-stack-deck/components/ContinuousSquareStackDeck.tsx
@@ -58,8 +58,14 @@ export function ContinuousSquareStackDeck<TCard extends SquareStackDeckCard>({
   onIndexChange,
 }: SquareStackDeckProps<TCard>) {
   const { width, height } = useWindowDimensions();
-  const maxSquare = Math.min(width - 40, height * 0.58, 390);
-  const cardSize = Math.max(280, Math.round(maxSquare));
+
+  // Fit the whole diagonal stack stage inside the phone viewport. LAB1 sized the
+  // card from the viewport and then added 82px for stack depth, which made the
+  // root wider than the screen on devices like iPhone 15.
+  const stageSize = Math.round(Math.max(292, Math.min(width - 36, height * 0.52, 420)));
+  const cardSize = Math.round(stageSize - 72);
@@ -158,9 +164,9 @@ export function ContinuousSquareStackDeck<TCard extends SquareStackDeckCard>({
   }

   return (
-    <View style={[styles.root, { width: cardSize + 82, height: cardSize + 82 }]}>
+    <View style={[styles.root, { width: stageSize, height: stageSize }]}>
       <GestureDetector gesture={gesture}>
-        <Animated.View style={[styles.gestureSurface, { width: cardSize + 82, height: cardSize + 82 }]}>
+        <Animated.View style={[styles.gestureSurface, { width: stageSize, height: stageSize }]}>
           <View pointerEvents="none" style={styles.debugBadge}>
             <View style={styles.debugDot} />
           </View>
@@ -197,6 +203,12 @@ const styles = StyleSheet.create({
   },
   layer: {
     position: "absolute",
+    top: 0,
+    right: 0,
+    bottom: 0,
+    left: 0,
+    alignItems: "center",
+    justifyContent: "center",
   },
   empty: {
     alignItems: "center",
diff --git a/features/square-stack-deck/components/SquareStackCardShell.tsx b/features/square-stack-deck/components/SquareStackCardShell.tsx
index 656482e..b6b5cf7 100644
--- a/features/square-stack-deck/components/SquareStackCardShell.tsx
+++ b/features/square-stack-deck/components/SquareStackCardShell.tsx
@@ -40,7 +40,6 @@ export function SquareStackCardShell({ size, children }: Props) {

 const styles = StyleSheet.create({
   shadow: {
-    position: "absolute",
     shadowOffset: { width: 0, height: 16 },
     shadowOpacity: 0.22,
     shadowRadius: 28,
diff --git a/app/square-deck.tsx b/app/square-deck.tsx
index e8c416a..199157c 100644
--- a/app/square-deck.tsx
+++ b/app/square-deck.tsx
@@ -129,6 +129,7 @@ const styles = StyleSheet.create({
     flex: 1,
     alignItems: "center",
     justifyContent: "center",
+    overflow: "visible",
     paddingHorizontal: 18,
   },
