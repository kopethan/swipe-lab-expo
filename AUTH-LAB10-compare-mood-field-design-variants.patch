diff --git a/features/auth-motion-lab/AuthMotionLabScreen.tsx b/features/auth-motion-lab/AuthMotionLabScreen.tsx
index 3c8d1a8..e98e76a 100644
--- a/features/auth-motion-lab/AuthMotionLabScreen.tsx
+++ b/features/auth-motion-lab/AuthMotionLabScreen.tsx
@@ -37,11 +37,13 @@ type FlowStepState = "idle" | "active" | "done" | "error";
 type CurveIntensity = "soft" | "standard" | "deep";
 type MotionSpeed = "calm" | "balanced" | "quick";
 type GlowIntensity = "off" | "soft" | "strong";
+type FieldStyle = "calmFill" | "innerWave" | "curvedField";
 
 type MotionTuning = {
   curveIntensity: CurveIntensity;
   speed: MotionSpeed;
   glow: GlowIntensity;
+  fieldStyle: FieldStyle;
   shakeEnabled: boolean;
   reducedMotion: boolean;
 };
@@ -91,10 +93,17 @@ const glowChoices: TuningChoice<GlowIntensity>[] = [
   { value: "strong", label: "Strong" },
 ];
 
+const fieldStyleChoices: TuningChoice<FieldStyle>[] = [
+  { value: "calmFill", label: "Calm fill" },
+  { value: "innerWave", label: "Inner wave" },
+  { value: "curvedField", label: "Clean curve" },
+];
+
 const defaultMotionTuning: MotionTuning = {
   curveIntensity: "standard",
   speed: "balanced",
   glow: "soft",
+  fieldStyle: "innerWave",
   shakeEnabled: true,
   reducedMotion: false,
 };
@@ -386,15 +395,33 @@ function getStateProgress(state: MoodState) {
   }
 }
 
-function getBendMetrics(intensity: CurveIntensity) {
+function getMoodVariantMetrics(intensity: CurveIntensity) {
   switch (intensity) {
     case "soft":
-      return { bulgeHeight: 20, bulgeInset: 74, bulgeOffset: 5 };
+      return {
+        waveInset: 58,
+        waveHeight: 14,
+        curveInset: 34,
+        curveHeight: 30,
+        curveOffset: -19,
+      };
     case "deep":
-      return { bulgeHeight: 28, bulgeInset: 48, bulgeOffset: 1 };
+      return {
+        waveInset: 36,
+        waveHeight: 22,
+        curveInset: 18,
+        curveHeight: 42,
+        curveOffset: -27,
+      };
     case "standard":
     default:
-      return { bulgeHeight: 24, bulgeInset: 60, bulgeOffset: 3 };
+      return {
+        waveInset: 46,
+        waveHeight: 18,
+        curveInset: 26,
+        curveHeight: 36,
+        curveOffset: -23,
+      };
   }
 }
 
@@ -478,7 +505,7 @@ function getMoodColor(
   isTyping: boolean,
 ) {
   if (isTyping) {
-    return "#FFB74D";
+    return "#D88917";
   }
 
   if (state === "frown") {
@@ -486,11 +513,11 @@ function getMoodColor(
   }
 
   if (state === "smile") {
-    return "#38E86F";
+    return "#35B86B";
   }
 
   if (state === "submitting") {
-    return "#FFB74D";
+    return "#D88917";
   }
 
   return isFocused ? "#FFB74D" : palette.border;
@@ -504,20 +531,42 @@ function getMoodFillColor(
   const isLight = palette.mode === "light";
 
   if (isTyping || state === "submitting") {
-    return isLight ? "#FFF8E7" : "#34250F";
+    return isLight ? "#FFFBF0" : "#2B210E";
   }
 
   if (state === "smile") {
-    return isLight ? "#F0FBF4" : "#102719";
+    return isLight ? "#F5FCF7" : "#102417";
   }
 
   if (state === "frown") {
-    return isLight ? "#FFF3F5" : "#35131A";
+    return isLight ? "#FFF7F8" : "#30121A";
   }
 
   return palette.surfaceAlt;
 }
 
+function getMoodOverlayColor(
+  state: MoodState,
+  palette: ReturnType<typeof useTheme>["palette"],
+  isTyping: boolean,
+) {
+  const isLight = palette.mode === "light";
+
+  if (isTyping || state === "submitting") {
+    return isLight ? "rgba(255,183,77,0.2)" : "rgba(255,183,77,0.16)";
+  }
+
+  if (state === "smile") {
+    return isLight ? "rgba(53,184,107,0.16)" : "rgba(53,184,107,0.18)";
+  }
+
+  if (state === "frown") {
+    return isLight ? "rgba(255,93,119,0.18)" : "rgba(255,93,119,0.2)";
+  }
+
+  return "transparent";
+}
+
 function getMoodBorderColor(
   state: MoodState,
   palette: ReturnType<typeof useTheme>["palette"],
@@ -763,7 +812,10 @@ function AuthMoodInput({
     isFocused,
     isTyping,
   );
-  const bendMetrics = getBendMetrics(tuning.curveIntensity);
+  const variantMetrics = getMoodVariantMetrics(tuning.curveIntensity);
+  const showInnerWave = tuning.fieldStyle === "innerWave";
+  const showCurvedField = tuning.fieldStyle === "curvedField";
+  const overlayColor = getMoodOverlayColor(visualState, palette, isTyping);
   const smileOpacity = progress.interpolate({
     inputRange: [0, 1, 2, 3],
     outputRange: [0, 0.9, 0, 0.55],
@@ -830,39 +882,75 @@ function AuthMoodInput({
             },
           ]}
         />
-        <Animated.View
-          pointerEvents="none"
-          style={[
-            styles.inputBendBulge,
-            styles.inputSmileBulge,
-            {
-              left: bendMetrics.bulgeInset,
-              right: bendMetrics.bulgeInset,
-              height: bendMetrics.bulgeHeight,
-              bottom: bendMetrics.bulgeOffset,
-              backgroundColor: moodFillColor,
-              borderColor: moodBorderColor,
-              opacity: smileOpacity,
-              transform: [{ scaleX: smileScaleX }, { scaleY: smileScaleY }],
-            },
-          ]}
-        />
-        <Animated.View
-          pointerEvents="none"
-          style={[
-            styles.inputBendBulge,
-            styles.inputFrownBulge,
-            {
-              left: bendMetrics.bulgeInset,
-              right: bendMetrics.bulgeInset,
-              height: bendMetrics.bulgeHeight,
-              top: bendMetrics.bulgeOffset,
-              backgroundColor: moodFillColor,
-              borderColor: moodBorderColor,
-              opacity: frownOpacity,
-            },
-          ]}
-        />
+        {showCurvedField ? (
+          <>
+            <Animated.View
+              pointerEvents="none"
+              style={[
+                styles.inputCurveField,
+                {
+                  left: variantMetrics.curveInset,
+                  right: variantMetrics.curveInset,
+                  height: variantMetrics.curveHeight,
+                  bottom: variantMetrics.curveOffset,
+                  backgroundColor: overlayColor,
+                  opacity: smileOpacity,
+                  transform: [{ scaleX: smileScaleX }, { scaleY: smileScaleY }],
+                },
+              ]}
+            />
+            <Animated.View
+              pointerEvents="none"
+              style={[
+                styles.inputCurveField,
+                {
+                  left: variantMetrics.curveInset,
+                  right: variantMetrics.curveInset,
+                  height: variantMetrics.curveHeight,
+                  top: variantMetrics.curveOffset,
+                  backgroundColor: overlayColor,
+                  opacity: frownOpacity,
+                },
+              ]}
+            />
+          </>
+        ) : null}
+
+        {showInnerWave ? (
+          <>
+            <Animated.View
+              pointerEvents="none"
+              style={[
+                styles.inputMoodWave,
+                styles.inputSmileWave,
+                {
+                  left: variantMetrics.waveInset,
+                  right: variantMetrics.waveInset,
+                  height: variantMetrics.waveHeight,
+                  bottom: 7,
+                  borderColor: moodColor,
+                  opacity: smileOpacity,
+                  transform: [{ scaleX: smileScaleX }, { scaleY: smileScaleY }],
+                },
+              ]}
+            />
+            <Animated.View
+              pointerEvents="none"
+              style={[
+                styles.inputMoodWave,
+                styles.inputFrownWave,
+                {
+                  left: variantMetrics.waveInset,
+                  right: variantMetrics.waveInset,
+                  height: variantMetrics.waveHeight,
+                  top: 7,
+                  borderColor: moodColor,
+                  opacity: frownOpacity,
+                },
+              ]}
+            />
+          </>
+        ) : null}
 
         <TextInput
           value={value}
@@ -1236,9 +1324,9 @@ function AuthMotionTuningPanel({
             Motion tuning
           </Text>
           <Text style={[styles.tuningIntro, { color: palette.muted }]}>
-            Test how playful the auth input should feel before moving it into
-            production. Reduced motion keeps the same states but removes shake,
-            pulse, and long transitions.
+            Compare three field treatments: calm fill, an inner mood wave, and a
+            cleaner curved-field layer. Reduced motion keeps the same states but
+            removes shake, pulse, and long transitions.
           </Text>
         </View>
         <Pressable
@@ -1258,7 +1346,13 @@ function AuthMotionTuningPanel({
 
       <View style={styles.tuningGrid}>
         <TuningOptionGroup
-          label="Field bend"
+          label="Field style"
+          value={tuning.fieldStyle}
+          options={fieldStyleChoices}
+          onChange={(fieldStyle) => update({ fieldStyle })}
+        />
+        <TuningOptionGroup
+          label="Curve strength"
           value={tuning.curveIntensity}
           options={curveChoices}
           onChange={(curveIntensity) => update({ curveIntensity })}
@@ -1593,7 +1687,13 @@ function AuthPreview({
           <Text
             style={[
               styles.settingsPillText,
-              { color: showPrototypeSettings ? "#B8FFF1" : palette.text },
+              {
+                color: showPrototypeSettings
+                  ? themeMode === "light"
+                    ? "#0F766E"
+                    : "#B8FFF1"
+                  : palette.text,
+              },
             ]}
           >
             {showPrototypeSettings ? "Close" : "⚙"}
@@ -2170,41 +2270,46 @@ const styles = StyleSheet.create({
     textTransform: "uppercase",
   },
   inputShell: {
-    minHeight: 52,
+    minHeight: 48,
     justifyContent: "center",
     paddingHorizontal: 14,
     position: "relative",
     overflow: "hidden",
-    borderRadius: 22,
+    borderRadius: 18,
     shadowRadius: 8,
     shadowOffset: { width: 0, height: 4 },
   },
   inputFill: {
     position: "absolute",
-    top: 6,
+    top: 0,
     right: 0,
-    bottom: 6,
+    bottom: 0,
     left: 0,
-    borderRadius: 17,
+    borderRadius: 18,
     borderWidth: 1,
   },
-  inputBendBulge: {
+  inputCurveField: {
     position: "absolute",
-    borderWidth: 1,
     borderRadius: 999,
   },
-  inputSmileBulge: {
+  inputMoodWave: {
+    position: "absolute",
+    borderRadius: 999,
+    borderWidth: 1.5,
+    opacity: 0,
+  },
+  inputSmileWave: {
     borderTopWidth: 0,
     borderLeftWidth: 0,
     borderRightWidth: 0,
   },
-  inputFrownBulge: {
+  inputFrownWave: {
     borderBottomWidth: 0,
     borderLeftWidth: 0,
     borderRightWidth: 0,
   },
   textInput: {
-    minHeight: 48,
+    minHeight: 46,
     paddingVertical: 0,
     fontSize: 14,
     lineHeight: 20,
