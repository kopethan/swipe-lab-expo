diff --git a/features/auth-motion-lab/AuthMotionLabScreen.tsx b/features/auth-motion-lab/AuthMotionLabScreen.tsx
index aa3b67d..c398b6c 100644
--- a/features/auth-motion-lab/AuthMotionLabScreen.tsx
+++ b/features/auth-motion-lab/AuthMotionLabScreen.tsx
@@ -443,7 +443,12 @@ function getMoodColor(
   state: MoodState,
   palette: ReturnType<typeof useTheme>["palette"],
   isFocused: boolean,
+  isTyping: boolean,
 ) {
+  if (isTyping) {
+    return "#FFB74D";
+  }
+
   if (state === "frown") {
     return palette.danger;
   }
@@ -513,18 +518,62 @@ function AuthMoodInput({
 }) {
   const { palette } = useTheme();
   const [isFocused, setIsFocused] = useState(false);
-  const progress = useRef(new Animated.Value(getStateProgress(state))).current;
+  const [isTyping, setIsTyping] = useState(false);
+  const typingTimer = useRef<ReturnType<typeof setTimeout> | null>(null);
+  const visualState: MoodState =
+    isTyping && state !== "submitting" ? "straight" : state;
+  const progress = useRef(
+    new Animated.Value(getStateProgress(visualState)),
+  ).current;
   const submitPulse = useRef(new Animated.Value(0)).current;
   const shake = useRef(new Animated.Value(0)).current;
 
   useEffect(() => {
     Animated.timing(progress, {
-      toValue: getStateProgress(state),
-      duration: getTransitionDuration(state, tuning),
+      toValue: getStateProgress(visualState),
+      duration: getTransitionDuration(visualState, tuning),
       easing: Easing.out(Easing.cubic),
       useNativeDriver: true,
     }).start();
-  }, [progress, state, tuning]);
+  }, [progress, visualState, tuning]);
+
+  useEffect(
+    () => () => {
+      if (typingTimer.current) {
+        clearTimeout(typingTimer.current);
+      }
+    },
+    [],
+  );
+
+  const handleChangeText = (nextValue: string) => {
+    onChangeText(nextValue);
+
+    if (state === "submitting") {
+      return;
+    }
+
+    setIsTyping(true);
+
+    if (typingTimer.current) {
+      clearTimeout(typingTimer.current);
+    }
+
+    typingTimer.current = setTimeout(() => {
+      setIsTyping(false);
+      typingTimer.current = null;
+    }, 1000);
+  };
+
+  const handleBlur = () => {
+    if (typingTimer.current) {
+      clearTimeout(typingTimer.current);
+      typingTimer.current = null;
+    }
+
+    setIsTyping(false);
+    setIsFocused(false);
+  };
 
   useEffect(() => {
     if (state !== "submitting" || tuning.reducedMotion) {
@@ -598,10 +647,13 @@ function AuthMoodInput({
     ]).start();
   }, [shake, shakeKey, state, tuning]);
 
-  const moodColor = getMoodColor(state, palette, isFocused);
+  const moodColor = getMoodColor(visualState, palette, isFocused, isTyping);
   const curveScale = getCurveScale(tuning.curveIntensity);
-  const stateLabel =
-    tuning.reducedMotion && state === "submitting" ? "checking" : state;
+  const stateLabel = isTyping
+    ? "typing"
+    : tuning.reducedMotion && state === "submitting"
+      ? "checking"
+      : state;
   const straightOpacity = progress.interpolate({
     inputRange: [0, 1, 2, 3],
     outputRange: [1, 0.08, 0.05, 0.3],
@@ -622,6 +674,16 @@ function AuthMoodInput({
     inputRange: [0, 1],
     outputRange: [0.94, 1.02],
   });
+  const fieldBodyTranslateY = progress.interpolate({
+    inputRange: [0, 1, 2, 3],
+    outputRange: [0, 4, -4, 2],
+  });
+  const fieldBodyScaleY = progress.interpolate({
+    inputRange: [0, 1, 2, 3],
+    outputRange: [1, 0.95, 0.95, 0.98],
+  });
+  const visibleHelper =
+    isTyping && state !== "submitting" ? field.helper : helper;
 
   return (
     <Animated.View
@@ -640,45 +702,37 @@ function AuthMoodInput({
         style={[
           styles.inputShell,
           {
-            borderColor: moodColor,
-            backgroundColor: palette.surfaceAlt,
             shadowColor: moodColor,
             shadowOpacity:
-              Platform.OS === "ios" ? getGlowOpacity(state, tuning) : 0,
+              Platform.OS === "ios" ? getGlowOpacity(visualState, tuning) : 0,
           },
         ]}
       >
-        <TextInput
-          value={value}
-          onChangeText={onChangeText}
-          onFocus={() => setIsFocused(true)}
-          onBlur={() => setIsFocused(false)}
-          placeholder={field.placeholder}
-          placeholderTextColor={palette.muted}
-          keyboardType={field.keyboardType}
-          secureTextEntry={field.secureTextEntry}
-          autoCapitalize="none"
-          autoCorrect={false}
-          editable={state !== "submitting"}
+        <Animated.View
+          pointerEvents="none"
           style={[
-            styles.textInput,
-            { color: palette.text },
-            Platform.OS === "web" ? styles.webTextInput : null,
+            styles.inputFill,
+            {
+              backgroundColor: palette.surfaceAlt,
+              transform: [
+                { translateY: fieldBodyTranslateY },
+                { scaleY: fieldBodyScaleY },
+              ],
+            },
           ]}
         />
-      </View>
-
-      <View style={styles.moodTrack} pointerEvents="none">
         <Animated.View
+          pointerEvents="none"
           style={[
-            styles.moodStraightStroke,
-            { backgroundColor: moodColor, opacity: straightOpacity },
+            styles.inputStraightFrame,
+            { borderColor: moodColor, opacity: straightOpacity },
           ]}
         />
         <Animated.View
+          pointerEvents="none"
           style={[
-            styles.moodCurveStroke,
-            styles.moodCurveSmile,
+            styles.inputCurveFrame,
+            styles.inputSmileFrame,
             {
               borderColor: moodColor,
               opacity: smileOpacity,
@@ -691,9 +745,10 @@ function AuthMoodInput({
           ]}
         />
         <Animated.View
+          pointerEvents="none"
           style={[
-            styles.moodCurveStroke,
-            styles.moodCurveFrown,
+            styles.inputCurveFrame,
+            styles.inputFrownFrame,
             {
               borderColor: moodColor,
               opacity: frownOpacity,
@@ -701,16 +756,35 @@ function AuthMoodInput({
             },
           ]}
         />
+
+        <TextInput
+          value={value}
+          onChangeText={handleChangeText}
+          onFocus={() => setIsFocused(true)}
+          onBlur={handleBlur}
+          placeholder={field.placeholder}
+          placeholderTextColor={palette.muted}
+          keyboardType={field.keyboardType}
+          secureTextEntry={field.secureTextEntry}
+          autoCapitalize="none"
+          autoCorrect={false}
+          editable={state !== "submitting"}
+          style={[
+            styles.textInput,
+            { color: palette.text },
+            Platform.OS === "web" ? styles.webTextInput : null,
+          ]}
+        />
       </View>
 
-      {helper ? (
+      {visibleHelper ? (
         <Text
           style={[
             styles.helper,
-            { color: state === "frown" ? palette.danger : palette.muted },
+            { color: visualState === "frown" ? palette.danger : palette.muted },
           ]}
         >
-          {helper}
+          {visibleHelper}
         </Text>
       ) : null}
     </Animated.View>
@@ -1872,59 +1946,61 @@ const styles = StyleSheet.create({
     textTransform: "uppercase",
   },
   inputShell: {
-    minHeight: 46,
-    borderWidth: 1,
-    borderRadius: 16,
+    minHeight: 56,
     justifyContent: "center",
     paddingHorizontal: 14,
-    shadowRadius: 12,
-    shadowOffset: { width: 0, height: 6 },
-  },
-  textInput: {
-    minHeight: 44,
-    paddingVertical: 0,
-    fontSize: 14,
-    fontWeight: "800",
-  },
-  webTextInput: {
-    outlineStyle: "none",
-  } as any,
-  moodTrack: {
-    alignSelf: "center",
-    width: "82%",
-    height: 15,
-    marginTop: -5,
     position: "relative",
     overflow: "visible",
+    shadowRadius: 12,
+    shadowOffset: { width: 0, height: 6 },
   },
-  moodStraightStroke: {
+  inputFill: {
     position: "absolute",
-    top: 6,
+    top: 5,
+    right: 0,
+    bottom: 5,
     left: 0,
+    borderRadius: 18,
+  },
+  inputStraightFrame: {
+    position: "absolute",
+    top: 5,
     right: 0,
-    height: 2,
-    borderRadius: 999,
+    bottom: 5,
+    left: 0,
+    borderWidth: 1,
+    borderRadius: 18,
   },
-  moodCurveStroke: {
+  inputCurveFrame: {
     position: "absolute",
     left: 0,
     right: 0,
-    height: 15,
-    borderLeftWidth: 2,
-    borderRightWidth: 2,
-  },
-  moodCurveSmile: {
-    top: -1,
-    borderBottomWidth: 2,
-    borderBottomLeftRadius: 999,
-    borderBottomRightRadius: 999,
+    height: 56,
+    borderLeftWidth: 1,
+    borderRightWidth: 1,
   },
-  moodCurveFrown: {
+  inputSmileFrame: {
     top: 1,
-    borderTopWidth: 2,
-    borderTopLeftRadius: 999,
-    borderTopRightRadius: 999,
+    borderBottomWidth: 1,
+    borderBottomLeftRadius: 46,
+    borderBottomRightRadius: 46,
+  },
+  inputFrownFrame: {
+    bottom: 1,
+    borderTopWidth: 1,
+    borderTopLeftRadius: 46,
+    borderTopRightRadius: 46,
+  },
+  textInput: {
+    minHeight: 50,
+    paddingVertical: 0,
+    fontSize: 14,
+    fontWeight: "800",
+    zIndex: 2,
   },
+  webTextInput: {
+    outlineStyle: "none",
+  } as any,
   helper: {
     fontSize: 12,
     lineHeight: 16,
