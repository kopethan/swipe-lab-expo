diff --git a/features/auth-motion-lab/AuthMotionLabScreen.tsx b/features/auth-motion-lab/AuthMotionLabScreen.tsx
index c398b6c..e313d40 100644
--- a/features/auth-motion-lab/AuthMotionLabScreen.tsx
+++ b/features/auth-motion-lab/AuthMotionLabScreen.tsx
@@ -439,6 +439,42 @@ function getGlowOpacity(state: MoodState, tuning: MotionTuning) {
   return tuning.glow === "strong" ? 0.34 : 0.18;
 }
 
+function getTextCurveAmplitude(intensity: CurveIntensity) {
+  switch (intensity) {
+    case "soft":
+      return 6;
+    case "deep":
+      return 13;
+    case "standard":
+    default:
+      return 9;
+  }
+}
+
+function getTextCurveRotation(intensity: CurveIntensity) {
+  switch (intensity) {
+    case "soft":
+      return 3.5;
+    case "deep":
+      return 7.5;
+    case "standard":
+    default:
+      return 5.5;
+  }
+}
+
+function getCurvedDisplayValue(field: FieldConfig, value: string) {
+  if (!value) {
+    return "";
+  }
+
+  if (field.secureTextEntry) {
+    return "•".repeat(value.length);
+  }
+
+  return value;
+}
+
 function getMoodColor(
   state: MoodState,
   palette: ReturnType<typeof useTheme>["palette"],
@@ -527,6 +563,27 @@ function AuthMoodInput({
   ).current;
   const submitPulse = useRef(new Animated.Value(0)).current;
   const shake = useRef(new Animated.Value(0)).current;
+  const textBendProgress = useRef(new Animated.Value(0)).current;
+
+  const clearTypingTimer = () => {
+    if (typingTimer.current) {
+      clearTimeout(typingTimer.current);
+      typingTimer.current = null;
+    }
+  };
+
+  const holdStraightWhileEditing = () => {
+    if (state === "submitting") {
+      return;
+    }
+
+    setIsTyping(true);
+    clearTypingTimer();
+    typingTimer.current = setTimeout(() => {
+      setIsTyping(false);
+      typingTimer.current = null;
+    }, 1000);
+  };
 
   useEffect(() => {
     Animated.timing(progress, {
@@ -539,38 +596,35 @@ function AuthMoodInput({
 
   useEffect(
     () => () => {
-      if (typingTimer.current) {
-        clearTimeout(typingTimer.current);
-      }
+      clearTypingTimer();
     },
     [],
   );
 
-  const handleChangeText = (nextValue: string) => {
-    onChangeText(nextValue);
-
-    if (state === "submitting") {
+  useEffect(() => {
+    if (state !== "submitting") {
       return;
     }
 
-    setIsTyping(true);
-
-    if (typingTimer.current) {
-      clearTimeout(typingTimer.current);
-    }
+    clearTypingTimer();
+    setIsTyping(false);
+  }, [state]);
 
-    typingTimer.current = setTimeout(() => {
-      setIsTyping(false);
-      typingTimer.current = null;
-    }, 1000);
+  const handleChangeText = (nextValue: string) => {
+    onChangeText(nextValue);
+    holdStraightWhileEditing();
   };
 
-  const handleBlur = () => {
-    if (typingTimer.current) {
-      clearTimeout(typingTimer.current);
-      typingTimer.current = null;
+  const handleFocus = () => {
+    setIsFocused(true);
+
+    if (value.length > 0) {
+      holdStraightWhileEditing();
     }
+  };
 
+  const handleBlur = () => {
+    clearTypingTimer();
     setIsTyping(false);
     setIsFocused(false);
   };
@@ -684,6 +738,27 @@ function AuthMoodInput({
   });
   const visibleHelper =
     isTyping && state !== "submitting" ? field.helper : helper;
+  const displayValue = getCurvedDisplayValue(field, value);
+  const showCurvedText =
+    displayValue.length > 0 &&
+    !isTyping &&
+    (visualState === "smile" || visualState === "frown");
+  const textCurveDirection = visualState === "smile" ? 1 : -1;
+  const textCurveAmplitude = getTextCurveAmplitude(tuning.curveIntensity);
+  const textCurveRotation = getTextCurveRotation(tuning.curveIntensity);
+  const textCharacters = Array.from(displayValue);
+  const textCenterIndex = Math.max((textCharacters.length - 1) / 2, 1);
+
+  useEffect(() => {
+    Animated.timing(textBendProgress, {
+      toValue: showCurvedText ? 1 : 0,
+      duration: tuning.reducedMotion
+        ? 0
+        : Math.round(220 * getSpeedMultiplier(tuning.speed)),
+      easing: Easing.out(Easing.cubic),
+      useNativeDriver: true,
+    }).start();
+  }, [showCurvedText, textBendProgress, tuning]);
 
   return (
     <Animated.View
@@ -760,7 +835,7 @@ function AuthMoodInput({
         <TextInput
           value={value}
           onChangeText={handleChangeText}
-          onFocus={() => setIsFocused(true)}
+          onFocus={handleFocus}
           onBlur={handleBlur}
           placeholder={field.placeholder}
           placeholderTextColor={palette.muted}
@@ -771,10 +846,66 @@ function AuthMoodInput({
           editable={state !== "submitting"}
           style={[
             styles.textInput,
-            { color: palette.text },
+            { color: showCurvedText ? "transparent" : palette.text },
             Platform.OS === "web" ? styles.webTextInput : null,
           ]}
         />
+
+        {displayValue.length > 0 ? (
+          <Animated.View
+            pointerEvents="none"
+            style={[
+              styles.curvedTextLayer,
+              { opacity: textBendProgress },
+            ]}
+          >
+            <View style={styles.curvedTextRow}>
+              {textCharacters.map((char, index) => {
+                const normalizedIndex =
+                  textCharacters.length <= 1
+                    ? 0
+                    : (index - textCenterIndex) / textCenterIndex;
+                const centerWeight = Math.max(
+                  0,
+                  1 - Math.abs(normalizedIndex),
+                );
+                const translateY =
+                  textCurveDirection * textCurveAmplitude * centerWeight;
+                const rotate =
+                  normalizedIndex * textCurveRotation * textCurveDirection;
+
+                return (
+                  <Animated.Text
+                    key={`${char}-${index}`}
+                    numberOfLines={1}
+                    style={[
+                      styles.curvedTextChar,
+                      {
+                        color: moodColor,
+                        transform: [
+                          {
+                            translateY: textBendProgress.interpolate({
+                              inputRange: [0, 1],
+                              outputRange: [0, translateY],
+                            }),
+                          },
+                          {
+                            rotate: textBendProgress.interpolate({
+                              inputRange: [0, 1],
+                              outputRange: ["0deg", `${rotate}deg`],
+                            }),
+                          },
+                        ],
+                      },
+                    ]}
+                  >
+                    {char === " " ? "\u00A0" : char}
+                  </Animated.Text>
+                );
+              })}
+            </View>
+          </Animated.View>
+        ) : null}
       </View>
 
       {visibleHelper ? (
@@ -1998,6 +2129,27 @@ const styles = StyleSheet.create({
     fontWeight: "800",
     zIndex: 2,
   },
+  curvedTextLayer: {
+    position: "absolute",
+    top: 3,
+    right: 14,
+    bottom: 3,
+    left: 14,
+    zIndex: 3,
+    justifyContent: "center",
+    overflow: "hidden",
+  },
+  curvedTextRow: {
+    minHeight: 36,
+    flexDirection: "row",
+    alignItems: "center",
+    overflow: "hidden",
+  },
+  curvedTextChar: {
+    fontSize: 14,
+    lineHeight: 22,
+    fontWeight: "900",
+  },
   webTextInput: {
     outlineStyle: "none",
   } as any,
